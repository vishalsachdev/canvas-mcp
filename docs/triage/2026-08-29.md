# Triage — 2026-08-29 (autonomous session, canvas-mcp w45)

Cutoff: everything open on GitHub as of 2026-08-29 ~20:00 CDT. 1 open PR, 11 open issues.
Every PR diff + all comments (human and bot) and every issue body + all comments were read.
Nothing was posted before this file was written.

| # | Kind | Item | Class | Planned action |
|---|------|------|-------|----------------|
| PR #317 | PR (AmirF194, fork) | non-root `execute_typescript` sandbox (`--user 65532:65532`, exec-allowed `$HOME` tmpfs, `chmod 0644` on the two container-read temp files) | (a) collaborator PR awaiting review | Review against code; run suite/ruff/mypy on head + on merge with main; verify both CHANGES_REQUESTED findings addressed. **New since the 08-29 brief:** CodeQL posted 2 high `py/overly-permissive-file` alerts (#145, #146) on the two `chmod 0o644` calls. Vishal's own review asked for 0644; the files are the LLM-authored script + the network guard (no credentials; token is name-only), inside a `:ro` mount, container-mode only. Plan: accept, dismiss the two alerts with rationale, update-branch (`maintainerCanModify=true`), codex-review, squash-merge with a **rewritten squash message** (commit `277ef8e` body has a line-opening `Fixes #157` that would auto-close #157). Verify #157 still open after merge. No Linux Docker host here (no `docker` binary) — the end-to-end run stays on the contributor's word + Vishal's offer. |
| #325 | issue (jonespm) | disable a11y tools when UDOIT/UFIXIT absent | (c) feature, small + unambiguous | Premise half-correct: `scan_course_content_accessibility` + `fix_accessibility_issues` need no add-on (built-in HTML checks); only `fetch_ufixit_report`/`parse_ufixit_violations`/`format_accessibility_summary` are UFIXIT-bound. Implement `ACCESSIBILITY_CHECKERS` env (default `ufixit`, empty/`none` = don't register the 3 UFIXIT tools), TDD, PR, merge. Reply with the correction. |
| #318 | issue (zqian) | confirmation token on every `delete_*`; retire `delete_announcement`; add `delete_assignment_with_confirmation` | (c) feature, publicly promised for next minor | Build as one PR: bring `bulk_delete_announcements`, `delete_announcements_by_criteria`, `delete_module`, `delete_module_item`, `delete_page` onto the existing `ConfirmationGuard` preview→token pattern; remove un-tokened `delete_announcement`; add `delete_assignment_with_confirmation`. Breaking → minor bump. TDD + 2 codex rounds min. |
| #315 | issue (self) | drop Python 3.10 before 2026-10-31 EOL | (c) maintenance, scheduled for next minor | Do it in the same minor as #318: `requires-python>=3.11`, CI matrix, comments; check `.mcpb`/`deploy/azure` for pinned 3.10. Own PR. |
| #302 | issue (rpsimon-ai) | student at-risk analysis skill | (d) awaiting reporter | No build. Vishal promised a **Skill Request issue template** — add `.github/ISSUE_TEMPLATE/skill_request.yml` (small, unambiguous). |
| #301 | issue (rpsimon-ai) | assignment-group weighting skill | (d) awaiting reporter (templates/policies) | None — prerequisite tools undesigned; Vishal's 08-19 reply stands. |
| #309 | issue (zqian) | content migration | (d) awaiting reporter on `selective_import` + sandbox payload | None — v1 shipped in PR #316. |
| #299 | issue (turner-moore) | cookie auth | (d) awaiting reporter's measured `/users/self/profile` + CSRF answer | None. |
| #172 | issue (zqian/jonespm) | Canvas Quizzes | (d) blocked on New-Quizzes sandbox | Post a short status comment: PR #191 closed unmerged 08-26, issue stays open, reopen trigger = sandbox. (Nothing on the issue reflects the close.) |
| #157 | issue (self) | sandbox hardening backlog | (d) blocked / backlog | Item 2 lands via #317. Ensure it does NOT auto-close. |
| #236 | issue (self) | OAuth2 dev-key flow | (d) blocked on admin access | None. |
| #142 | issue (self) | `mcp<2` pin watch | (d) blocked upstream | Quick check whether fastmcp-slim lifted `mcp<2.0`; note result only. |

(b) bugs with a reproducible path: **none open.**

Order: #317 → #325 → #302 template → #318 → #315 → version/changelog → #172 comment → #142 check.
Results are appended below as they land.

## Results

### Final — Sat 2026-08-29 evening CDT

| Issue / PR | Action | Evidence |
|---|---|---|
| PR #317 (AmirF194, non-root sandbox) | **Reviewed, not merged.** Suite 1401 / ruff / mypy green on head `b28ae4b` and on a merge with main; both earlier review findings verified addressed. New: CodeQL alerts 145/146 (`chmod 0644`). Codex r1: P1 "exec HOME tmpfs" checked against code and rejected as a regression (no `--read-only`, rootfs was writable+exec as root before); P2 "0644 code file on multi-user hosts" is real. Comment posted with options (a) 0644 + dismiss, (b) stdin delivery, (c) 0640 + host gid (breaks rootless podman), and a `--read-only` follow-up. **Vishal decides.** Do not dismiss the CodeQL alerts until then. | https://github.com/vishalsachdev/canvas-mcp/pull/317#issuecomment-5465907681 |
| #325 (jonespm, a11y gate) | **Shipped.** PR #329 merged (`8311b37`): `ACCESSIBILITY_CHECKERS` (default `ufixit`, alias `udoit`, `none` hides the 3 UFIXIT tools; built-in scanner always on); exposed in env.template, `.mcpb` user_config, server.json. 9 tests. Codex r1 2×P2 fixed, r2 1×P2 fixed, r3 clean. Reply posted with the premise correction. Issue open for reporter confirmation. | https://github.com/vishalsachdev/canvas-mcp/pull/329 · https://github.com/vishalsachdev/canvas-mcp/issues/325#issuecomment-5465958964 |
| #318 (zqian, delete confirmation) | **Shipped.** PR #330 merged (`fd8102c`): token on all 7 delete tools (finding: `delete_announcement_with_confirmation` never had a token either), `delete_announcement` removed, `delete_assignment_with_confirmation` added, `dry_run` removed, `by_criteria` now idempotent. 43 new tests; suite 1451. Codex r1 P1+P2 fixed, r2 2×P2 fixed, r3 clean; claude[bot] 3 items addressed. Follow-up posted. Issue open for zqian. | https://github.com/vishalsachdev/canvas-mcp/pull/330 · https://github.com/vishalsachdev/canvas-mcp/issues/318#issuecomment-5466045369 |
| #315 (drop 3.10) | **Shipped + CLOSED.** PR #331 merged (`2cc394d`): floor 3.11, CI matrix 3.11–3.13, publish interpreter, `.mcpb` compat, README + 3 role guides, uv.lock re-resolved, UP017/UP041. Codex r1 1×P2 (guides) fixed, r2 clean. | https://github.com/vishalsachdev/canvas-mcp/pull/331 |
| #302 (rpsimon-ai) | Skill Request issue template shipped: PR #328 merged (`ec71e51`), `skill` label created. Issue open (awaiting reporter). | https://github.com/vishalsachdev/canvas-mcp/pull/328 |
| #172 (quizzes) | Status comment posted: PR #191 closed unmerged, why, and what unblocks it (sandbox or a sanitized New-vs-Classic payload). | https://github.com/vishalsachdev/canvas-mcp/issues/172#issuecomment-5465956570 |
| #142 (mcp<2 pin) | Re-checked: `fastmcp-slim` 3.4.7 still `mcp<2.0,>=1.24.0`. Blocked. No comment. | PyPI JSON 2026-08-29 |
| #309, #301, #299, #236, #157 | No action; awaiting reporters / blocked as before. #157 item 2 pending via #317. | — |
| Version bump | PR #332 **merged**: v1.12.0 across all checklist stamps, CHANGELOG dated 2026-08-30, README release notes. **Not tagged, not published.** | https://github.com/vishalsachdev/canvas-mcp/pull/332 |
| 08-28 triage gap | Not a scheduler fault: run `cse_01876GnXnvE6QQKZ8xxvRayA` fired 01:38Z and took the quiet-day exit (58 s). The 08-29 executor's "routine did not fire" guess was wrong. | RemoteTrigger list_runs / get_run_log |

**Suite on `main` after all merges:** 1451 passed, 21 skipped; ruff + mypy clean.

**For Vishal (in order):**
1. Decide #317's 0644 question (comment lists the options), then update-branch + merge with the `Fixes #157` trailer stripped from the squash message.
2. Tag `v1.12.0` when ready (PyPI, MCP Registry, `.mcpb`, site deploy via wrangler). CHANGELOG date is 2026-08-30; adjust if the tag lands later. **Do not wrangler-deploy `docs/` before tagging** — the site banner already says v1.12.0.
3. `deploy/azure/README.md` still says "Python 3.12-slim"; the Dockerfile is 3.14. Doc nit, not touched.

One process note: the executor in the 08-29 brief stated the routine had not fired on 08-28 without checking; the run log shows it did. Worth a line in the routine prompt: check `list_runs` before asserting a missed fire.
