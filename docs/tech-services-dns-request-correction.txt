To: consult@illinois.edu (reply on the same disruptionlab.illinois.edu / Canvas MCP thread)
Subject: RE: DNS records for disruptionlab.illinois.edu — Canvas MCP (one correction to the CNAME target)

Hi,

Quick correction to my Canvas MCP request from yesterday, before you create the
records — one value changed.

I restructured the Azure side to match how mindforum.disruptionlab.illinois.edu
is set up (a single production app with a staging slot, rather than a standalone
staging app). So the CNAME should point at the production app's hostname:

  • CNAME  canvas-mcp.disruptionlab.illinois.edu
           ->  gies-canvas-mcp.azurewebsites.net        (was: gies-canvas-mcp-staging…)

  • TXT    asuid.canvas-mcp.disruptionlab.illinois.edu
           F8E842062F6451724C1F92914D09E0721596D0D79DA568EE28429F3ADED767A3   (UNCHANGED)

Only the CNAME target hostname changed (dropped the "-staging"). The TXT
verification value is the same — it's subscription-scoped, not per-app — so no
change there.

Everything else from yesterday stands: single hostname, no wildcard, no apex
record; I'll bind the hostname + an Azure managed cert on the App Service side
once both records land.

Thanks, and sorry for the extra round-trip,

Vishal Sachdev
Gies College of Business
vishal@illinois.edu
