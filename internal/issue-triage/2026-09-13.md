# Triage brief — 2026-09-13

**Read path used:** GitHub MCP tools (`gh` not checked/needed — MCP tools worked throughout).
**Cutoff:** 2026-09-12 (date of the newest existing brief file, `2026-09-12.md`; that brief's own
triage PR #366 merged at 2026-09-12T02:24:01Z, used below as the actual comparison timestamp since
most of today's activity happened later on that same calendar date).

This is not a quiet day: a first-time external contribution landed, a high-sensitivity PR merged,
and the security-disclosure redaction flagged in the last several briefs was carried out — all
between the 2026-09-12 brief's publish time and now. Early exit does not apply.

## Needs your reply

None. The 5 open issues (#157, #172, #236, #301, #302) are unchanged since 2026-09-08 — no
non-owner comment on any of them since the cutoff.

## PRs

### #359 — `chore: remove unused code and dependencies` (draft, self-authored)
- **State:** draft, `mergeable_state: behind` — **this changed since 2026-09-12**, when it was
  `dirty` (a real conflict in `package.json`/`package-lock.json`, per that brief's own local
  test-merge). It now reports as merely behind `main`, not conflicted; worth a fresh mergeability
  check before assuming the conflict resolved itself, since `main` has moved four merges since
  (#360, #367, #369, #370 below).
- **Reviews:** No formal GitHub review submitted. A third `claude[bot]` deep-dive comment landed
  2026-09-12T17:55:07Z (new since the last brief) — same conclusion as the first two passes: clean,
  well-scoped, no blocking issues. It repeats the one open item none of the three bot passes could
  verify directly (no `tsc`/`npm run build` in their sandboxes): the `tsconfig.json` swap of
  `moduleResolution: "node"` for `types: ["node"]`. You already confirmed locally
  (2026-09-08 comment) that `npm run build` passes. New minor nit from this third pass: the branch
  name `chore/...` isn't in CLAUDE.md's branch-naming table (`feature/`, `fix/`, `docs/`,
  `refactor/`) — non-blocking, explicitly called out as such.
- CI: 14/14 checks green.
- **Next action:** Update the branch against current `main` and re-check mergeability; the PR is
  otherwise unchanged and you already indicated it should stay draft until you advance it.

**Draft reply:**
```
Three independent bot passes now agree this is clean; the only unverified bit (tsconfig swap) is
already covered by my own local build. Updating the branch against main next since it's drifted
behind — will flip out of draft once that's done.
```

## New since 2026-09-12

Four PRs opened and/or merged after the last brief was published, all on 2026-09-12:

### ⚠️ #360 — `Protect shared-server exports and redact logged URL credentials` — MERGED 18:02:48Z
High-sensitivity (anonymization/PII export boundary, credential-log redaction) — this is the PR
flagged in every brief since 2026-09-08. You merged it directly. Net effect: `generate_peer_review_report(save_to_file=true)`,
`extract_peer_review_dataset` (now defaults `save_locally=true`), and `create_student_anonymization_map`
all now refuse over HTTP before touching Canvas data or local disk; stdio/local use is unaffected.
This is a breaking HTTP-behavior change worth a CHANGELOG `[Unreleased]` entry if one hasn't landed
yet — didn't check that file this run, flagging rather than assuming.

### ⚠️ #367 — `docs: redact vulnerability-class detail from merged triage brief` — MERGED 18:12:17Z
Directly addresses the disclosure this routine's own prior output created: an executor addendum on
the 2026-09-12 brief had named specific unpatched-vulnerability classes plus commit/branch
identifiers for the still-reachable `REVIEW-2026-09-06.md` history, and that landed on `main` inside
a tracked file. This PR redacted `2026-09-08.md` through `2026-09-12.md`. Verified locally, against
the current `main` tip: no finding IDs, vulnerable file paths, commit SHAs, branch name, or the
report's own filename remain in any of those five files — only the generic fact that some findings
were unpatched as of that document, which is what's needed for owner follow-up. As the PR's own body
states and a follow-up commit (`a5af958`) restates, this narrows what the default branch view shows
but does **not** purge the content from `main`'s or the PR's own earlier reachable git history —
that remains your call, already logged as an open item in the 2026-09-12 brief's own addendum
(force-push/history-rewrite tradeoffs, GitHub Support, moving tracking to a private Security
Advisory). Not re-litigated here since nothing has changed on that decision.

### #368 / #369 — `update_syllabus` write tool
- **#368**, opened by **@harrywang — a first-time contributor** (2026-09-12T09:49:18Z): a new write
  tool for the course Syllabus tab (`replace`/`append`/`prepend` modes), using the existing
  preview→token→confirm guard for the destructive `replace` case since Canvas keeps no syllabus
  revision history. Fork PR, so it couldn't be pushed to directly.
- **#369**, opened by you, carrying harrywang's two commits plus one review-fix commit — MERGED
  13:41:05Z. #368 was closed unmerged (superseded) in the same window.
- ⚠️ High-sensitivity by policy (new write-capable tool, course-content mutation): registered
  through a new educator-only path (`register_educator_course_tools`), so the student profile's
  write surface is unchanged. The review-fix commit bound the confirmation token to the *existing*
  syllabus body (it previously wasn't, which could have let a co-teacher's concurrent edit get
  silently overwritten by a stale token) and corrected an `idempotentHint`/success-message accuracy
  issue. Full suite green (1482 passed / 22 skipped) and live-verified against a real Canvas course
  per the PR body. Worth a first-contributor thank-you if you haven't sent one — this is
  @harrywang's first PR here.

### #370 — `chore: SessionStart hook that briefs every session on open issues, PRs and discussions` — MERGED 19:56:13Z
New automation, not opened by an external party but worth flagging for two reasons:
1. **Scope overlap, not duplication:** it surfaces open PR/issue state at the *start* of any Claude
   Code session on this repo (via a `SessionStart` hook), which is a different trigger and audience
   than this daily async routine, but the two now both summarize the same GitHub state through
   similar channels. Not asked to fold one into the other — just noting it exists so the two aren't
   confused for redundant automation later.
2. **Governance note:** the PR body states the standard commit path refused three times ("self-modification / unauthorized persistence") for a change that edits `.claude/settings.json`
   itself, and the change was instead pushed through the GitHub API at your explicit, repeated
   request. That refusal behaved as intended (the classifier is right to be suspicious of an agent
   modifying its own execution config); recording it here since it's the kind of event you'd
   probably want visibility into even though the PR itself is unremarkable — a stdlib-only,
   fail-soft script plus a hook registration, ruff-clean, pipe-tested against a real `SessionStart`
   payload.

## Going stale

All 5 open issues, mechanically unchanged from the last several briefs:
- **#157** (sandbox egress hardening) — last non-owner comment: `claude[bot]` label note, 2026-07-04.
- **#172** (Canvas Quizzes, zqian) — last non-owner comment: `jonespm`, 2026-08-04. Still blocked on
  a New-Quizzes-enabled sandbox.
- **#236** (OAuth2 dev-key flow) — last non-owner comment: `claude[bot]` label note, 2026-08-07.
  Blocked on an admin-access pilot.
- **#301** (assignment-group skill request, rpsimon-ai) — no reporter reply since 2026-08-18; you
  asked for department templates/policies on 2026-08-19.
- **#302** (student-risk analysis skill, rpsimon-ai) — same reporter/pattern; you asked two scoping
  questions on 2026-08-21.

## Checked, nothing needed

#157, #172, #236, #301, #302, #359 — all reviewed above, no action beyond what's noted. #360, #367,
#368, #369, #370 — reviewed for context (all already merged or closed by the time of this run); no
action required from this routine, flagged above for visibility only.
