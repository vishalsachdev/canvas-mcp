# Triage brief — 2026-09-01

**Read path used:** GitHub MCP tools (`mcp__github__*`). `gh` CLI not installed in this sandbox
(expected); MCP access worked on the first call.

**Cutoff:** 2026-08-31T01:50:14Z — the merge timestamp of PR #341 (the 2026-08-31 triage brief),
using the same "previous triage PR's merge time" convention as recent briefs, rather than the raw
date of the `2026-08-31.md` file (which was written to disk *before* that merge, at generation
time).

## Needs your reply

None. No open issue has a comment since the cutoff from anyone other than `vishalsachdev` or the
`claude[bot]` auto-label bot (out of scope — `auto-label-issues.yml`).

## PRs

**#340 — `fix(security): make sandbox uid:gid configurable via TS_SANDBOX_UID_GID (#338)`**
k0shir0 (fork PR, first-time contributor), not a draft. Substantial movement since the last brief:

- **AmirF194 (returning contributor, author of #317) independently reproduced the exact `0:0`
  root-uid gap** the previous brief had already flagged as unverified analysis — this time with a
  live Docker repro posted as a PR review comment at 06:32:46Z (`config.py:449`): the
  `^\d+:\d+$` regex validates shape only, so `TS_SANDBOX_UID_GID=0:0` sails through
  `validate_config()` and reaches `--user` in `code_execution.py:580`, silently running the
  sandbox as root — undoing what #317 was for. Two independent measurements now agree.
- **k0shir0 already fixed it**: commit `f8cba18` (2026-08-31T23:17:48Z) adds an explicit
  uid==0/gid==0 rejection after the format check, falling back to the safe default, plus a new
  test (`test_root_uid_gid_rejected_with_default`). You'd also merged `main` into the branch
  yourself at 19:18:16Z (`df8c061`), so you're already engaged with this PR outside this routine.
- **Still blocked:** GitHub keeps withholding the Actions run on every push to this fork branch —
  `action_required` at 06:32, 19:18, and again at 23:18 (right after the fix commit). None of
  the required checks (`lint`, `test-enhancements`) have executed yet. This is the same approval
  ask the 2026-08-31 brief made and it's still outstanding.
- AmirF194's review thread is unresolved (comment posted, no reply/resolve yet).

**Next action:** Approve the withheld workflow run (Actions tab → the run → "Approve and run
workflows") so CI actually executes against the fix commit. Once green, this looks ready to merge
— the finding that was blocking it is fixed. Consider a short reply to AmirF194 on the thread
before merging:

```
Nice catch, and good repro. k0shir0 already pushed a fix (f8cba18) rejecting uid/gid 0 explicitly
with a fallback to 65532:65532, plus a test for it. Approving the run now so CI can confirm it —
will resolve this thread once it's green.
```

After merge, #338 needs a manual close — no closing keyword anywhere in the chain by design.

---

**#342, #343, #344, #345 — Dependabot version bumps** (opened 04:38–04:44 UTC, after cutoff)

| PR | Bump | Required checks | Note |
|---|---|---|---|
| #342 | Docker `python` base image digest bump (3.14-slim → 3.14-slim, new digest) | lint ✅ test-enhancements ✅ | routine |
| #343 | `@types/node` 26.2.0 → 26.4.0 | lint ✅ test-enhancements ✅ | routine |
| #344 | `mcp` requirement `<2,>=1.26.0` → `>=1.26.0,<3` | lint ✅ test-enhancements ✅ | **see below** |
| #345 | GitHub Actions group bump (6 actions) | lint ✅ test-enhancements ✅ | routine |

All four: every required check (`lint`, `test-enhancements`, the security-scan jobs) is green.
The only failing check on all four is `claude-review` (non-required per #188) — unexplained why it
fails on same-repo dependabot branches rather than the fork-secrets reason #188 documented, but it
doesn't block merge either way. `mergeable_state` reports `blocked` on all four, which reads as
"awaiting the review branch protection requires" (none has a human review yet), not a conflict —
green-but-unreviewed.

**#344 is worth a closer look before merging on autopilot**: it widens the *direct* `mcp` pyproject
constraint past the `<2` boundary that issue #142 exists to track. In practice this alone probably
doesn't pull in `mcp` 2.x — `fastmcp` (`>=3.4.7,<4`) still transitively pins `mcp<2.0` per the
2026-08-30 weekly maintenance report — but merging it changes what the *direct* pin says, which is
exactly the kind of drift #142 is watching for. Recommend: merge it (it's a correctness fix to the
requirement string, not a functional change while fastmcp's pin holds), but leave a short note on
#142 that the direct floor moved.

**Next action for #342/#343/#345:** routine, mergeable once you review; no special handling.

## New since cutoff

- #342, #343, #344, #345 — the four Dependabot PRs above. No new issues.

## Going stale

Same standing list as recent briefs, no change: #172 (last non-Vishal touch: jonespm, 2026-08-04,
28d — still blocked on a New-Quizzes sandbox), #309/#301/#302/#299 (reporter's last touch 11–15d
ago, no answer yet to your scoping questions), #318/#325 (fixes already shipped, last reporter
touch was the original issue — just waiting on their confirmation).

## Checked, nothing needed

#142 (your own tracking issue, actively maintained — see #344 note above), #335 (automated weekly
maintenance report, out of scope per instructions), #157/#236 (your own issues, no external
activity), #338 (your own issue; only touched by the auto-label bot — its PR is #340, covered
above).
