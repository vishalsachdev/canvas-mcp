# Triage brief — 2026-08-23

**Read path used:** GitHub MCP tools (`mcp__github__*`).
**Cutoff:** 2026-08-22 (`internal/issue-triage/2026-08-22.md` is the newest prior brief). Using
that file's last commit timestamp, `2026-08-22T01:50:03Z` ("docs: executor results for 2026-08-22
brief"), as the effective boundary.

Categories 1 (issues awaiting reply) and 3 (new issues/PRs) are both empty — every open issue and
PR has an `updated_at` at or before the cutoff, confirmed against the full open-issues/open-PRs
listing. But category 2 is not empty: several PRs remain in a flagged state (green-but-unreviewed,
or blocked pending approval), so per Step 4 this does not qualify for early exit. Proceeding with a
status-only brief — no new items, PR states carried forward with today's fresh check.

## Needs your reply

None. No open issue has a comment after the cutoff from anyone other than `vishalsachdev`.

## PRs

- **#308 — `chore: refresh impact stats`** (you, ready, not draft) — head `7b0a28c`, based on
  current `main` (`0955a1ab`). All **17 checks green** (last run completed 2026-08-22T01:46Z). No
  reviews posted. `mergeable_state: unknown` — likely a stale-cache artifact from yesterday's
  branch update rather than a real conflict (base sha matches current `main` exactly). Green,
  self-authored, ready for your merge decision.

- **#295, #296, #297, #298 (dependabot)** — all four now on current `main` (per yesterday's branch
  updates) and all **17 checks green** on each (runs completed 2026-08-22T01:45–01:46Z).
  `mergeable_state: unknown` on all four, same likely cache-lag pattern as #308. Docker base-image
  bump, two npm dev-dependency bumps, one Actions group bump — green-but-unreviewed, ready for
  merge decisions.

- **#317 — `fix(security): run execute_typescript sandbox as non-root user`** (AmirF194, ready, not
  draft) — head `277ef8e`, unchanged since 2026-08-21. Still **zero CI checks reported** — fork PR
  from a first-time contributor, so this remains "not yet approved to run," not "failing."
  `mergeable_state` now reads `behind` (was `blocked` yesterday); base sha shown is current `main`,
  so this may just be GitHub's cache catching up rather than a real out-of-date branch. Next action
  is still yours: approve the pending Actions run from the Actions tab. Independently verified
  yesterday (not re-verified today): the PR's new tests fail on unpatched `main` and pass on this
  branch, full suite + ruff clean.

- **#191 — `feat: Add dual-engine read tools for Canvas Quizzes (Classic and New)`** (Copilot,
  draft, targets #172) — unchanged since 2026-08-14, same head (`9e2d146`). Zero CI checks.
  `mergeable_state` now reads `unknown` (was `dirty` yesterday) — worth a fresh look before
  assuming the conflict resolved itself; not re-verified by me today. PR body still ends with a
  live closing-keyword line (`Fixes` directly followed by the reference to issue 172) that would
  auto-close the issue if merged as-is — this is on GitHub's copy of the PR body, not something
  this brief can edit. Same three blockers as every recent brief: (1) a possible non-mechanical
  conflict in `assignments.py` (state now ambiguous, see above), (2) the closing-keyword line needs
  rewording before any merge, (3) still needs a New-Quizzes-enabled sandbox to verify the
  underlying `is_quiz_assignment AND external_tool` detection, which measured live as a *Classic*-
  quiz signal. This is the item Current Focus flags as needing a decision: source a sandbox from
  zqian, or close the PR honestly as unverifiable.

## New since 2026-08-22

Nothing.

## Going stale

- **#172** (zqian, New Quizzes) — last substantive activity 2026-08-07 (16 days). Tracked above
  under PR #191, which is the actual blocker. No new action here beyond what's already logged.
- **#142** (watch item, `mcp<2` pin) — last activity 2026-07-30 (24 days). Genuinely blocked on an
  upstream fastmcp release lifting its own `mcp<2` pin; no action available until that happens.

## Checked, nothing needed

- **#318** — last activity is `vishalsachdev`'s own correction commits from 2026-08-22, not a
  request needing a reply; zqian hasn't responded yet.
- **#309** — last comment is your own reply asking zqian two scoping questions; waiting on zqian.
- **#302** — last comment is your own reply; waiting on rpsimon-ai's answer.
- **#315** — last comment is `claude[bot]`'s automated labeling explanation; not a request.
- **#283** — last comment (2026-08-21T16:02:44Z, khagyard's confirmation) predates the cutoff and
  was already the subject of yesterday's draft reply; no new activity to act on today.
- **#301, #299, #236, #157** — no activity since the last several briefs; already covered in
  Current Focus or genuinely blocked on external input (admin access, sandbox access).

---

*Note for Vishal: no "executor" pass was run against this brief — today's session is a straight
triage-only run producing this file and its PR. If you're used to seeing a same-day
"Executor results" addendum with branch updates, local test runs, etc., that's a separate process
and didn't run today.*

> **Superseded.** The note above was accurate when written, but the executor pass ran afterwards
> against this same brief. See the section below.

---

## Executor results (2026-08-23T02:21:49Z)

### Corrections to the brief

The brief's central PR claim is wrong, and in the direction that matters: it read
`mergeable_state: unknown` on #308/#295/#296/#297 as "a stale-cache artifact ... base sha matches
current `main` exactly." Measured fresh, **all four read `behind`, not `unknown`, and none of them
matched `main`.** Every one recorded base sha `0955a1ab`, while `main` was `7b7861c` — they were
genuinely out of date by yesterday's brief merge (#319) plus its executor commits. The brief
inferred "cache lag" from a value it did not re-read; the branches really did need updating, and
I updated them.

- **#191 is `dirty`, not `unknown` — the conflict did not resolve itself.** The brief flagged this
  as "worth a fresh look before assuming the conflict resolved itself," which was the right
  instinct; the answer is that it has not. I reproduced the merge locally against current `main`:
  exactly one conflicted file, `src/canvas_mcp/tools/assignments.py`, one hunk spanning **263 lines
  (348–610)**. It is **not mechanical**: `main` holds a 257-line inline `get_assignment_analytics`
  body, the PR replaces it with a 3-line delegation to `get_assignment_analytics_impl`. Resolving
  it means deciding whether `main`'s 257 lines — which have changed since the PR was cut — are
  faithfully reproduced in the PR's `assignments_analytics.py`. This **confirms** the "262-line
  non-mechanical conflict" already recorded in Current Focus; treat that item as live, not stale.
- **#191's closing-keyword line is live, confirmed.** The PR body on GitHub still ends with a
  closing keyword directly followed by the reference to issue 172. Merging as-is would auto-close
  it a third time. Neither the brief nor I can edit GitHub's copy of a PR body — this one is yours.
- **#317 is `behind`, not conflicted.** The brief hedged that `behind` "may just be GitHub's cache
  catching up rather than a real out-of-date branch." It was real (same `0955a1ab` base), but
  `behind` never meant conflicted: I merged it into current `main` locally with **zero conflicts**.

### Branch updates (5 PRs, all same-repo)

Updated via the GitHub API; all five went from `behind` to **0 commits behind `main`**, which
re-triggered CI on each new head:

| PR | branch | old head | new head |
|----|--------|----------|----------|
| #308 | `docs/impact-stats-2026-08-17` | `7b0a28c` | `5bba05f` |
| #295 | `dependabot/docker/python-ce40764` | `b6001a1` | `6dcc386` |
| #296 | `dependabot/npm_and_yarn/tsx-4.23.12` | `5644a72` | `bd28630` |
| #297 | `dependabot/npm_and_yarn/types/node-26.2.0` | `328fdc0` | `f472d82` |
| #298 | `dependabot/github_actions/actions-3e7fba502d` | `e01ff7a` | `98c89a1` |

The brief's "17 checks green" for these PRs described the **old** heads and is now historical —
fresh runs are in flight on the new heads. Re-check before merging; do not merge on the brief's
green.

**#317 was not touched** — fork branch, per the standing rule. Its zero-CI state is still a pending
maintainer approval of the Actions run, not a failure. **#191 was not touched** — no empty-commit
CI retrigger, because it is `dirty`: CI on a conflicted draft would tell you nothing.

### Local test runs (real output)

Environment: `uv run python -m pytest tests/ -q` and `uv run ruff check .`, Python 3.12.

- **Baseline** (`triage/2026-08-23`, i.e. `main` + this brief): `1397 passed, 22 skipped,
  2 warnings in 25.47s`. Ruff: `All checks passed!`
- **#308, #295, #296, #297, #298** — each checked out at its **post-update** head and run in full.
  All five returned **`1397 passed, 22 skipped, 2 warnings`**, ruff `All checks passed!` —
  byte-identical to baseline. No regressions.
- **#317** (fetched read-only via `refs/pull/317/head`, merged into current `main` locally, never
  pushed): merge clean, 2 files changed. **`1400 passed, 21 skipped, 2 warnings in 22.94s`**, ruff
  `All checks passed!` — i.e. +3 tests, and one previously-skipped test now runs.
  I then re-ran the contributor's differential independently: with `main`'s unpatched
  `code_execution.py` and only the new test file added, **2 of the 3 new tests fail**
  (`test_user_flag_present_and_non_root`,
  `test_home_is_writable_and_exec_allowed_not_the_noexec_scratch_tmpfs`). The claimed
  fails-on-main / passes-on-branch differential **holds, independently confirmed today** — not
  carried forward from yesterday's note.

**Honest caveat on the dependabot greens:** a green Python suite is weak evidence for #295
(Dockerfile base-image digest), #296/#297 (npm dev-dependencies) and #298 (GitHub Actions
version bumps). None of those four change a line of Python the suite exercises; the suite passing
mainly confirms they broke nothing, not that the bumped thing works. #298 in particular bumps
five Actions across 6 workflow files — only a real workflow run exercises that, which is what the
re-triggered CI is now doing.

**Not tested:** #191 (cannot be checked out mergeably — conflicted, see above).

### Claims neither the brief nor I can verify

Nothing in today's brief asserts unmeasured live-Canvas behavior — it is status-only, with no draft
replies. Flagging the standing one anyway, since it gates #191: the `is_quiz_assignment AND
external_tool` New-Quizzes detection measured live as a *Classic*-quiz signal, and **neither the
triage agent nor I can measure Canvas**. That verification needs a New-Quizzes-enabled sandbox and
cannot be closed from this repo.

### Needs Vishal

Everything here is public or irreversible action, which this pass is not permitted to take.

1. **Merge decisions on 5 green PRs** — #308, #295, #296, #297, #298. All now current with `main`,
   all locally clean. Confirm the re-triggered CI is green on the **new** heads first.
2. **#317 — approve the pending Actions run** from the Actions tab. This is the only thing blocking
   a first-time outside contributor's security fix, now sitting 2 days. Independently verified
   today: clean merge, full suite green, differential holds.
3. **#191 / #172 — the decision, now 3+ weeks old.** Current Focus already calls this "decide this
   week." The conflict is confirmed real and non-mechanical (263 lines), the closing-keyword line
   is confirmed live, and the correctness blocker still needs a sandbox. Either source a
   New-Quizzes sandbox from zqian, or close the PR honestly as unverifiable and reopen when one
   exists.
4. **#191's PR body** — reword the closing-keyword line before any merge. Issue 172 has already
   been auto-closed twice this way.
5. No replies to send. Category 1 was empty, independently confirmed: zero open issues have any
   update after the cutoff (`list_issues since=2026-08-22T01:50:03Z` → 0 results).
