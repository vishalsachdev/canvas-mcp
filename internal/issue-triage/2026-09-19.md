# Triage brief — 2026-09-19

**Read path used:** GitHub MCP tools throughout (`gh` not checked — not needed for any read in
this run).

**Cutoff:** 2026-09-15 (date of the newest existing brief file, `2026-09-15.md`). That brief's own
internal comparison instant was PR #377's merge at 2026-09-14T02:15:21Z; nothing in this run turns
on the few hours' difference from 2026-09-15T00:00Z, since no comment activity falls in that
window.

## Needs your reply

None. No open issue has a comment newer than the cutoff whose most recent commenter is someone
other than vishalsachdev. The #375/#374 threads that the 2026-09-15 brief surfaced are unchanged
since then — last comments on both are 2026-09-14, before this cutoff — so they're old news, not
new activity.

## PRs

- **#380** — `chore(deps): bump anyio from 4.14.1 to 4.14.2` (dependabot, not draft). A patch
  release; the linked release notes are bugfixes only, no CVE. 16 of 17 checks are green,
  including the required `test-enhancements` and `lint`. The one red check is `claude-review`, and
  it fails on infrastructure, not on the diff: the job log shows `CLAUDE_CODE_OAUTH_TOKEN secret is
  not set`. GitHub withholds repo secrets from Dependabot-triggered workflow runs by default, so
  this check cannot run at all on a Dependabot PR here — the same failure hit dependabot PR run
  #610 back on 2026-09-14. Not a new problem, and not a required check (dropped from required
  status in #188 for a related reason — it can't reliably run in every context). `mergeable_state`
  reads `blocked`, which with every required check green most likely means it's waiting on a
  review approval under branch protection rather than any outstanding failure. **Next action:**
  review and merge; nothing needs fixing first.
- **#381** — `docs: add Codex MCP setup instructions` (Mighty303, **draft**, from a fork). Adds a
  Codex CLI / `~/.codex/config.toml` setup section to the README's supported-clients list — one
  file, +22/-1, docs-only, and accurate on read (matches the existing pattern used for the other
  client sections). 0 reported checks; the underlying workflow run shows `action_required`, i.e.
  Actions is waiting on maintainer approval to run for this fork/first-time contributor, not that
  anything failed. **Next action:** none required while it stays draft. If Mighty303 marks it
  ready, approve the workflow run so CI executes, then review the addition.

## New since 2026-09-15

- **#380** — opened 2026-09-18 by dependabot[bot]. See PRs above.
- **#381** — opened 2026-09-18 by Mighty303. See PRs above.

No new issues since the cutoff.

## Going stale

Unchanged from the 2026-09-15 brief — all five still mechanically untouched by anyone but you:
- **#157** (sandbox egress hardening) — last non-owner comment: `claude[bot]` label note,
  2026-07-04.
- **#172** (Canvas Quizzes, zqian) — last non-owner comment: `jonespm`, 2026-08-04. Still blocked
  on a New-Quizzes-enabled sandbox.
- **#236** (OAuth2 dev-key flow) — last non-owner comment: `claude[bot]` label note, 2026-08-07.
  Blocked on an admin-access pilot.
- **#301** (assignment-group skill request, rpsimon-ai) — no reporter reply since 2026-08-18; you
  asked for department templates/policies on 2026-08-19.
- **#302** (student-risk analysis skill, rpsimon-ai) — same reporter/pattern; you asked two scoping
  questions on 2026-08-21.

## Checked, nothing needed

#157, #172, #236, #301, #302 — reviewed, unchanged since the last brief (see Going stale above).
#374, #375 — reviewed; both are waiting on the reporter (chiptoe-svg) to retest against a live
instance after PR #378 merged, not on you — no new comments on either since 2026-09-14. #380, #381
— reviewed and covered under PRs/New above; neither needs a reply from you right now.
