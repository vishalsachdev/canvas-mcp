# Triage brief — 2026-08-24

**Read path used:** GitHub MCP tools (`mcp__github__*`).
**Cutoff:** 2026-08-23 (`internal/issue-triage/2026-08-23.md` is the newest prior brief). That
brief's own executor pass ran at `2026-08-23T02:21:49Z` and its PR (#320) merged to `main` at
`2026-08-23T02:30:16Z` (`87cf671`) — using that merge as the effective boundary.

Categories 1 (issues awaiting reply) and 3 (new issues/PRs) are both empty — every open issue and
PR has an `updated_at` at or before the cutoff, confirmed against the full open-issues/open-PRs
listing. Category 2 is not empty: the same PRs flagged yesterday remain in flagged states (three
now confirmed genuinely behind `main` again, one still pending fork-CI approval, one still a
non-mechanical conflict), so per Step 4 this does not qualify for early exit. Status-only brief —
no new items.

## Needs your reply

None. No open issue has a comment after the cutoff from anyone other than `vishalsachdev`.

## PRs

- **#308, #295, #296, #297, #298** — all five now show **`mergeable_state: behind`, base sha
  `7b7861c`**, while current `main` is `87cf671` (yesterday's own triage PR #320, merged
  02:30:16Z — 8 minutes after these five branches were last updated at ~02:16–02:19Z). This is a
  real one-commit lag, not a cache artifact: I checked `main`'s actual head via `list_commits`
  before concluding this, rather than inferring it the way the 2026-08-22 brief did (and got
  wrong). All five carry **17/17 checks green** on their current heads (fresh runs completed
  2026-08-23T02:17–02:19Z, confirmed via `get_check_runs`, not the stale `get_status` legacy API
  which returns empty for this repo). No reviews posted on any of them. Green-but-unreviewed,
  and now one commit behind `main` — a routine re-sync-and-merge, not a new problem.
- **#317 — `fix(security): run execute_typescript sandbox as non-root user`** (AmirF194, ready,
  not draft) — head `277ef8e`, unchanged since 2026-08-21 (3 days). Still **zero CI checks
  reported** — fork PR from a first-time contributor, so this remains "not yet approved to run,"
  not "failing." `mergeable_state: behind` (base `0955a1a` vs current `main` `87cf671`) — this one
  really is stale relative to yesterday's independent local-merge verification (clean, full suite
  green, differential holds), which this brief did not re-run today. Next action is still yours:
  approve the pending Actions run from the Actions tab.
- **#191 — `feat: Add dual-engine read tools for Canvas Quizzes (Classic and New)`** (Copilot,
  draft, targets #172) — unchanged since 2026-08-14 (10 days), same head (`9e2d146`).
  `mergeable_state: dirty` — confirmed still conflicted (not re-verified locally today; the
  2026-08-23 executor pass reproduced the conflict directly: `assignments.py`, 263 lines,
  non-mechanical). PR body still ends with a live closing-keyword line (`Fixes` directly before
  the reference to issue 172) that would auto-close the issue if merged as-is. Same standing
  blockers: (1) the non-mechanical conflict, (2) the closing-keyword line needs rewording before
  any merge, (3) still needs a New-Quizzes-enabled sandbox to verify `is_quiz_assignment AND
  external_tool`, measured live as a *Classic*-quiz signal. This is the item Current Focus flags
  as needing a decision.

## New since 2026-08-23

Nothing.

## Going stale

- **#172** (zqian, New Quizzes) — last substantive activity 2026-08-07 (17 days). Tracked above
  under PR #191, which is the actual blocker.
- **#142** (watch item, `mcp<2` pin) — last activity 2026-07-30 (25 days). Blocked on an upstream
  fastmcp release; no action available until that happens.
- **#301** (rpsimon-ai, assignment-group skill request) — you replied 2026-08-19 asking for
  department-template details and policy answers; no response from rpsimon-ai since their
  2026-08-18 filing (6 days). Not an action item for you — just newly crossing the 5-day mark.
- **#302** (rpsimon-ai, student-analytics skill request) — you replied 2026-08-21 pointing to
  existing tools (`get_student_analytics`, `canvas-morning-check`) and asked two clarifying
  questions; no response since their 2026-08-18 filing (6 days).
- **#299** (turner-moore, cookie-auth feature) — you replied 2026-08-19 with required security
  constraints and asked for a sanitized live-session test before a full PR; no response since
  their 2026-08-17 filing (7 days).

## Checked, nothing needed

- **#318** — last activity is `vishalsachdev`'s own correction commits from 2026-08-22, not a
  request needing a reply; zqian hasn't responded yet (3 days, not yet stale).
- **#309** — last comment is your own reply (2026-08-21) with a detailed v1 design and two
  scoping questions; waiting on zqian (5 days, not yet over the stale threshold).
- **#315** — last comment is `claude[bot]`'s automated labeling explanation; not a request.
- **#283** — last comment (2026-08-21T16:02:44Z, khagyard's confirmation) predates the cutoff;
  no new activity to act on today (3 days, not yet stale).
- **#236, #157** — no activity since the last several briefs; already covered in Current Focus or
  genuinely blocked on external input (admin access, sandbox access).

---

## Executor results (2026-08-24T01:49:16Z)

### Verification of the brief

The brief holds up on every claim I could re-measure. Independently confirmed, not carried forward:

- **Categories 1 and 3 really are empty.** `list_issues` with `since=2026-08-23T02:30:16Z` (the
  #320 merge boundary the brief used) returns **0 open issues**, and the open-PR listing contains
  no PR created after that boundary. Status-only day confirmed.
- **The five PRs were genuinely `behind`, not a cache artifact.** All five read base sha
  `7b7861c` against `main` at `87cf671` when I re-read them fresh, exactly as the brief said. The
  brief drew this conclusion by checking `main`'s real head first; that method was right and the
  answer was right.
- **#191 is still `dirty`, and the closing-keyword line in its PR body is still live** (the body
  still ends with the keyword directly followed by the reference to issue 172). Re-read from
  GitHub today, not inferred. Head unchanged at `9e2d146`.
- **#317 is still a fork PR with zero CI checks** and base `0955a1a` — behind, unchanged since
  2026-08-21.

No draft replies in today's brief, so nothing to flag as asserting unmeasured Canvas behavior.
The standing one still gates #191: `is_quiz_assignment AND external_tool` measured live as a
*Classic*-quiz signal, and neither the triage agent nor I can measure Canvas. That needs a
New-Quizzes-enabled sandbox.

Closing-keyword scan: `scripts/check_closing_keywords.py --strict` over the brief exits **0**,
both before my additions and after.

### Branch updates (5 PRs, all same-repo)

Updated via the GitHub API at 01:42:49Z; all five went from `behind` to current with `main`
(`87cf671`), which re-triggered CI on each new head:

| PR | branch | old head | new head |
|----|--------|----------|----------|
| #308 | `docs/impact-stats-2026-08-17` | `5bba05f` | `d063c28` |
| #295 | `dependabot/docker/python-ce40764` | `6dcc386` | `3f84664` |
| #296 | `dependabot/npm_and_yarn/tsx-4.23.12` | `bd28630` | `77ab1cc` |
| #297 | `dependabot/npm_and_yarn/types/node-26.2.0` | `f472d82` | `1ac01e7` |
| #298 | `dependabot/github_actions/actions-3e7fba502d` | `98c89a1` | `be8ad5a` |

**CI on the new heads has already completed and is green** — unlike yesterday, where the runs were
still in flight when the executor section was written. Re-read via `get_check_runs` at 01:47–01:48Z:
each of #308/#295/#296/#297/#298 shows **17 checks, zero failures** (15 success, `Commits pushed to
main` skipped by design, and the two Cursor agent checks `neutral` — their normal state on this
repo). #297 cross-checked a second way via `list_workflow_runs` filtered to its branch: all four
workflow runs on `1ac01e7` conclude `success`.

**#317 was not touched** — fork branch, per the standing rule. Its `behind` state and zero-CI state
are both still pending your action, not something this pass may fix. **#191 was not touched** — no
empty-commit retrigger, because it is `dirty`; CI on a conflicted draft would tell you nothing.

### Local test runs (real output)

Environment: `uv run python -m pytest tests/ -q` and `uv run ruff check .`, **Python 3.11.15**
(pytest 9.1.1). Note the version differs from yesterday's pass, which ran 3.12 — see the correction
below.

- **Baseline** (`triage/2026-08-24`, i.e. `main` + this brief): `1397 passed, 22 skipped,
  2 warnings in 32.99s`. Ruff: `All checks passed!`
- **#308, #295, #296, #297, #298** — each checked out at its **post-update** head and run in full.
  All five returned **`1397 passed, 22 skipped, 2 warnings`**, ruff `All checks passed!` —
  byte-identical to baseline. No regressions.
- **#317** (fetched read-only via `refs/pull/317/head`, merged into current `main` locally, never
  pushed): merge clean, 2 files changed, 118 insertions. **`1399 passed, 22 skipped, 2 warnings in
  29.37s`**, ruff `All checks passed!`
  Differential re-run independently: with `main`'s unpatched `code_execution.py` and only the new
  test file added, **both new tests fail** (`test_user_flag_present_and_non_root`,
  `test_home_is_writable_and_exec_allowed_not_the_noexec_scratch_tmpfs`) and both pass with the
  patch. The fails-on-main / passes-on-branch differential **holds, re-confirmed today**.

**Correction to yesterday's executor section.** It recorded #317 as "+3 tests, and one
previously-skipped test now runs" → `1400 passed, 21 skipped`. That does not reproduce.
`tests/security/test_sandbox_nonroot_user.py` collects **exactly 2 tests** — I ran the file alone
to confirm (`2 passed`), and it contains no `skipif`, no `importorskip`, and no docker-availability
gate that could vary by environment. Today's measurement is **+2 tests, skip count unchanged at 22**
(1399/22 vs. a 1397/22 baseline). The substantive conclusion is unaffected — the merge is clean,
the suite is green, the differential holds — but the counts in yesterday's brief are wrong and
should not be quoted.

**Honest caveat on the dependabot greens, restated:** a green Python suite is weak evidence for
#295 (Dockerfile base-image digest), #296/#297 (npm dev-dependencies), and #298 (GitHub Actions
version bumps). None of those changes a line of Python the suite exercises. For #298 specifically,
the thing that actually exercises five Action bumps across 6 workflow files is a real workflow run
— and that has now happened on `be8ad5a`, green, which is meaningfully stronger evidence than the
pytest result.

**Not tested:** #191 (cannot be checked out mergeably — conflicted). **Not tested locally beyond
the merge check:** nothing else; every other open PR was run.

### Needs Vishal

Everything below is public or irreversible action, which this pass is not permitted to take.

1. **Merge decisions on 5 green PRs** — #308, #295, #296, #297, #298. All current with `main`,
   all 17/17 green on their **new** heads as of 01:48Z, all locally clean. These are ready to
   merge now; no further re-check needed unless something lands on `main` first.
2. **#317 — approve the pending Actions run** from the Actions tab. Still the only thing blocking
   a first-time outside contributor's security fix, now sitting **3 days**. Independently
   re-verified today: clean merge into current `main`, full suite green, differential holds. Note
   the branch is also `behind` — a fork branch this pass may not update, so it will need either
   your update or a merge-with-base at merge time.
3. **#191 / #172 — the decision, now 3+ weeks old and unchanged for 10 days.** Current Focus has
   said "decide this week" for two weeks. Conflict confirmed real and non-mechanical, the
   closing-keyword line confirmed live, correctness blocker still needs a sandbox. Either source a
   New-Quizzes sandbox from zqian, or close the PR honestly as unverifiable and reopen when one
   exists.
4. **#191's PR body** — reword the closing-keyword line before any merge. Issue 172 has already
   been auto-closed twice this way.
5. **The promised Skill Request issue template** (owed to Rebecca Simon on #302 and by email) is
   still not filed — it is not in `.github/ISSUE_TEMPLATE/`. Unchanged from previous briefs;
   flagging it because it is the one item with an outside person waiting on it.
6. No replies to send. Category 1 was empty, independently confirmed.
