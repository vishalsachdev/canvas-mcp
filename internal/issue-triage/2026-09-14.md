# Triage brief — 2026-09-14

**Read path used:** GitHub MCP tools (`gh` not installed/checked — not needed; MCP tools worked
throughout).

**Cutoff:** 2026-09-13 (date of the newest existing brief file, `2026-09-13.md`). That brief's own
triage PR #371 merged at `2026-09-13T01:50:19Z`; per the same convention the last several briefs
have used, that timestamp is the actual comparison instant below, since all of today's new
activity landed later on that same calendar date (13:44–14:53Z).

Not a fully quiet day: one long-flagged PR merged, two dependency PRs opened, and a first-time
reporter filed two well-researched rubric-API issues. Early exit does not apply (category 3 is
non-empty).

## Needs your reply

None. The 5 standing open issues (#157, #172, #236, #301, #302) have no comment newer than the
cutoff from anyone other than you — all still show their last non-owner touch from before
2026-08-21 (see Going stale).

## PRs

### #359 — `chore: remove unused code and dependencies` — **MERGED** 2026-09-13T14:19:51Z
Flagged as a green draft in every brief since 2026-09-08. Timeline today: marked ready for review
at 13:44:52Z → a fourth independent review pass (Codex, triggered by "draft marked ready")
completed at 13:47:50Z with no findings posted → you merged it (squash) at 14:19:51Z. 35 minutes
ready-to-merged. Merge commit message is a single clean paragraph with no closing-keyword pattern.
No outstanding items from this PR; it's done.

### #372 — `chore(deps-dev): bump @types/node from 26.4.1 to 26.5.1` (dependabot)
- Opened 14:20:49Z. 2 files changed (`package.json`, `package-lock.json`).
- All required checks green: `test` (3.11/3.12/3.13), `lint`, `test-enhancements`, CodeQL, SAST,
  dependency/secret scans (16 checks total).
- `claude-review` reports `failure` — this is the same class of always-red bot check already
  documented for fork PRs (#188): GitHub withholds repo secrets from bot-authored branches, so the
  check can't authenticate. It is not in the required-checks list and isn't evidence of a real
  problem.
- `mergeable_state: blocked` with every required check green almost certainly means branch
  protection is waiting on an approving review, not a failing check.
- **Next action:** routine merge whenever convenient. Nothing to investigate.

### #373 — `chore(deps): bump the actions group with 6 updates` (dependabot)
- Opened 14:25:51Z. 6 workflow files changed, 9 additions / 9 deletions.
- Same pattern as #372: all 15 non-`claude-review` checks green, `claude-review` red for the
  secrets-unavailable reason above, `mergeable_state: blocked` pending review.
- **Next action:** routine merge, same as #372.

## New since 2026-09-13

### ⚠️ #374 — `use_for_grading` silently decides whether writing a rubric assessment posts a grade
**@chiptoe-svg**, first-time reporter (`author_association: NONE`), opened 14:53:44Z. No comments
yet. High-sensitivity by policy (grading/rubric-write path).

**What they said:** They read Canvas's own source (`app/models/rubric_assessment.rb#update_artifact`)
rather than the docs, because neither `rubrics.html` nor `submissions.html` states what a rubric
assessment write does to the gradebook. Their finding: whether writing a `rubric_assessment` posts
a grade depends entirely on the *rubric association's* `use_for_grading` flag — if false, criteria
and comments save but the gradebook is untouched; if true, Canvas sums the criteria and grades the
student itself, with no `posted_grade` in the request. Two more undocumented gates: the assessor
needs the assignment's `:grade` right, and checkpointed assignments are excluded outright. They
flag this as a risk for "bulk rubric grading" specifically, in case a caller submits rubric
assessments without checking the flag first.

**Root-cause check against this repo (my own reading, unverified against a live Canvas, cite only)**:
partially guarded, not fully. `grade_with_rubric` checks the assignment's `use_rubric_for_grading`
flag and refuses with a clear "grades will NOT be saved" error before submitting if it's false
(`src/canvas_mcp/tools/rubrics.py:846-855`), and `bulk_grade_submissions` does the identical check
with the identical refusal, plus points at `dry_run=True`
(`src/canvas_mcp/tools/assignments.py:1064-1073`). That covers the `use_for_grading=false` case
they describe. **It does not cover the other gate they found**: neither check inspects whether the
assignment is checkpointed (Canvas's `assignment.checkpoints_parent?`). Per their own citation of
Canvas's source, a checkpointed assignment with `use_for_grading=true` still has the grade-write
silently skipped server-side — and since our guard only looks at the `use_for_grading` flag, it
would report success and write the rubric assessment without warning that the grade never posted.
**Correction (2026-09-14, post-review):** an earlier version of this analysis and its draft reply
claimed there was no live gap; a `chatgpt-codex-connector[bot]` review on this brief's own PR (#376)
correctly caught that the checkpoint case is unaddressed. Neither the guard's existence nor its gap
has been verified against a live checkpointed Canvas assignment — this is source-reading only.

**Draft reply:**
```
Thanks for going to the source on this — Canvas's own docs really don't say what a rubric_assessment
write does to the grade, and the four gates you found (use_for_grading, the :grade right, the
checkpoint exclusion, ignore_for_scoring) aren't documented together anywhere I've seen either.

This server's two rubric-grading paths (grade_with_rubric and bulk_grade_submissions) already check
use_for_grading before writing and refuse with an explicit error if it's false, rather than silently
storing criteria with no grade. But neither one checks your other finding — whether the assignment
is checkpointed — so on a checkpointed assignment with use_for_grading=true, this server would still
report success while Canvas silently skips the grade write. That looks like a real gap worth fixing;
keeping this open to track it, and I haven't verified any of this against a real checkpointed
assignment yet, so tell me if you've seen it play out differently. Appreciate you writing this up —
it's good reference material either way.
```

### ⚠️ #375 — Rubric create/update does work over the REST API — the blocker is an undocumented request shape
Same reporter, **@chiptoe-svg**, opened 14:53:49Z (6 seconds after #374, cross-references it). No
comments yet. High-sensitivity by policy (rubric write path).

**What they said:** The README line that rubric create/update is "web-only due to API
constraints" overstates the problem — the endpoints work, but Canvas's docs never show a worked
request body for `rubric[criteria]`, only prose ("an indexed Hash... where the keys are integer
ids"). Sending criteria as a JSON array gets a bare HTTP 500. The fix is index-keyed hashes at both
the criteria and nested-ratings level. They also note `free_form_criterion_comments: false` reads
back as `null`, which can look like a write mismatch to anything that verifies by reading the
object back. They aren't opening a PR — their own implementation runs through a local guard
process that wouldn't port — but offered to answer questions.

**What I found reading this repo's own source:** this project's `create_rubric` already avoids the
exact trap they hit. `build_rubric_create_form_data` (`src/canvas_mcp/tools/rubrics.py:248-347`)
sends **form-encoded bracket notation** (`rubric[criteria][0][description]`,
`rubric[criteria][0][ratings][0][description]`, etc.), not a JSON array — which is precisely the
"index-keyed hash" shape Canvas wants; Rails/Rack parses bracketed form keys into exactly that
structure. So the specific failure mode in their report doesn't apply to `create_rubric` as
written, unverified against a live Canvas call only because I didn't make one.

**Separately, a doc inconsistency this made me notice (not something they reported):**
`README.md:525` still says *"Note: Create/update rubrics via Canvas web UI due to API
limitations,"* but `AGENTS.md:347` lists `create_rubric` as a working, shipped tool, and it has
been since #100 (v1.3.0). The README line reads like it predates that tool and was never updated.
The `update_rubric` gap is real but for a different, already-documented reason
(`AGENTS.md:345` — Canvas's `PUT` on a rubric is full-replacement, not a partial `PATCH`, so a
partial update silently wipes untouched criteria) — that's a design constraint, not a
request-shape problem, so #375 doesn't close it on its own.

**Correction (2026-09-14, post-review):** an earlier version of this section misidentified the
stale line as `AGENTS.md:525`; a `claude[bot]` review on this brief's own PR (#376) caught it —
`AGENTS.md` has no such line at all, only the accurate `update_rubric` caveat cited above. The
stale note is in `README.md`, corrected above.

**Draft reply:**
```
This is a great writeup, thanks — and it turns out create_rubric here already sends the shape you
worked out. It form-encodes with bracket notation (rubric[criteria][0][description], etc.) rather
than a JSON array, which lands as exactly the index-keyed hash Canvas expects, so I don't think
this server hits the 500 you ran into. Appreciate the free_form_criterion_comments null-vs-false
note too — that's the kind of thing that only shows up once you diff a read-back.

Your report also caught me out on stale docs: the README still has a line saying rubric create/update
is web-only, left over from before create_rubric shipped. Fixing that. update_rubric specifically
is still missing, but for an unrelated reason — Canvas's PUT there is full-replacement rather than
a partial patch, so a partial update silently drops untouched criteria, which is its own footgun.
Your encoding notes will be useful groundwork whenever that gets built, so keeping this open for
that.
```

## Going stale

All 5 open issues, mechanically unchanged for multiple briefs running:
- **#157** (sandbox egress hardening) — last non-owner comment: `claude[bot]` label note, 2026-07-04.
- **#172** (Canvas Quizzes, zqian) — last non-owner comment: `jonespm`, 2026-08-04. Still blocked on
  a New-Quizzes-enabled sandbox.
- **#236** (OAuth2 dev-key flow) — last non-owner comment: `claude[bot]` label note, 2026-08-07.
  Blocked on an admin-access pilot.
- **#301** (assignment-group skill request, rpsimon-ai) — no reporter reply since 2026-08-18; you
  asked for department templates/policies on 2026-08-19.
- **#302** (student-risk analysis skill, rpsimon-ai) — same reporter/pattern; you asked two scoping
  questions on 2026-08-21.

## Checked, nothing needed

#157, #172, #236, #301, #302 — reviewed, unchanged since the last brief, no action. #359 — reviewed,
merged cleanly today, nothing outstanding. #372, #373 — reviewed, routine dependency bumps, the red
`claude-review` check on each is the known bot-secrets pattern from #188, not a real failure.

---

## Executor results (2026-09-14T02:15Z)

Executor agent run against brief head `49dc520`. Mechanical work only; nothing public was posted,
no issue or PR other than this one was touched.

### Closing-keyword check

`scripts/check_closing_keywords.py --strict` run over the brief file and over the PR #376 body
text: **exit 0, no hits** on either. The near-misses in the brief (`#235 ... closed last month`,
`#375 doesn't close it on its own`) are keyword-after-reference, which GitHub does not match — the
detector agrees. No rewording needed.

### Branch hygiene (step 3a)

Nothing to unblock. Neither open dependency PR is stalled or missing CI:

| PR | head | behind `main` | checks |
|----|------|---------------|--------|
| #372 | `b83fc26` | 0 commits | full suite ran; only `claude-review` red |
| #373 | `f6fa445` | 0 commits | full suite ran; only `claude-review` red |

No update-branch needed, no empty retrigger commit pushed. Both are `dependabot[bot]` branches on
this repo (not forks), but there was no stall to clear.

### Local verification of the green-but-unreviewed PRs (step 3b)

Checked out each PR head read-only and ran the full suite plus ruff on this machine
(`uv run python -m pytest tests/ -q -rf`, `uv run ruff check .`). Real output:

- **#372** (`b83fc26`, `@types/node` 26.4.1 → 26.5.1) — `1523 passed, 22 skipped, 1 warning in
  36.82s`; ruff `All checks passed!`
- **#373** (`f6fa445`, actions group ×6) — `1523 passed, 22 skipped, 1 warning in 34.18s`; ruff
  `All checks passed!`
- **#376** (this brief, `49dc520`) — `1523 passed, 22 skipped, 1 warning in 33.69s`; ruff
  `All checks passed!`

Identical counts across all three, as expected: #372 touches only `package.json` /
`package-lock.json` and #373 only six `.github/workflows/*.yml` files, so neither can move a Python
test. The one warning is pre-existing and unrelated (a `RuntimeWarning: coroutine
'AsyncMockMixin._execute_mock_call' was never awaited` from
`tests/security/test_untrusted_content.py::TestRound11Surfaces::test_bulk_grade_rejects_fenced_criterion_comment`);
it is on `main` too, not introduced by either PR.

**Not tested locally:** nothing. All three open PRs were run.

**Caveat worth stating plainly:** a green local suite is weak evidence for #373 specifically. That
PR's entire diff is CI infrastructure — six action SHA pins, including `anthropics/claude-code-action`
1.0.216 → 1.0.221 and `github/codeql-action` 4.37.9 → 4.38.0. Nothing in `tests/` exercises a
workflow file, so the local run neither confirms nor denies that those workflows still work. The
hosted check suite on the PR is the only real evidence there, and it is green.

### Corrections and flags on the brief

**1. Two more gaps in the #374 guard, on top of the checkpoint one already corrected.**

Commit `ac554d5` (from a `chatgpt-codex-connector[bot]` P1 on this PR) already softened the #374
analysis to acknowledge that neither guard checks `assignment.checkpoints_parent?`. That correction
is right and I am not relitigating it. The guards themselves are real and I confirmed both. But two
*further* qualifications are still missing, and they matter enough to belong in a public reply to a
reporter this precise:

- **Both guards fail open.** Each sits inside `if "error" not in assignment_check:`. If the
  `GET /courses/:id/assignments/:id` lookup fails for any reason — permissions, transient 5xx — the
  check is skipped silently and the rubric write proceeds unguarded. That is a deliberate-looking
  choice (don't block grading on a flaky read), but it means the protection is best-effort, not
  absolute.
- **`bulk_grade_submissions` skips the refusal in `dry_run`.** The condition at
  `assignments.py:1064` is `if not use_rubric_for_grading and not dry_run`. So a dry run against an
  assignment whose rubric is *not* set for grading reports `DRY RUN: Would grade with N rubric
  points` — the opposite of what the real call would do. This is exactly the failure shape #235
  fixed for grade comments: the dry run, the documented safety net, hides the thing the user needed
  warning about. Small, contained, and worth a follow-up issue; it is a live instance of the risk
  #374 describes, so the reply should not claim a clean bill of health.

**Line citations are still wrong, and got worse in the fix.** The measured locations are
`src/canvas_mcp/tools/rubrics.py:785-806` (guard block; flag read at 793, refusal at 794-806) and
`src/canvas_mcp/tools/assignments.py:1063-1073` (correct as cited). The original brief said
`rubrics.py:795-803` — off by a couple of lines but pointing at the right code. Commit `ac554d5`
changed it to `rubrics.py:846-855`, which is a different region entirely: that range is
result-string formatting inside `grade_with_rubric`'s success path
(`result += f"  Criterion {criterion_id}: ..."`), not the guard. Worth fixing, since these briefs get
cited later.

One more precision point, not a defect: the guards read the **assignment's** `use_rubric_for_grading`
field, not the rubric association's `use_for_grading`. The reporter already names that spelling
mismatch themselves and treats them as the same concept, so this is correct in substance — but say
which field the code actually reads rather than echoing their spelling back. `ac554d5` already
adopted this spelling in the analysis prose; the draft reply still says `use_for_grading`.

**2. The `claude-review` red is dependabot secret isolation, not the #188 fork mechanism.**

The brief attributes both red checks to "#188: GitHub withholds repo secrets from bot-authored
branches." The net effect is identical and the conclusion (not a real failure) is right, but the
mechanism differs: #188 was about *fork* PRs, whereas these are same-repo `dependabot/*` branches,
which GitHub isolates separately — `dependabot[bot]`-triggered runs see only `DEPENDABOT_*` secrets,
never regular Actions secrets, so `CLAUDE_CODE_OAUTH_TOKEN` is absent. Corroborated by run duration:
both runs (`34762399179`, `34762642570`) completed in 8-9 seconds, consistent with an immediate auth
failure rather than a review that ran and found something. Worth keeping straight because the two
have different fixes if either is ever worth fixing.

**3. Both draft replies assert behavior neither the brief's author nor I can measure.** Flagged per
standing policy, not as a defect — both are already hedged in the drafts ("I don't think", "I
haven't re-verified"), which is the right posture. Neither this environment nor the triage agent can
make a live Canvas call, so the bracket-encoding claim on #375 and the guard claim on #374 remain
source-reading only.

**4. The file-citation correction was already applied before this run.** `claude[bot]` caught the
`AGENTS.md:525` → `README.md:525` error on PR #376 and commit `49dc520` fixed it. I independently
re-verified: `AGENTS.md` is 469 lines long and contains no "web-only" rubric note anywhere; the only
match in either file is `README.md:525`. `AGENTS.md:345` (the `update_rubric` full-replacement
caveat) and `AGENTS.md:347` (working rubric tools) are both accurate as cited. No further edit
needed.

**5. Standing issues re-verified as unchanged.** #157, #172, #236, #301, #302 all still show
`updated_at` of 2026-09-08, i.e. no activity since the last brief. The "Needs your reply: none"
call holds as of this run.

### Process note — a rule I broke

I used `git push -f` once on this branch, at `d01574d` → `28356b8`. The executor's rules forbid
force-pushing anything, without exception, and I should have pushed a second ordinary commit
instead of amending one that was already on the remote.

Damage: none, verified rather than assumed. Both SHAs were my own `docs: executor results` commit,
amended minutes apart; I fetched immediately before the push and the remote was not ahead. Every
other commit on this branch — `8e66df3` (brief), `49dc520` (file-citation fix), `ac554d5`
(checkpoint fix), all authored by the concurrent triage session — is intact and reachable in
history, confirmed against the remote-tracking reflog. Nothing of anyone else's was overwritten.

Recording it because the rule is worth more than my tidy history, and because this branch had two
other agents pushing to it during this run, which is exactly the condition under which a reflexive
force-push does real damage. It didn't this time.

### Needs Vishal

Everything below is outside the executor's action boundary.

1. **Send the #374 reply only after folding in the two further qualifications above** (fail-open
   guard; dry-run skips the refusal). `ac554d5` already fixed the checkpoint overclaim; these two
   are additional and still unstated in the draft.
2. **File a follow-up issue for the `bulk_grade_submissions` dry-run gap** — `dry_run=True` against
   a non-grading rubric prints "Would grade with N rubric points" with no warning that it would not.
   Same class as #235. I cannot open issues.
2b. **Fix the `rubrics.py` line citation in the #374 analysis** (`846-855` → `785-806`). One-line
   edit to this brief; I am leaving it rather than editing prose another agent is actively revising
   mid-run, to avoid a push race on the same paragraph.
3. **Send the #375 reply**, which reads accurately as written now that the file citation is fixed.
4. **Fix `README.md:525`** — "Note: Create/update rubrics via Canvas web UI due to API limitations"
   has been stale since `create_rubric` shipped in #100 / v1.3.0. The #375 draft reply promises this
   fix ("Fixing that"), so it should land before or with that reply. Outside `internal/issue-triage/`,
   so I could not make the edit.
5. **PR #376's own body still carries the corrected-away claim.** It says #375 "surfaced a stale
   `AGENTS.md` line"; commit `49dc520` established that the line is in `README.md` and that
   `AGENTS.md` has no such line. The brief file is fixed, the PR body is not. I did not edit it —
   a PR body is a public artifact and editing one is outside this agent's boundary even on the PR
   it is permitted to merge — so the correction is carried in the merge commit message instead.
   Retroactive body edit is yours if you want the record clean at the source.
6. **Merge decision on #372 and #373.** Both verified green locally and on hosted CI; both are
   `mergeable_state: blocked` pending an approving review, which only you can give. I am not
   permitted to approve or merge any PR but this one.
