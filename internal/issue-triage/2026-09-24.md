# Triage brief — 2026-09-24

**Read path used:** GitHub MCP tools throughout (`gh` not installed in this sandbox).

**Cutoff:** 2026-09-23T02:26:30Z — the merge time of the previous brief's triage PR, **#392**
(same convention the last several briefs have used). Everything below newer than that timestamp
counts as "since cutoff."

Not a quiet day: one new PR (a Dependabot bump) arrived after the cutoff, and one of your own
open issues has a follow-up worth flagging even though the mechanical rules don't classify it as
"new."

## Needs your reply

None mechanically — no open issue has a comment newer than cutoff from anyone other than you.
See **Flagged for you** below for one substantive reply you likely want to send anyway.

## PRs

### #395 — chore(deps): bump the actions group across 1 directory with 6 updates (Dependabot) — NEW since cutoff

Opened 2026-09-23T10:02:52Z, after the cutoff. Bumps `anthropics/claude-code-action` (1.0.222 →
1.0.231), `docker/build-push-action` (7.3.0 → 7.4.0), and the four `github/codeql-action/*`
actions (4.38.0 → 4.38.1) — all patch/point releases, no breaking-change notes in the bundled
changelogs.

19 of 20 check runs are green (test matrix 3.11–3.13, lint, CodeQL, security scans, PR-body
check, test-enhancements). The one failure is `claude-review`, for the same reason as every prior
Dependabot PR in this repo: Dependabot PRs don't get the Claude GitHub App's OAuth secret, so the
review job can't run and reports failure rather than skipped. `mergeable_state` is `blocked`,
consistent with that one non-required-but-red check rather than an actual merge conflict.

Next action: routine dependency bump, low risk. Merge at your discretion.

### #390 — fix: separate external-tool assignments from missing submissions (@EastArctica, fork) — unchanged since last brief

Still zero check runs reported (`total_count: 0`) — still the known first-time-contributor
pattern (GitHub withholds Actions runs until you approve them from the Actions tab), not a
failing PR. New since last brief: `mergeable_state` is now `behind` — main has moved on in the
two days since this was opened, so it will need a rebase/branch-update before merge regardless of
what you decide on review. Diff is unchanged: one function, `get_my_submission_status` in
`src/canvas_mcp/tools/student_tools.py`, adding an `external_tools` bucket for LTI/external-tool
assignments instead of counting them as missing/overdue.

Next action: approve the Actions run if you want real CI signal before reviewing; either way it
will need an update-branch/rebase before it can merge cleanly.

### #387 — chore(deps-dev): bump @types/node from 26.5.1 to 26.6.1 (Dependabot) — unchanged since last brief

All real checks green (17 check runs); `claude-review` fails for the same OAuth-secret reason as
#395. No new activity since cutoff.

Next action: none required; merge at your discretion.

## New since 2026-09-23T02:26:30Z

- #395 — covered above under PRs.

(#399 was opened after the cutoff too, but by you — see **Flagged for you**, since the mechanical
"opened by someone other than you" rule doesn't apply to your own issue.)

## Going stale

Unchanged, no activity since 2026-09-08: #157, #172, #236, #301, #302.

Also stale by the mechanical rule, but for a different reason: **#374** — you gave a full
technical reply on 2026-09-14 and explicitly invited chiptoe-svg to retest the merged fix
(dd403bb) against a live instance; no reply from them in the 10 days since. This is waiting on
the reporter, not on you.

## Flagged for you

- **#375 — rubric update-safety issue; a shipped fix hasn't been reported back to the reporter.**
  Your last comment (2026-09-19) told chiptoe-svg that `update_rubric` "remains a separate
  capability" and doesn't exist yet. PR #386, "feat: add guarded rubric updates," merged the very
  next day (2026-09-20T17:47:29Z, tree `ad685aee`) and implements exactly the two hazards
  chiptoe-svg traced in Canvas source (silent criterion re-keying on a partial update; a PUT that
  can silently create a new rubric instead of updating one): it requires the complete ID-keyed
  criterion/rating set on every write, requires an explicit `rubric_association_id`, and re-fetches
  plus verifies the returned rubric/association identity after the write, reporting anything
  unexpected as unconfirmed rather than assuming success. Nobody has told chiptoe-svg it shipped.

  ⚠️ High-sensitivity: this issue is about writes that affect student grade/rubric data. The draft
  below is deliberately conservative — it states this is source-and-mocked-test verified only,
  not confirmed against a live Canvas write, matching PR #386's own scope note.

  Draft reply:
  ```
  Update: this shipped. PR #386 (merged 2026-09-20, ad685aee) adds a guarded `update_rubric` tool
  built around the two hazards you traced in Canvas source — it requires the full ID-keyed
  criterion/rating set on every write (never a partial replace), requires an explicit
  rubric_association_id, and re-fetches + verifies the returned rubric/association identity after
  the PUT, reporting anything unexpected as unconfirmed rather than assuming success. Structural
  criterion/rating changes are still a Canvas UI operation, not something the tool attempts.

  This is source-and-mocked-test verified only, not tested against a live Canvas write — if you
  get a chance to exercise it against a real instance, especially the read-back identity check,
  I'd like to know whether it holds up. Thanks again for the RubricsController#update /
  unique_item_id digging; it's what shaped the guard.
  ```

- **#399 — self-filed, no action owed to anyone else, flagged only for its labels.** You opened
  this yourself: `fetchAllPaginated` in the TypeScript client (`code_api/client.ts`) stops when a
  page is shorter than `per_page` instead of following the response's `Link` header, so it can
  silently under-report paginated results, and the acceptance criteria call out preventing
  credential forwarding if a malformed/malicious `next` link is followed. The repo's own
  auto-label automation already left a triage/categorization comment (`bug`, `infrastructure`,
  `security`); no external party is waiting on a reply. No PR exists against it yet. Surfacing it
  here only because it carries the `security` label and touches request-forwarding/credential
  safety, per this routine's standing instruction to flag anything security- or write-adjacent.

## Checked, nothing needed

#384 (weekly maintenance report from `claude[bot]`, opened 2026-09-20, no comments — 4 days old,
under the 5-day stale threshold), #391 (Windows install failure, still zero comments since it
surfaced in the last brief — nothing new to add beyond what that brief already drafted), #399
(reviewed — see Flagged for you; no reply owed).
