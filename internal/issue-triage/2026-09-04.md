# Triage brief — 2026-09-04

**Read path used:** GitHub MCP tools (`mcp__github__*`). `gh` CLI not installed in this sandbox
(expected per routine instructions); MCP access worked on the first call.

**Cutoff:** 2026-09-03T02:24:41Z — the merge timestamp of PR #348 (the 2026-09-03 triage brief),
continuing the "previous triage PR's merge time" convention used the last several briefs.
Confirmed via `search_pull_requests(is:merged base:main, sort:updated)`: #348 is the most recent
merge to `main` — nothing has landed on `main` since.

## Needs your reply

None. `search_issues` with `updated:>=2026-09-02` (a superset of the actual cutoff) returns **0**
results for this repo. No open issue has a comment newer than the cutoff; no one is waiting on a
reply.

## PRs

**#340 — `fix(security): make sandbox uid:gid configurable via TS_SANDBOX_UID_GID (#338)`**
(k0shir0, fork PR, not draft). Fifth brief in a row carrying this — nothing has moved since
2026-09-03, but the withheld-Actions problem is still open, which is why this brief exists
instead of an early exit:

- Still **`total_count: 0` check runs**. GitHub is still withholding the Actions run on this fork
  branch (first-time-contributor `action_required` gate) — unchanged since PR creation
  (2026-08-31). Nothing about this PR can move until "Actions tab → the run → Approve and run
  workflows" happens; no automation in this repo, including this routine, can clear it.
- Head is unchanged: `f8cba185b8e5ce5ddfdc22e93c616c42870d8d20` (k0shir0's last push:
  2026-08-31T23:17:48Z).
- `mergeable_state` now reads **`behind`** — GitHub finished the lazy recompute that yesterday's
  brief flagged as pending (`unknown`). This confirms yesterday's git-measured finding: #340 is
  behind `main` by its accumulated commits, but the two change sets are disjoint (#340 touches
  only `env.template`, `core/config.py`, `tools/code_execution.py`,
  `tests/security/test_sandbox_nonroot_user.py`; nothing on `main` since touches those files), so
  this is nominal staleness, not a conflict risk.
- AmirF194's review thread (`PRRT_kwDOOAwVQc6doTyC`, root-uid-0 finding at `config.py:449`) is
  still unresolved — no reply posted yet.
- **Unchanged, still true:** k0shir0's fix (`f8cba18`) rejects the literal `0:0`/`0:65532` but not
  leading-zero spellings (`00:00`, `00:65532`) — `config.py` does a string comparison
  (`uid == "0"`) rather than a numeric one, so those forms pass the `^\d+:\d+$` regex and skip the
  root guard. No new commit has touched this since 2026-08-31. (Per the 2026-09-03 executor note:
  this is measured against `validate_config()` directly, not against an actual container run — no
  docker daemon is available in this sandbox, so "skips the root guard" is confirmed but "runs the
  container as root" is inferred, not observed.)

**Next action:** unchanged from yesterday — approve the withheld workflow run so CI can run at
all; that is the sole blocking step for everything else (merge decision, the leading-zero
follow-up). Draft reply, if you want to raise the leading-zero gap alongside approving CI (same
draft carried forward, still accurate and not yet sent per the repo history):

```
Thanks both — AmirF194's repro was the clincher and k0shir0's fix (f8cba18) closes the literal
0:0 case with a test. One more gap before I merge: the fix compares strings, so 00:00 or 00:65532
(leading zeros) still pass the ^\d+:\d+$ format check and skip the uid==0 check. Could you switch
to a numeric comparison (int(uid) == 0 or int(gid) == 0) and add a test for the leading-zero case?
Approving the workflow run now so CI runs either way.
```

No other open PRs.

## New since 2026-09-03T02:24:41Z

Nothing. `search_issues`/`search_pull_requests` with `created:>2026-09-03` both return zero
results for this repo (other than this brief's own PR, opened after the search).

## Going stale

Same standing list as the last several briefs, no change (no activity on any of these since the
cutoff): **#172** (last non-Vishal touch: jonespm, 2026-08-04 — blocked on a New-Quizzes sandbox),
**#309 / #301 / #302 / #299** (reporters' last touch 12–18 days ago, no answer yet to open scoping
questions), **#318 / #325** (fixes already shipped in v1.12.0 — just waiting on reporter
confirmation).

## Checked, nothing needed

#142 (your own issue; no new activity since the fastmcp 4.0 re-scoping noted in the 2026-09-03
brief), #338 (your own issue; tracked by PR #340, covered above), #335 (automated weekly
maintenance report, out of scope per instructions — `weekly-maintenance.yml`), #157 / #236 (your
own issues, no external activity since cutoff).

---

## Executor results (2026-09-04T01:43:42Z)

Quiet day confirmed. No mechanical unblocking was available: the sole open non-triage PR (#340) is
a **fork** PR, which the routine forbids touching (its checks await maintainer approval — the empty
retrigger commit is a same-repo-only tool and would not clear an `action_required` gate anyway).
So the useful work today was **substituting for the withheld CI locally** and **measuring** the two
claims the brief could only assert.

### #340 — ran the checks GitHub is withholding

CI has never run on this PR (`total_count: 0` check runs, confirmed again today). I checked the
head out read-only and ran the suite both as-is and, more importantly, **merged with current
`main`** — the latter is what CI would actually build, since `mergeable_state` is `behind`.

| What | Result |
|---|---|
| Merge of `main` into `f8cba18` | **Clean, no conflicts** (15 files, +665/-58, all from `main`) |
| `pytest tests/` on PR head as-is | **1460 passed, 22 skipped** (29.2s) |
| `pytest tests/` on **merged** result | **1460 passed, 22 skipped** (25.2s) |
| `ruff check src/ tests/` (exact CI command) | **All checks passed** |
| `mypy src/` (exact CI command) | **Success: no issues found in 50 source files** |

This **confirms the brief's "disjoint file sets, nominal staleness, not a conflict risk" call by
measurement** rather than by inspection. Practical consequence: the only thing the withheld
approval is actually gating is the *record* of a green run — the run itself would pass on today's
`main`. That removes CI risk from the merge decision, though it does not remove the leading-zero
correctness question below.

Caveat, deliberately: this is my sandbox (Python 3.13, single version), not the 3.11/3.12/3.13
matrix CI runs. A version-specific failure would not show up here.

### Leading-zero gap — measured, not inferred

The brief asserted the gap from reading the code. I ran it. `validate_config()` on the merged
branch, effective value after validation:

```
'0:0'          -> '65532:65532'   (guard fires)
'0:65532'      -> '65532:65532'   (guard fires)
'00:00'        -> '00:00'         (guard SKIPPED)
'00:65532'     -> '00:65532'      (guard SKIPPED)
'abc'          -> '65532:65532'   (regex fallback)
```

**The brief's claim is correct as stated.** `00:00` and `00:65532` pass `^\d+:\d+$` and survive the
`uid == "0"` string comparison at `config.py:457` unchanged, so that literal string is what reaches
`--user`. Consistent with the 2026-09-03 executor note, the *second half* — that Docker then parses
`00` as uid 0 and runs the container as root — remains **inferred**: no docker daemon in this
sandbox, so I confirmed the guard is skipped, not the resulting container identity.

**Correction/context worth giving the contributor:** the string comparison is not a lapse on
k0shir0's part — AmirF194's review comment proposed `if uid == "0" or gid == "0":` verbatim, and
k0shir0 implemented exactly that. The draft reply reads as if the contributor chose a weaker check
than suggested; it should credit that they implemented the suggestion as given and that the gap is
in the suggestion. Draft amended accordingly below.

### Draft reply for #340 (amended — NOT SENT)

```
Thanks both — AmirF194's repro was the clincher, and k0shir0's f8cba18 implements the suggested
guard exactly as written, with a test. One gap that's in the suggestion rather than the
implementation: the comparison is on strings, so leading-zero spellings still slip through. I
measured it against validate_config() on your branch merged with current main:

  '0:0'     -> 65532:65532  (guard fires)
  '00:00'   -> 00:00        (guard skipped)
  '00:65532'-> 00:65532     (guard skipped)

Could you switch to a numeric comparison (int(uid) == 0 or int(gid) == 0) and add a leading-zero
test? Approving the workflow run now so CI runs either way — FWIW I ran it locally against merged
main and it's green (1460 passed, ruff + mypy clean), so CI should be a formality.
```

Unmeasured-claim audit of this draft: every number in it is measured above. It does **not** claim
the container runs as root under `00:00` — only that the guard is skipped, which is what I
observed.

### Verification of the brief's other claims

All spot-checked against live state, all accurate: #340 head still `f8cba18` (unchanged since
2026-08-31T23:17:48Z), `mergeable_state: behind`, zero check runs, AmirF194's thread
`PRRT_kwDOOAwVQc6doTyC` still `is_resolved: false`. `list_pull_requests` returns exactly two open
PRs (#350, #340) — "no other open PRs" confirmed. Closing-keyword check passed on the brief
(`scripts/check_closing_keywords.py --strict`, exit 0) and on the PR body; nothing to reword.

Not tested and why: **no other PR exists to test.** #350 is this brief (docs-only).

### Needs Vishal

1. **#340 — approve the withheld Actions run.** Fifth consecutive day. Actions tab → the pending
   run → "Approve and run workflows". Human-only: a first-time-contributor fork gate cannot be
   cleared by any automation in this repo, including this routine. *(Now lower-stakes: the run is
   green locally against merged `main`.)*
2. **#340 — post the amended draft reply above** (raises the leading-zero gap, credits the
   contributor correctly). I may not comment on PRs.
3. **#340 — merge decision is yours.** From here it is a one-line change (`int(uid) == 0`) plus a
   test away from complete; tests/lint/types are green and the merge is conflict-free.
4. Nothing else. No issue needed a reply today; the going-stale list is unchanged and none of it is
   actionable without reporter or sandbox input.
