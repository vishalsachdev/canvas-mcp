# Daily Triage Brief — 2026-09-08

**GitHub read path:** GitHub MCP tools (`list_issues`, `list_pull_requests`, `pull_request_read`, `issue_read`).
**Cutoff:** 2026-09-05 — newest existing brief file was `internal/issue-triage/2026-09-05.md`.

## Needs your reply

None. No open issue received a comment from anyone after the 2026-09-05 cutoff — all six open issues (#172, #302, #301, #299, #236, #157) are quiet since before the cutoff.

## PRs

### #360 — Protect shared-server exports and redact logged URL credentials
*Author: vishalsachdev (from `codex/review-2026-09-06`), ready for review, not draft.*

- CI: all 16 checks green — `test (3.11/3.12/3.13)`, `lint`, `claude-review`, CodeQL, SAST, secret scan, dependency scan, Cursor agents (neutral). Mergeable state is `blocked` — there's no approving review yet, so this is **green but unreviewed**.
- ⚠️ **High-sensitivity — read before merging.** Both automated PR reviews (Codex and Claude) independently flagged the same thing: the PR's own commit history added `REVIEW-2026-09-06.md`, a security-review report documenting five **still-unpatched** findings with file:line detail and exploit scenarios — S2 (unbounded pipe buffering / host memory exhaustion in `code_execution.py`), S4 (no request-body size cap on the authenticated HTTP path in `server.py`), S5 (ReDoS via caller-controlled regex in `discussions.py`), S6 (timing side-channel in the access-key comparison in `server.py`), S7 (unbounded anonymization-cache growth). The file was removed before the PR's current head, but the content is real git history reachable through the PR's own commits, which cuts against `SECURITY.md`'s own instruction not to publicly disclose unpatched vulnerabilities. This is independent of the actual code change, which both reviewers verified as sound (HTTP export guards for `generate_peer_review_report` / `extract_peer_review_dataset` / `create_student_anonymization_map` fire before any Canvas fetch or file write; `sanitize_url()` correctly strips credentials from logged URLs; tests cover the refusal paths).
- This PR is itself student-data-adjacent (peer-review reports, anonymization maps) — treat with the usual FERPA caution, though the change here is a restriction, not a new exposure.
- **Next action:** decide whether the exposed history needs remediation (advisory, or accept it's already shared via PR history) before approving; then get an approving review and merge.

**Draft note for yourself (not sent anywhere):**
```
Before merging #360: both automated reviews flagged that this branch's history carries
REVIEW-2026-09-06.md, documenting 5 still-unpatched findings (S2 pipe buffering, S4 no
body-size cap, S5 ReDoS, S6 timing side-channel, S7 unbounded cache) with exploit detail.
It's off the final tree but still reachable via PR history, which conflicts with our own
SECURITY.md disclosure policy. Want to open private advisories for those five (or fix them)
before this merges — the export/redaction change itself looks clean on both reviews.
```

### #359 — chore: remove unused code and dependencies
*Author: vishalsachdev, draft (from `chore/repository-simplification-audit`).*

- CI: all 16 checks green.
- Still marked draft by the author's own note ("This PR remains draft for review").
- Both automated reviews verified the deletions against the actual tree (not just the PR description) and found no functional regressions, but both independently flagged the same soft spot: `tsconfig.json` swaps `moduleResolution: "node"` for `types: ["node"]` — a real config change, not a like-for-like tidy — and there is no CI job that runs `tsc`/`npm run build` for `code_api`, so nothing currently catches a regression there.
- **Next action:** run `npm ci && npm run build` locally against `code_api` to confirm that swap compiles clean, then mark ready for review.

### #358 — chore(deps): bump the actions group (2 updates)
*Author: dependabot[bot], new since cutoff (opened 2026-09-07).*

- CI: `test (3.11)` **failing**; `claude-review` also failing.
- The `claude-review` failure matches the known no-repo-secrets pattern (per #188, this check isn't required and routinely fails on runs that don't get secrets). The `test (3.11)` failure is different and, on its face, unexpected — this PR only bumps pinned SHAs for `anthropics/claude-code-action` and `trufflesecurity/trufflehog` in workflow YAML, which shouldn't touch Python test behavior. I have not opened the job log, so this is unverified — don't assume it's a flake without checking.
- **Next action:** check the `test (3.11)` job log before merging. If it's unrelated to this bump, one re-run is within policy; if it reproduces, it needs root-causing rather than merging through it.

### #357, #356, #355 — routine dependency bumps
*Author: dependabot[bot], all new since cutoff (opened 2026-09-07).*

- #357: `chore(deps): bump python from cae66f2 to cad9a2c` (Docker base image).
- #356: `chore(deps-dev): bump @types/node from 26.4.0 to 26.4.1`.
- #355: `chore(deps-dev): bump tsx from 4.23.12 to 4.23.13`.
- CI: all green except `claude-review` on each (same expected no-secrets pattern as #358 — non-required, non-blocking).
- **Next action:** none beyond routine merge when convenient.

## New since 2026-09-05

- PR #360 (vishalsachdev) — see PRs above.
- PR #359 (vishalsachdev) — see PRs above.
- PR #358, #357, #356, #355 (dependabot[bot]) — see PRs above.
- No new issues since cutoff.

## Going stale

- **#172** (zqian, Canvas Quizzes) — last non-owner touch was jonespm on 2026-08-04; work is blocked on a New-Quizzes-enabled sandbox to verify detection logic, so this is tracked-and-blocked, not neglected.
- **#302, #301** (rpsimon-ai, skill requests) — you asked follow-up questions on 2026-08-21 / 2026-08-19; no reply from the reporter since the issues were opened on 2026-08-18.
- **#299** (turner-moore, cookie-auth) — you asked for a sanitized verification result on 2026-08-19; no reply since it was opened on 2026-08-17.
- **#236, #157** (your own backlog/roadmap items) — only an automated triage-label comment since filing; not waiting on anyone external, just unscheduled.

## Checked, nothing needed

#172, #302, #301, #299, #236, #157 — read full comment history on each; no non-owner activity on any of them since the 2026-09-05 cutoff.
