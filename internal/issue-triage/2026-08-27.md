# Triage brief — 2026-08-27

**Read path used:** GitHub MCP tools (`mcp__github__*`).
**Cutoff:** 2026-08-26 (`internal/issue-triage/2026-08-26.md` is the newest prior brief). That
brief's own triage PR (#324) merged to `main` at `2026-08-26T02:28:16Z` — using that merge as the
effective boundary for "new since cutoff" and "comment newer than cutoff."

Since that boundary: PR #191 was **closed** by Vishal (not merged — closed as unverifiable, per
the standing recommendation), four dependabot PRs (#295, #296, #297, #322) were **merged**, one
new issue was filed (#325), and PR #317 got a full review round-trip (changes requested → fixes
pushed). None of categories 1/2/3 is empty, so this is a full brief, not an early exit.

## Needs your reply

### ⚠️ #317 — `fix(security): run execute_typescript sandbox as non-root user` (AmirF194, PR)

Security-sensitive (sandbox/execute_typescript hardening, tracks issue #157). Not a comment on an
issue, but the PR thread is squarely in "waiting on you": you left `CHANGES_REQUESTED` at
`13:12:46Z` flagging that the two files the container reads (`code_execution.py`'s temp script,
`NamedTemporaryFile` → mode 0600, and the `.cjs` network-guard, `chmod 0o600`) would be unreadable
to the new non-root uid (`65532:65532`) inside a read-only bind mount on a real Linux Docker host —
Docker Desktop for Mac was hiding this. You approved the pending Actions run for the existing head
at the same time. CI then failed on collection (`ModuleNotFoundError: tests.security`, an import
that only resolved because the repo root happened to be on `sys.path` locally). AmirF194 pushed a
fix at `14:34:17Z` (head now `b28ae4b`): inlined `get_execute_typescript` instead of importing it
across test modules, and chmod'd both files to `0o644` in container mode — and says they verified
the permission fix directly on a Linux Docker host, which is exactly what you asked for.

The new commits are unreviewed by CI: `get_check_runs` on the current head still returns
`total_count: 0` — a first-time contributor's new push needs a fresh Actions approval, same as the
first one did.

**Draft reply:**
```
Thanks for turning this around fast, and for actually verifying on a Linux Docker
host rather than just asserting it — that was the missing piece. Approving the
Actions run for b28ae4b now; once it's green I'll take a final look and merge.

One thing I'll sanity-check on the way past, not blocking: 0644 widens both
files from 0600. They're already inside a read-only bind mount scoped to the
container, so I don't think it reopens anything, but I want to look at it
directly rather than wave it through.
```

**Action needed from you, outside the PR:** approve the pending Actions run from the Actions tab
for head `b28ae4b` (same mechanism as last time) before CI can report.

## PRs

- **#317** — see above. `mergeable_state: behind` (one merge-commit's worth, from #324 landing
  after the branch was last synced — same routine drift as other PRs in past briefs). Not draft,
  ready once green + your final look.
- No other open PRs. #191 was closed (not merged) by you on 2026-08-26; #295/#296/#297/#322
  (dependabot) all merged 2026-08-26 between 13:13 and 13:22 UTC.

## New since 2026-08-26

- **#325 — "[Feature]: Provide option to disable accessibility checkers if not using
  UDOIT/UFIXIT"** (jonespm, opened 2026-08-26T17:23:12Z). Reports that the four accessibility
  tools (`scan_course_content_accessibility`, `fetch_ufixit_report`, `parse_ufixit_violations`,
  `format_accessibility_summary`) all depend on the UDOIT/UFIXIT add-on, and return nothing useful
  — while still cluttering the tool list — for any institution that doesn't have it installed.
  Asks for an env var to gate the whole group off when no checker is configured, with an eye
  toward supporting other accessibility-checker backends later.

  Unverified root-cause check: confirmed in `src/canvas_mcp/server.py:467`,
  `register_accessibility_tools(mcp)` is called unconditionally with no environment-variable
  gate, and `src/canvas_mcp/tools/accessibility.py` has no feature-flag check anywhere in it — so
  the premise is accurate as read, not just as reported.

  **Draft reply:**
  ```
  Thanks for the detailed report, and the tool list is right — all four are
  UDOIT/UFIXIT-only today, unconditionally registered. Confirmed there's no
  gate at all right now (server.py just always registers the group). An
  env var that turns the group off when no checker is configured makes
  sense, and I like leaving the door open for other backends later rather
  than hard-coding UDOIT as the only option. Not scheduled yet — I'll fold
  it into the backlog and follow up here with a plan.
  ```

## Going stale

- **#309** (zqian, content migration) — your reply with v1 design + scoping questions was
  2026-08-21; no response since his 2026-08-19 filing (8 days).
- **#172** (zqian/jonespm, New Quizzes) — last activity 2026-08-07 (your update after PR #191's
  correctness bug); now moot in one sense (#191 closed 2026-08-26) but the issue itself has had no
  comment reflecting that closure yet. Worth a short status comment noting #191 is closed as
  unverifiable and the issue stays open pending a New-Quizzes sandbox — see draft below.
- **#301** (rpsimon-ai, assignment-group skill request) — you replied 2026-08-19; no response since
  their 2026-08-18 filing (9 days).
- **#302** (rpsimon-ai, student-analytics skill request) — you replied 2026-08-21; no response
  since their 2026-08-18 filing (9 days).
- **#299** (turner-moore, cookie-auth feature) — you replied 2026-08-19; no response since their
  2026-08-17 filing (10 days).
- **#142** (watch item, `mcp<2` pin) — last activity 2026-07-30 (28 days). Still blocked on an
  upstream fastmcp release; no action available.
- **#236, #157** — no activity in weeks; both already tracked in `CLAUDE.md` Current Focus as
  blocked on external access (admin access; sandbox access).

### Draft reply — #172 (optional, since #191 just closed)

```
Closing the loop here: PR #191 is closed, not merged. It had a real
correctness bug (the New Quizzes detection filter matched Classic quizzes
in live testing) and I'd rather leave this open than ship something that
confidently returns an empty list. Re-opening implementation once a
New-Quizzes-enabled sandbox is available to verify against — still open
to a scoped token if you have one.
```

## Checked, nothing needed

- **#318** — you already replied 2026-08-26T13:12:41Z with the corrected, accurate answer (no
  delete tool uses a confirmation token today; three of seven have partial guards; three are
  unguarded; `delete_assignment` doesn't exist yet). Ball is in zqian's court now; not stale (1
  day).
- **#315** — last comment is `claude[bot]`'s automated labeling explanation; not a request.
- **#325** — covered above under "New since cutoff," draft reply included there.
