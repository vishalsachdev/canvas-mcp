# Triage brief — 2026-09-23

**Read path used:** GitHub MCP tools throughout (`gh` not installed in this sandbox).

**Cutoff:** 2026-09-22T02:25:15Z — the merge time of the previous brief's triage PR, **#389**
(same convention the last several briefs have used). Everything below newer than that timestamp
counts as "since cutoff."

Not a quiet day: one new issue and one new PR arrived from outside contributors after the cutoff.

## Needs your reply

None. No open issue has a comment newer than cutoff from anyone other than you. (#391 is a
newly-opened issue, not a new comment on an existing thread — see "New since cutoff.")

## PRs

### #390 — fix: separate external-tool assignments from missing submissions (@EastArctica)

First-time contributor to this repo (no prior issues or PRs found), submitting from a fork. Check
runs report `total_count: 0` — per the known pattern, GitHub withholds Actions runs from
first-time contributors until a maintainer approves them from the Actions tab, so this is "not yet
approved to run," not a failing PR. `mergeable_state` is `blocked`, consistent with required checks
simply not having run yet rather than a merge conflict.

I read the diff directly rather than trusting the description. It touches one function,
`get_my_submission_status` in `src/canvas_mcp/tools/student_tools.py` (around line 270), adding an
`external_tools` bucket alongside the existing `submitted`/`missing` split: assignments whose
`submission_types` includes `external_tool` (e.g. Gradescope) are reported separately instead of
being counted as missing/overdue. The rationale is sound and matches how Canvas actually behaves —
Canvas's local submission record can stay `unsubmitted` for LTI-graded work even after it's been
submitted and graded on the external provider's side, so folding that into "missing" produces a
false overdue alert. The new assignment-name line uses `fence_untrusted_inline`, consistent with
the #239 provenance-fencing convention, and a new regression test
(`test_get_my_submission_status_separates_external_tools_from_missing`) covers a past-due
external-tool assignment. The PR body reports local verification (ruff clean; `pytest -q` — 1,582
passed, 21 skipped) and a live read against a real Gradescope-linked course; I have not repeated
that live check myself. Single file + one test file changed, +52/-1.

**Next action (yours):** approve the workflow run for this fork PR in the Actions tab so real CI
(the full test matrix, lint, security scans) actually reports before you consider merging — this
routine has no ability to do that. Once CI is green, this reads as a small, well-scoped, low-risk
fix on its own merits.

### #388 / #387 — Dependabot bumps (unchanged since the 2026-09-22 brief)

No new commits and no new comments on either since the last brief — same head SHAs (`d6eb37e` /
`8f63289`) and the same check results: all real checks green (`test-enhancements`, `lint`, full
test matrix, CodeQL, Secret Detection, SAST, Dependency Vulnerability Scan, Security Test Suite),
and `claude-review` still fails only because Dependabot-authored runs don't receive the
`CLAUDE_CODE_OAUTH_TOKEN` secret (the same class of limitation as the fork-PR case in #390, and the
reason `claude-review` isn't a required check). Both remain green-but-unreviewed, low-risk version
bumps (CI/tooling metadata + a devDependency type-stub package). Merge decision is yours and
outside this routine's boundary; last brief already flagged the once-only idea of a Dependabot
auto-merge rule for green patch/minor bumps, not repeating that analysis here.

## New since 2026-09-22T02:25:15Z

- **#391** — "Installation challenges" (@dmalpiCS, first-time reporter). Detail and draft reply
  below.
- **#390** — covered above under PRs.

### #391 — Installation challenges (@dmalpiCS)

They installed the Claude Desktop Extension (`.mcpb`) on Windows 11 and it fails to launch, with
Claude Desktop showing:

```
Command: uv
Arguments: run --directory C:\Users\dm\AppData\Roaming\Claude\Claude Extensions\local.mcpb.vishal-sachdev.canvas-mcp canvas-mcp-server
Error: spawn uv ENOENT
```

They're asking for a recommended install path, and ideally one where they don't have to manage
`uv` themselves at all.

**Unverified root-cause analysis** (read from source, not reproduced on a live Windows machine):
`manifest.json:21-22` hardcodes the launch command as the bare string `"uv"` with no absolute path
and no bundled/vendored `uv` binary — it relies entirely on `uv` being resolvable on PATH at the
moment Claude Desktop spawns the subprocess. `ENOENT` from `spawn` is the standard Node/Electron
error for "command not found on PATH." On Windows specifically, GUI-launched applications
(Claude Desktop included) typically inherit the PATH that was captured at the last sign-in/login,
not a live view of it — so installing `uv` *after* that snapshot (or without a subsequent
sign-out/sign-in or reboot) can leave a fresh terminal working fine while any already-running GUI
app still can't see it. That would explain a Windows-specific `ENOENT` on an otherwise-correct
install. This is plausible, not confirmed — I have no Windows sandbox to test it in.

```
Thanks for the detailed error output — that's exactly what I needed to narrow this down.

The extension currently launches the server with a bare `uv` command, relying on it being
resolvable on your PATH, rather than a full install path. Windows GUI apps like Claude Desktop
often only pick up PATH changes from your last sign-in, so if `uv` was installed after Claude
Desktop was already running (or since your last full sign-in), the extension can't see it yet —
even though `uv --version` works fine in a brand-new terminal.

Could you try:

1. Open a *new* Command Prompt or PowerShell window and run `uv --version`. If that fails too, the
   install itself didn't land on PATH — reinstalling via the official installer
   (https://docs.astral.sh/uv/getting-started/installation/) should fix it.
2. If `uv --version` works in a fresh terminal but Claude Desktop still can't find it, try fully
   signing out and back in (or rebooting). That forces Windows to refresh the environment that
   Claude Desktop inherits.
3. As a workaround in the meantime, you can skip the extension and do a full manual install
   instead (this doesn't need `uv` at all):
   - Clone the repo and set up a virtualenv:
     ```
     git clone https://github.com/vishalsachdev/canvas-mcp
     cd canvas-mcp
     python -m venv .venv
     .venv\Scripts\activate
     pip install -e .
     ```
   - Copy `env.template` to `.env` and fill in your `CANVAS_API_TOKEN` and `CANVAS_API_URL`.
   - In `%APPDATA%\Claude\claude_desktop_config.json`, point the `command` at the full path to
     the installed binary, e.g.
     `C:\Users\dm\canvas-mcp\.venv\Scripts\canvas-mcp-server.exe` — no `uv` involved at that
     point.

You're right that you shouldn't have to debug PATH resolution to use the extension — I'll look at
having the manifest resolve `uv`'s path more defensively so this doesn't come up for the next
person. Let me know if the steps above get you unblocked in the meantime.
```

## Going stale

- **#157** (sandbox egress hardening) — unchanged; last non-owner touch still a `claude[bot]` label
  note from 2026-07-04.
- **#172** (Canvas Quizzes, zqian) — unchanged; last non-owner comment still `jonespm` on
  2026-08-04. Still blocked on a New-Quizzes-enabled sandbox.
- **#236** (OAuth2 dev-key flow) — unchanged; last non-owner touch still a `claude[bot]` label note
  on 2026-08-07.
- **#301** (assignment-group skill request, rpsimon-ai) — unchanged; no reporter reply since
  2026-08-18.
- **#302** (student-risk analysis skill, rpsimon-ai) — unchanged; no reporter reply since
  2026-08-21.
- **#374** (`use_for_grading` grading gap, chiptoe-svg) — last non-you touch is still the original
  report (2026-09-13), now 10 days old. Parked on your 2026-09-14 reply asking the reporter to
  retest main and report back Canvas version/feature flags — still waiting on them, not neglected.
- **#375** (rubric-update design question, chiptoe-svg) — last non-you touch is chiptoe-svg's
  2026-09-14T11:29:15Z comment, now 9 days old, mechanically over the threshold. See the flagged
  note below — this one needs a fresh reply from you for a reason unrelated to staleness.

## Checked, nothing needed

⚠️ **#375** (high-sensitivity — rubric-update design question) — read the full thread again. Your
most recent comment (2026-09-19T17:18:26Z) is still the latest activity, so this doesn't trigger
today's "new comment from someone else" rule. But it's worth flagging anyway: that comment was
written *before* **PR #386** (`feat: add guarded rubric updates`) merged to `main` as `e5ff3b1` on
2026-09-20T17:47:29Z, which added `update_rubric` at `src/canvas_mcp/tools/rubrics.py:1648` and
implements the exact safeguards you listed on the issue (ID-preserving replacements,
preview/confirmation, read-back verification). chiptoe-svg supplied the Canvas-source hazard
analysis that shaped those safeguards and, as of this run, still hasn't been told the capability
exists. This was flagged in the previous brief's addendum too and remains open — worth a reply
when you have a moment, and it's a judgment call whether it also means #375 can close (the update
capability now exists, but its hazards are still source-reported, not live-verified).

**#384** (weekly maintenance report, opened by `claude[bot]` 2026-09-20T02:08:02Z) — no activity
since creation, and only 3 days old, so it doesn't clear the 5-day staleness threshold this round
either. Not re-investigated beyond that.

**#157, #172, #236, #301, #302, #374** — reviewed via issue reads, unchanged in substance since the
last brief; no action beyond what's noted in Going Stale above.

---

Grepped this file for `fix|fixes|fixed|close|closes|closed|resolve|resolves|resolved` immediately
preceding an issue reference: no hits (all issue references above are bare `#NNN` mentions in
prose, not adjacent to a closing verb).

---

## Executor results (2026-09-23T02:21:54Z)

**Closing-keyword check — clean.** Ran `scripts/check_closing_keywords.py --strict` over this brief
file (exit 0) and over PR #392's body (exit 0), plus a negative control — a throwaway line pairing a
closing keyword with a fake issue number — which the detector rejected with exit 1, proving it was
actually firing rather than silently passing everything. Nothing to reword. Every issue reference in
the brief and the PR body is a bare `#NNN` in prose.

### Mechanical work done

**Stalled / no-CI PRs (step 3a): one flagged, and it is untouchable from here.**
#390 is the only PR with no CI. A live read confirms the brief exactly: `get_check_runs` on #390
returns `total_count: 0`. It is a **fork** PR (`EastArctica`), so per the routine's own boundary I
did not update-branch it and did not push a retrigger commit — fork check runs wait on a
maintainer's approval in the Actions tab, which no push from here can substitute for. Recorded, not
acted on.

**One thing changed under the brief since yesterday, which the brief does not reflect (see
correction 1): #387 and #388 are now `behind` `main`.** They are *not* stalled — both carry complete
CI on their existing head SHAs — so I left them alone rather than update-branch them. Nothing here
is blocked by that; noted so the state is not a surprise at merge time.

**Local verification of open PRs (step 3b): all three tested, real output below.**
Checked out each PR head read-only in a separate `git worktree` under the scratchpad, synced with
`uv sync --group dev`, ran the full suite plus lint. (Same invocation note as yesterday: dev deps
live in `[dependency-groups]`, so `--group dev` is the working form, not `--extra dev`.)

| PR | head | `uv run python -m pytest tests/ -q -rf` | `uv run ruff check .` |
|---|---|---|---|
| #387 (`@types/node` 26.5.1→26.6.1) | `8f63289` | `1580 passed, 22 skipped in 31.72s` | `All checks passed!` |
| #388 (actions group, 7 updates) | `d6eb37e` | `1580 passed, 22 skipped in 31.02s` | `All checks passed!` |
| #390 (external-tool split) | `93b5c78` | `1581 passed, 22 skipped in 32.15s` | `All checks passed!` |

No failures, so `-rf` printed no failure lines on any run. **PRs not tested: none.** All three open
non-triage PRs were run locally; the triage PR itself is covered by its own CI (all 15 checks green
on `646a4e6`, including `claude-review` — which passes here precisely because this PR is not
bot-authored).

**#390's new test is not vacuous — I measured that, rather than assuming it.** I reverted
`student_tools.py` to `main`'s version inside the #390 worktree and reran only the new test. It
fails, and it fails with exactly the symptom the PR describes:

```
E   AssertionError: assert 'External-tool assignments (1)' in 'Submission Status for TEST-101:\n\n
    ⚠️  Missing Submissions (1):\n\n• <<<UNTRUSTED CANVAS CONTENT (assignment name, ...):
    Gradescope homework>>>\n  \n  Due: 2026-09-21T02:18:54Z\n  Status: OVERDUE\n'
```

A past-due `external_tool` assignment is reported `OVERDUE` under `main` and moves to its own bucket
under the PR. The worktree was `git reset --hard` afterwards and the test passes at the PR head.
This confirms the *mechanism* against mocked Canvas payloads; it does not confirm what a live
Gradescope-linked course returns, which only the contributor has checked.

### Corrections and additions to the brief

1. **#387 and #388 are no longer up to date with `main`.** The brief says they are "unchanged since
   the 2026-09-22 brief — same head SHAs and the same check results." Head SHAs and check results
   are indeed unchanged, but their *merge position* is not: yesterday's executor pass measured both
   as up to date, and today both report `mergeable_state: "behind"`, with
   `git merge-base origin/main <head>` = `e5ff3b14` while `main` is now `af2ad87f`. The cause is
   benign and is this routine's own footprint — PR #389 (the 2026-09-22 brief) merged at
   02:25Z on 09-22 and moved `main` past their base. Practical effect: if branch protection requires
   branches to be up to date before merging, both will need an update-branch or an
   `@dependabot rebase` comment before the merge button works, and that rerun of CI is the
   maintainer's call (and a public comment, so outside this routine's boundary either way).

2. **The #391 draft reply's step 3 misdescribes the README's manual install, and should not be sent
   as written.** The draft says: "run `where uv` to get its full path, then use the manual
   (non-extension) install method in the README, pointing your MCP client config at that full path
   instead of the bare `uv` command." The README's manual method does not invoke `uv` at all — every
   client config block (`README.md:387`, `:432`, `:472`, `:489`) sets
   `"command": "/absolute/path/to/canvas-mcp/.venv/bin/canvas-mcp-server"`, i.e. the console script
   inside a virtualenv. Two consequences: the reporter has no `.venv` to point at (they installed the
   `.mcpb` extension, not a clone), so this is a *full manual install*, not a config tweak — and the
   documented path is the POSIX layout. The Windows equivalent exists but is a parenthetical at
   `README.md:512` (`C:\Users\YourName\canvas-mcp\.venv\Scripts\canvas-mcp-server.exe`), well below
   the config blocks a reader would copy from. A Windows reporter handed the draft's step 3 verbatim
   would be sent to a path that does not exist on their machine.

   **Fixed in this commit** (flagged again by Codex's own review of this PR, which caught that the
   copyable draft itself — not just this corrections section — still had the bad step 3). Step 3 of
   the draft above now spells out the actual Windows manual-install path: clone, venv, `pip install
   -e .`, `.env`, then point the client config at `.venv\Scripts\canvas-mcp-server.exe`. No `uv`
   involved.

3. **The #391 root-cause claim about `manifest.json` is confirmed by source.** `manifest.json:21-22`
   reads `"command": "uv"` with `"args": ["run", "--directory", "${__dirname}", "canvas-mcp-server"]`.
   Bare command string, no absolute path, no vendored `uv`. The extension does depend entirely on
   `uv` being resolvable on the PATH that Claude Desktop inherits, exactly as the brief says. The
   *Windows PATH-snapshot* half of the diagnosis remains unverified — I have no Windows sandbox
   either — but the brief marks it as unverified and the draft reply hedges it ("often"), which is
   the right posture. Steps 1 and 2 of the draft are sound and diagnostic rather than assertive.

4. **The brief's #375 flag is accurate, and I verified the shipped tool actually answers both of
   chiptoe-svg's hazards** — this is stronger than "implements the safeguards you listed" and makes
   the reply easier to write. `update_rubric` is at `src/canvas_mcp/tools/rubrics.py:1648`, merged as
   `e5ff3b1` (2026-09-20T17:47:29Z). Against their two source-read hazards:
   - *Hazard 1 — an update that omits IDs silently re-keys every criterion and orphans existing
     assessment scores.* The tool requires `criteria` as an **ID-keyed JSON object containing every
     current criterion and rating, with each object's `id` repeated inside it**, validated against
     the rubric's current data by `validate_rubric_update_criteria()`; it refuses to add or remove
     criteria/ratings at all, only editing text and points.
   - *Hazard 2 — a `PUT` can write a new rubric instead of updating one, and the association is only
     touched when specified.* `rubric_association_id` is a **required** parameter (their exact
     `params[:rubric_association_id]` point), and the write path compares the returned rubric and
     association IDs against the requested ones; on mismatch it calls
     `unconfirmed_write_warning(... "returning a different copy" ...)` rather than reporting success.
     A read-back `GET` then re-verifies title, criteria and the free-form-comments flag, and also
     fails to `unconfirmed_write_warning` rather than claiming success.
   - Plus the preview/confirmation token Vishal asked for, with drift invalidating the token.
   Still true, and still worth saying on the issue: these are safeguards written *against* a
   source-read hazard model. Nothing here has been exercised against a live Canvas instance, by
   them or by us.

5. **Test-count discrepancy in #390's PR body, explained and benign.** The PR body reports
   `1,582 passed, 21 skipped`; I measured `1581 passed, 22 skipped` on the same head. The one
   test that differs is `tests/test_closing_keyword_guard.py:360`, which skips with *"shallow clone:
   the replay needs full history to be meaningful. CI sets fetch-depth: 0 for exactly this test."*
   The contributor ran against a full clone, this sandbox's checkout is shallow. Not a discrepancy
   in their work, and not a reason to doubt their reported run.

6. **Unverified nit on #390, for the review not for the merge decision** (flagged, not acted on —
   commenting on someone else's PR is outside this routine's boundary). The new line is
   `"external_tool" in assignment.get("submission_types", [])`. The `[]` default only applies when
   the key is *absent*; if Canvas ever returns `"submission_types": null`, this raises
   `TypeError: argument of type 'NoneType' is not iterable` and takes down the whole tool rather
   than one assignment. `assignment.get("submission_types") or []` would be the defensive form. I
   have **not** established that Canvas ever returns null there — every assignment in the test
   fixtures carries a list — so this is a robustness observation, not a defect I can demonstrate.

7. **No other unmeasured-behavior claims to flag.** Aside from item 2, the brief is careful: it
   labels the #391 root cause "Unverified root-cause analysis," says outright "This is plausible,
   not confirmed — I have no Windows sandbox to test it in," and does not claim to have repeated
   #390's live Gradescope check. The #374/#375 Canvas-behavior items stay marked source-reported.

8. **Verified unchanged / no correction needed:** #390's diff matches the brief's description
   (one function in `student_tools.py`, one new regression test, `fence_untrusted_inline` used on
   the new assignment-name line, +52/-1); #375's last comment really is Vishal's at
   2026-09-19T17:18:26Z with chiptoe-svg's 2026-09-14T11:29:15Z analysis preceding it; #391's quoted
   error and ask match the issue body verbatim.

### Needs Vishal

1. **Approve the workflow run for #390 in the Actions tab.** This is the one action that unblocks a
   first-time outside contributor, and nothing in this routine can substitute for it. Until then the
   PR shows zero checks and `mergeable_state: blocked`. Local evidence is good — full suite and lint
   clean at `93b5c78`, and the new test demonstrably fails without the fix — but that is this
   sandbox, not the 3.11/3.12/3.13 matrix or the security scans.
2. **Reply to chiptoe-svg on #375 — carried over from yesterday, still unsent, still the highest-value
   item in the pass.** Correction 4 above gives you the mapping from their two hazards to the shipped
   safeguards, so this can be concrete rather than a bare "it shipped." Whether #375 then closes is
   still a judgement call: the capability exists and is guarded, but the hazard model behind those
   guards has never been exercised against a live instance.
3. **Reply to #391 — the draft is now fixed and copyable as-is.** Step 3 was rewritten (see
   correction 2) to the actual Windows manual-install fallback: clone, venv, `pip install -e .`,
   `.env`, then point the client at `...\.venv\Scripts\canvas-mcp-server.exe` — no `uv` path. The
   reporter's actual ask — "even better, if the end user did not have to handle that" — is a real
   product gap in `manifest.json`, and is a change outside `internal/issue-triage/`, so this routine
   cannot make it.
4. **Merge decision on #387 and #388.** Both clean locally (table above), all real checks green,
   `claude-review` failing only for the known bot-secret reason. Read correction 1 first: both are
   now behind `main` and may need an update-branch before the merge button is live. Yesterday's
   point about #388 still holds and is worth re-reading before merging it — the bumped
   `anthropics/claude-code-action` pin is the one piece of that group its own CI cannot exercise.
5. **Not fixable from here, unchanged:** `claude-review` will keep failing on every Dependabot PR,
   and fork PRs will keep arriving with zero checks. Both live in `.github/workflows/`, outside this
   routine's write boundary.

---

## Executor results (2026-09-23T09:34:00Z) — second pass

This is a **second executor pass on the same day**. The first pass (02:21:54Z, recorded above)
ran against the brief PR **#392**, which merged. This pass ran against **#393**, the open
`triage/2026-09-23-followup` PR that carries the post-merge-race Windows fix into `main`. It is
the newest open `triage/*` PR, so it is this pass's target.

**Closing-keyword check — clean.** `scripts/check_closing_keywords.py --strict` over this brief
file: exit 0. Over #393's PR body (saved verbatim to a scratch file): exit 0. Negative control
(a throwaway line pairing a closing keyword with a fake issue number) rejected with exit 1 and the
expected "this will close [issue 999]" warning, so the detector was firing rather than passing
everything. (The literal warning string is not quoted here: it contains the very pattern the guard
blocks, and quoting it verbatim failed this file's own check on the first attempt — a small
demonstration that the guard catches prose, which is exactly what it exists for.) Nothing to
reword.

### The finding of this pass: PR #394's CI is RED, and its PR body reports the suite as green

**#394** ("fix: verify ConfirmationGuard and close burn/expiry replay gaps", `f355c58`,
same-repo branch `fix/confirmation-guard-verification`) was opened at 09:24:14Z — **after** the
brief and after the first executor pass — so nothing above covers it. Five checks failed:
`test (3.11)`, `test (3.12)`, `test (3.13)`, `test-enhancements` (the required aggregator, which
fails because the matrix failed), and `Security Test Suite`. `lint`, `test-code-api`,
`verify-confirmation`, CodeQL, SAST, Secret Detection and Dependency Vulnerability Scan all passed;
`claude-review` was still in progress at the time of this read.

The PR body states: *"Full Python suite: 1,589 passed, 21 skipped."* CI's 3.11 job actually ended:

```
FAILED tests/security/test_confirmation_state_machine.py::test_mismatch_during_inflight_send_survives_rejection_release - ModuleNotFoundError: No module named 'tests'
================= 1 failed, 1589 passed, 20 skipped in 20.21s ==================
```

**Root cause, measured — the author's run and CI's run are not the same command.** The new test
does `from tests.tools.test_messaging import get_educator_tool_function`
(`tests/security/test_confirmation_state_machine.py:53`). `tests/tools/__init__.py` and
`tests/security/__init__.py` exist, but **`tests/__init__.py` does not**, so `tests` is importable
only when the repository root happens to be on `sys.path`. `python -m pytest` puts the working
directory on `sys.path`; a bare `pytest` does not. CLAUDE.md's documented local command is
`uv run python -m pytest tests/ -v` (the `-m` form); CI runs bare `pytest tests/ -q`
(`.github/workflows/canvas-mcp-testing.yml:103`) and bare `pytest tests/security/ -v`
(`.github/workflows/security-testing.yml:44`). Both of CI's forms miss the path the author's form
supplies. That is the whole discrepancy — not a flake, not an environment difference in CI.

A/B measured in a read-only worktree at `f355c58`, same machine, same interpreter, one test:

| invocation | result |
|---|---|
| `uv run python -m pytest <the one test> -q` | `1 passed in 0.05s` |
| `uv run pytest <the one test> -q` (CI's form) | `1 failed` — `ModuleNotFoundError: No module named 'tests'` |

Full-suite runs at `f355c58`, same worktree:

| run | result |
|---|---|
| `uv run python -m pytest tests/ -q -rf` | `1588 passed, 22 skipped in 29.05s` (exit 0) |
| `uv run ruff check .` | `All checks passed!` (exit 0) |
| `uv run pytest tests/ -q` (CI's form), **unmodified** | reproduces CI's single failure |

**Candidate one-token fix, measured (not pushed — #394 is not this routine's PR to commit to).**
`from tests.tools.test_messaging import ...` → `from tools.test_messaging import ...`. With
`tests/tools/__init__.py` present and no `tests/__init__.py`, pytest inserts `tests/` itself on
`sys.path`, so `tools.test_messaging` resolves under both invocations. Measured under CI's bare
`pytest`: the single test passes, and the **full suite is `1588 passed, 22 skipped in 25.52s`**.
The worktree was reverted afterwards (`git checkout --`, clean status). Adding `tests/__init__.py`
would also work but renames every test module across the suite, which is a larger blast radius than
this failure justifies. This is a recommendation for the author, not a change made here.

**Worth noting beyond the one failure:** `from tests.tools...` is the **only** cross-test package
import in the entire suite — grep over `tests/` finds exactly one hit, the new line. So this is a
new idiom this PR introduces, not an existing convention that broke. The 1,589-passed count in the
PR body is otherwise consistent with CI's 1,589; only the skip count differs (21 local vs 20 in CI),
which is the same shallow-clone-vs-full-clone `test_closing_keyword_guard.py` skip explained in the
first pass's correction 5.

### Other mechanical work

**Stalled / no-CI PRs (step 3a): still exactly one, still untouchable.** `get_check_runs` on
**#390** returns `total_count: 0` again — unchanged from the first pass. It is a fork PR
(`EastArctica`), so no update-branch and no retrigger commit from here; a maintainer must approve
the run in the Actions tab. Nothing else has zero checks.

**Local verification (step 3b): one PR tested this pass, three deliberately not re-tested.**
Tested: **#394** at `f355c58` — full output above. **Not re-tested: #387 (`8f63289`),
#388 (`d6eb37e`), #390 (`93b5c78`).** All three carry the exact head SHAs the first pass tested
six hours earlier (verified against the live PR list this pass), so re-running identical trees
would produce identical numbers and prove nothing new; the first pass's table stands. **#393**
itself is not separately tested — it is this pass's own triage PR, touches one Markdown file under
`internal/issue-triage/`, and carries its own CI (15 checks, all success on `668530e`, including
`claude-review`).

**#393's content verified against source, and it is correct.** The PR's premise is that the
README's manual install does not use `uv` at all, so the old step 3 sent a Windows reader to a
path that does not exist for them. Checked: `README.md:327-334` is `python3 -m venv .venv` +
`pip install -e .` — no `uv` anywhere in that section; `README.md:340` is `cp env.template .env`
(and `env.template` exists at the repo root); `README.md:387` sets
`"command": "/absolute/path/to/canvas-mcp/.venv/bin/canvas-mcp-server"`; the Windows equivalent is
the parenthetical at `README.md:512`,
`C:\Users\YourName\canvas-mcp\.venv\Scripts\canvas-mcp-server.exe`. `pyproject.toml:45-46` declares
the `canvas-mcp-server` console script, which is what produces `canvas-mcp-server.exe` on Windows.
Every element of the rewritten step 3 checks out against the repository.

### Corrections and additions

1. **One gap in the rewritten #391 step 3, worth a one-line edit before sending.** It says
   `python -m venv .venv` without stating a Python version. `pyproject.toml:11` requires
   `>=3.11`, and this project dropped 3.10 deliberately. A Windows reporter whose `python` is
   3.10 or older will get an install-time resolution error rather than a clear message. Adding
   "(Python 3.11 or newer)" to that bullet closes it. Not edited here: the draft reply is
   Vishal's to send, and changing its substance is a judgement call, not mechanical work. Note
   also that `python` vs `python3` is correct as written for Windows — `python3` is the POSIX form
   and frequently absent on Windows.

2. **No claim of a missed triage fire.** There is a brief for today and it is the file this
   section appends to. Nothing in this pass speaks to whether any routine fired on any date; only
   the routine's own run history could, and this environment cannot see it.

3. **Nothing in this pass touched FERPA-sensitive territory.** The one code area examined (#394's
   ConfirmationGuard) is write-confirmation state, not student data or anonymization; the
   examination was read-only plus a reverted one-line experiment in a scratch worktree.

4. **The first pass's corrections stand unchanged.** #387 and #388 are still `behind` `main`
   (still `mergeable_state` blocked/behind, head SHAs unmoved). The #390 null-`submission_types`
   robustness nit (first pass, correction 6) is still an unproven robustness observation, still
   not commented anywhere.

### Needs Vishal

1. **#394 is red and the PR body says it is green — this is the top item.** One test fails on all
   three Python versions and in the Security Test Suite, for the `sys.path` reason measured above,
   and it takes the required `test-enhancements` aggregator down with it. The candidate one-token
   change is measured green across the whole suite under CI's own invocation. **This routine cannot
   push it** — #394 is not a PR this routine created, so a non-empty commit to its branch is outside
   the boundary, and an empty retrigger commit would not help because the failure is deterministic,
   not a stalled run. It needs a commit from you (or from the session that opened it).
2. **Consider whether CI and the documented local command should agree.** The deeper issue is that
   `uv run python -m pytest` (what CLAUDE.md tells contributors to run) and bare `pytest` (what CI
   runs) resolve imports differently, so a contributor can measure green and land red. That is a
   change to `.github/workflows/` or to CLAUDE.md, both outside this routine's write boundary.
   Filing it as its own issue would keep it from recurring; this pass has not filed one.
3. **Approve the workflow run for #390 in the Actions tab** — carried over, unchanged, still the
   one action that unblocks a first-time outside contributor.
4. **Reply to chiptoe-svg on #375** — carried over from the first pass and the day before, still
   unsent, still the highest-value reply available. First pass's correction 4 has the hazard-to-
   safeguard mapping ready to use.
5. **Reply to #391** — the draft above is now copyable as-is, with the one caveat in correction 1
   of this section (state the Python 3.11+ requirement).
6. **Merge decision on #387 and #388** — carried over, unchanged; both need an update-branch first
   if branch protection requires up-to-date branches.
