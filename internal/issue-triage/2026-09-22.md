# Triage brief — 2026-09-22

**Read path used:** GitHub MCP tools throughout (`gh` not installed in this sandbox; MCP tools
covered every read needed, including job logs for the CI failure below).

**Cutoff:** 2026-09-20T02:23:42Z — the merge time of the previous brief's triage PR, **#383**
(same convention the last several briefs have used: the comparison instant is that PR's merge,
not the calendar date in its filename). Everything below newer than that timestamp counts as
"since cutoff."

Not a quiet day: two new dependency-bump PRs arrived from Dependabot after the cutoff, both
showing a red check, so the early-exit condition does not apply even though no issue needs a
fresh reply from you.

## Needs your reply

None. No open issue has a comment newer than cutoff from anyone other than you. (#384, the
weekly-maintenance issue, was opened *before* cutoff and has had no further activity — see
"Checked, nothing needed.")

## PRs

### #388 — chore(deps): bump the actions group with 7 updates (@dependabot[bot])
Groups 7 GitHub Actions bumps: `claude-code-action` 1.0.222→1.0.230, `docker/build-push-action`
7.3.0→7.4.0, `codeql-action/{init,autobuild,analyze,upload-sarif}` 4.38.0→4.38.1, and a
`trufflehog` SHA pin bump. Workflow-file-only change (8 files, +11/-11).

### #387 — chore(deps-dev): bump @types/node from 26.5.1 to 26.6.1 (@dependabot[bot])
DevDependency-only bump (2 files, +5/-5), no runtime code touched.

**Both PRs — same shape, checked individually:**
- `mergeable_state: blocked` on both. This is branch protection waiting on a review/approval, not
  a merge conflict — no conflict markers, no failing required check.
- All 16 real checks green on both (`test-enhancements`, `lint`, `test (3.11/3.12/3.13)`,
  `test-code-api`, CodeQL, Secret Detection, SAST, Dependency Vulnerability Scan, Security Test
  Suite, PR body/commits check, Cursor agents — neutral/informational).
- **`claude-review` fails on both**, and I checked why rather than assuming: the job log
  (`get_job_logs`, job 106215075045 for #387 / 106215808824 for #388) shows
  `##[error]CLAUDE_CODE_OAUTH_TOKEN secret is not set. Add it in repo Settings > Secrets.` —
  GitHub withholds repo secrets from Dependabot-authored PR runs, the same class of problem #188
  already documented for fork PRs (and the reason `claude-review` was dropped from required
  checks). This is not a finding about the dependency bumps themselves; it's an infra limitation
  that will recur on every future Dependabot PR and isn't actionable per-PR.
- Required checks (`test-enhancements`, `lint`) are green on both. No review has been requested or
  posted on either.

**Next action (yours):** both are green-but-unreviewed, low-risk version bumps (CI/tooling
metadata and a devDependency type-stub package — no application code changed). Normal review/merge
call; nothing here blocks on more investigation from this routine.

**Not a per-PR problem, but worth doing once:** if this repo has no Dependabot auto-merge rule for
green, low-risk updates (patch/minor CI and devDependency bumps), that would remove this manual
step going forward — your call whether that fits your risk tolerance for Actions-pin bumps.

## New since 2026-09-20T02:23:42Z

- **#388** and **#387** (@dependabot[bot]) — covered above under PRs. These are the only new
  issues or PRs since cutoff from anyone other than you. (#381, the Codex docs PR from the last
  brief, merged 2026-09-20T11:34:56Z — after cutoff for *that* brief's window, so it's closed and
  out of scope here, not new.)

## Going stale

- **#157** (sandbox egress hardening) — no activity since your 2026-09-08 triage decision comment;
  last non-owner touch was a `claude[bot]` label note back on 2026-07-04.
- **#172** (Canvas Quizzes, zqian) — no activity since your 2026-09-08 note; last non-owner comment
  was `jonespm` on 2026-08-04. Still blocked on a New-Quizzes-enabled sandbox.
- **#236** (OAuth2 dev-key flow) — no activity since your 2026-09-08 note; last non-owner touch was
  a `claude[bot]` label note on 2026-08-07. Blocked on an admin-access pilot.
- **#301** (assignment-group skill request, rpsimon-ai) — no reporter reply since 2026-08-18; you
  asked for department templates/policies on 2026-08-19. Still waiting on them.
- **#302** (student-risk analysis skill, rpsimon-ai) — same reporter/pattern; you asked two scoping
  questions on 2026-08-21. Still waiting on them.
- **#374** (`use_for_grading` grading gap, chiptoe-svg) — last non-you touch is still the original
  report (2026-09-13), now 9 days old. Substantively parked, not neglected: your 2026-09-14
  comments closed the loop on the PR #378 fix and asked the reporter to retest main and report
  back Canvas version/feature flags.
- **#375** (rubric-update design question, chiptoe-svg) — last non-you touch is chiptoe-svg's
  2026-09-14T11:29:15Z comment (root-cause analysis of Canvas's update-replaces-criteria and
  PUT-can-create-new-rubric hazards), now 8 days old, so this mechanically clears the 5-day
  threshold. Your 2026-09-19 reply already answered it in full (see "Checked, nothing needed") —
  nothing is actually pending, it's just old by the clock.

## Checked, nothing needed

⚠️ **#375** (high-sensitivity — rubric-update design question) — read the full thread. Your
2026-09-19T17:18:26Z comment is the latest activity and is a complete response: it corrects an
earlier brief's mischaracterization, sets concrete acceptance criteria for a future `update_rubric`
tool (preserve all criterion/rating IDs, explicitly identify the association being changed, verify
returned rubric identity/association post-write, treat a different returned ID as an unconfirmed
outcome rather than success, no auto-retry, preflight/confirmation since read-back can detect but
not undo a bad write), and is explicit that PR #378 covered creation docs and grading safeguards
only, not update semantics. Nothing pending on your side; issue stays open by design.

**#384** (weekly maintenance report, opened by `claude[bot]` 2026-09-20T02:08:02Z, before cutoff) —
no activity since. Still unlabeled — this is the known `auto-label-issues.yml` bug from the last
brief (the action's OIDC-to-app-token exchange rejects both outside-reporter and bot-authored
triggers). Not re-investigated today since nothing changed; flagged again only so it doesn't get
lost. This routine cannot fix `.github/workflows/auto-label-issues.yml` (outside
`internal/issue-triage/`).

**#157, #172, #236, #301, #302, #374** — reviewed via issue reads/comments, unchanged in substance
since the last brief; no action beyond what's noted in Going Stale above.

---

Grepped this file and the PR body below for
`fix|fixes|fixed|close|closes|closed|resolve|resolves|resolved` immediately preceding an issue
reference: no hits (all issue references above are bare `#NNN` mentions in prose, not adjacent to
a closing verb).

---

## Executor results (2026-09-22T02:20:41Z)

**Closing-keyword check — clean.** Ran `scripts/check_closing_keywords.py --strict` over this brief
file (exit 0) and over PR #389's body (exit 0), plus a negative control — a
synthetic throwaway line pairing a closing keyword with a fake issue number, which the detector
rejected with exit 1 — to prove it was actually firing and not silently no-opping. Nothing to reword.

### Mechanical work done

**Stalled / no-CI PRs (step 3a): none — no action taken, and none was warranted.**
Both open PRs already have complete CI on their head commits, and both are *already up to date with
`main`* (`git merge-base --is-ancestor origin/main <head>` returns true for `pr387` @ `8f63289` and
`pr388` @ `d6eb37e`), so there was nothing to update-branch and no reason to push a retrigger commit.
Both are Dependabot branches on this repo (not forks), so an empty retrigger commit would have been
permitted — it just wasn't needed.

**Local verification of green-but-unreviewed PRs (step 3b): both tested, real output below.**
Checked out each PR head read-only in a separate `git worktree` under the scratchpad, synced the
`dev` dependency group with `uv sync --group dev`, and ran the full suite plus lint. (Note: the
repo's dev deps live in `[dependency-groups]`, not `[project.optional-dependencies]` — `uv sync
--extra dev` fails with `Extra 'dev' is not defined`. `--group dev` is the working invocation.)

| PR | head | `uv run python -m pytest tests/ -q -rf` | `uv run ruff check .` |
|---|---|---|---|
| #387 (`@types/node` 26.5.1→26.6.1) | `8f63289` | `1580 passed, 22 skipped in 32.58s` | `All checks passed!` |
| #388 (actions group, 7 updates) | `d6eb37e` | `1580 passed, 22 skipped in 31.36s` | `All checks passed!` |

No failures, so `-rf` printed no failure lines on either run. **PRs not tested: none** — those two
are the only open PRs other than this triage PR, which CI covers on its own head.

Worth knowing about what those local runs do *not* prove: neither PR changes Python code (#387 is
`package.json` + `package-lock.json` only; #388 is eight `.github/workflows/*.yml` files, +11/-11,
SHA pins only). A green Python suite is therefore the expected result on both and is weak evidence
about either change. The meaningful signal for #388 is its own CI, which is discussed below.

### Corrections and additions to the brief

1. **#375 is materially out of date in this brief — the "future `update_rubric` tool" has shipped.**
   The brief files #375 under "Going stale" and "Checked, nothing needed," describing your
   2026-09-19 comment as setting acceptance criteria "for a future `update_rubric` tool." But
   **PR #386 (`feat: add guarded rubric updates`) merged to `main` as `e5ff3b1` on
   2026-09-20T17:47:29Z** — after this brief's cutoff — and `update_rubric` now exists at
   `src/canvas_mcp/tools/rubrics.py:1648`. The merge adds 584 lines to `rubrics.py` and 710 lines
   of tests, and its message names exactly the safeguards you listed on the issue (ID-preserving
   replacements, preview/confirmation, read-back verification, rubric-association IDs exposed
   through `get_rubric`). The brief's own filter ("only new PRs from anyone other than you")
   mechanically excludes #386 because you authored it, which is how the miss happened — but the
   effect is that #375's status note says "nothing pending" when in fact the capability the
   reporter was told was future now exists on `main` and **chiptoe-svg has not been told**. See
   "Needs Vishal" below. (#385 and #381 also merged in this window, same author filter; neither
   changes an open issue's status the way #386 does.)

2. **#384 is not unlabeled.** The brief says "Still unlabeled — this is the known
   `auto-label-issues.yml` bug." A live read of the issue returns `"labels": ["maintenance"]`. The
   label came from the creating workflow, not the auto-labeler: `.github/workflows/weekly-maintenance.yml:94`
   instructs the run to "create the issue with label `maintenance`," and the issue's `updated_at`
   is identical to its `created_at` (`2026-09-20T02:08:02Z`), i.e. nothing touched it after
   creation. So the underlying concern may still be real — the auto-labeler added **no** topical
   labels on top — but the stated fact is wrong, and "unlabeled" would be the wrong thing to
   repeat in a future brief. Whether `auto-label-issues.yml` actually ran and failed on this issue
   is **not verified from here**; only that it contributed nothing.

3. **The `claude-review` diagnosis is confirmed, by source and not just by log quote.**
   `.github/workflows/claude-code-review.yml:28-33` has an explicit pre-flight step that emits
   `::error::CLAUDE_CODE_OAUTH_TOKEN secret is not set. Add it in repo Settings > Secrets.` and
   exits 1 when the secret is empty. The failing job on #388 ran 04:38:19→04:38:27Z (8 seconds),
   consistent with failing at that guard before doing any review work. The brief's read is right.

4. **New, and the one thing in #388 that isn't purely mechanical:** the bumped
   `anthropics/claude-code-action` pin (`56cf60f` → `4036a180`) is **not exercised by #388's own
   CI**. The four workflows that use it are `claude-code-review.yml`, `claude.yml`,
   `auto-label-issues.yml` and `weekly-maintenance.yml` — the first dies at the secret guard on
   Dependabot PRs, and the other three don't trigger on `pull_request` at all. The other pins in
   the group *were* exercised (Enhancement Testing, Security Testing and Closing Keyword Guard all
   passed on `d6eb37e`). So the new `claude-code-action` SHA first executes on the next
   non-Dependabot PR after merge. Not a reason to hold the PR — just the reason the green checkmark
   covers less than it looks like it does.

5. **Minor, also in #388:** the trufflehog pin moves `4b7d1d3` → `288a8a8` but Dependabot left the
   trailing comment reading `# main@2026-08-10` on both the old and new lines. That date now
   describes the wrong commit. Cosmetic, but it's the comment a future reader would trust to know
   how old the pin is.

6. **Verified and unchanged:** #375's last comment really is yours at 2026-09-19T17:18:26Z (read
   the thread; chiptoe-svg's 2026-09-14T11:29:15Z source analysis precedes it). Both Dependabot
   diffs match the brief's description exactly (#387: 2 files, +5/-5; #388: 8 files, +11/-11).

7. **No unmeasured-behavior claims to flag.** This brief contains no draft replies, and it is
   careful to mark the Canvas-behavior items on #374/#375 as source-reported rather than
   live-verified. Nothing here asserts live Canvas behavior as confirmed.

### Needs Vishal

1. **Tell chiptoe-svg on #375 that `update_rubric` shipped.** PR #386 merged to `main` as `e5ff3b1`
   on 2026-09-20 and implements the acceptance criteria you spelled out on the issue five days
   earlier. They supplied the Canvas-source hazard analysis that shaped those criteria and have had
   no reply since. This is the highest-value open item in today's pass, and it is a public reply, so
   it is yours to post. It also bears on whether #375 stays open: the docs half is done and the
   update capability now exists, but the hazards remain source-reported, never verified against a
   live instance — so "close it" is a judgement call, not a mechanical one.
2. **Merge decision on #387 and #388.** Both green on every real check, both up to date with `main`,
   both clean locally (table above). #387 is a devDependency type-stub bump with no runtime reach.
   #388 is workflow SHA pins only — read point 4 before merging, since the `claude-code-action` pin
   is the one piece of it CI could not test. Approving/merging either is outside this routine's
   boundary.
3. **Optional, once:** a Dependabot auto-merge rule for green patch/minor CI and devDependency
   bumps would remove this recurring manual step, as the brief suggests. The counter-argument is
   point 4 — Actions SHA bumps are exactly the class where the green check is thinnest.
4. **Not fixable from here:** the `claude-review` failure will recur on every future Dependabot PR
   (GitHub withholds secrets from bot-authored runs). If the noise is not worth it, the fix is a
   condition on the job in `.github/workflows/claude-code-review.yml` — a file outside
   `internal/issue-triage/`, so this routine cannot touch it.
