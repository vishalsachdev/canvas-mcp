# Triage brief — 2026-08-30

**Read path used:** GitHub MCP tools (`mcp__github__*`); `gh` CLI not installed in this sandbox.
**Cutoff:** 2026-08-29, using `internal/issue-triage/2026-08-29.md` (the newest prior brief) as the
boundary file. Its own triage PR (#327) merged to `main` at `2026-08-29T02:23:12Z` — using that
merge timestamp as the effective "new since cutoff" boundary, consistent with the last several
briefs' convention.

**Heads up before the detail below:** a large amount of work landed between 2026-08-30T01:03Z and
01:36Z — three PRs opened and merged (#328, #329, #331), one PR opened and still open (#330), one
issue closed (#315), and substantive comments posted on #325 and #172. All of it was done under
`vishalsachdev`'s own account (author == committer == commenter on every item), evidently a
separate work session, not this triage routine and not an external contributor. So none of it
trips category 1 ("comment from someone other than you") or category 3 ("opened by someone other
than you") — but two things from that session are still open decisions worth putting in front of
you, which is why this brief exists instead of an early exit.

## Needs your reply

None. No open issue has a comment newer than the cutoff from anyone other than you.

## PRs

- ⚠️ **#317** — `fix(security): run execute_typescript sandbox as non-root user` (AmirF194, fork
  PR, security-sensitive, tracks issue #157). **Materially changed since the last brief: CI ran for
  the first time.** The Actions-approval block that stalled this for days is gone — `test-enhancements`,
  `lint`, the full 3.10–3.13 matrix, and the security-scan jobs are all green on head `b28ae4b`.
  Two jobs are red:
  - `claude-review` — **failure, but expected.** `CLAUDE.md`'s #188 entry already documents this:
    `claude-review` was dropped from required checks because it can never pass on a fork PR
    (GitHub withholds repo secrets from fork Actions runs). Not actionable.
  - `CodeQL` — **failure, and this is real.** Two high-severity `py/overly-permissive-file` alerts
    (145, 146) on the two `os.chmod(..., 0o644)` calls this PR adds. You already reviewed this
    yourself in a same-day PR comment (2026-08-30T01:06Z) and did the legwork: the `.cjs`
    network-guard file only holds an allowlist, so 0644 there is free and you're fine dismissing
    that alert. The `.ts` script is the real question — it can carry student IDs, grades, or
    comment text from a bulk-grading run, and `code_api/` is traversable on a multi-user host, so
    0644 makes it host-locally readable while a run is in flight. You laid out three options in
    the thread: (a) keep 0644 + document the caveat + dismiss the alert (simplest, only option
    that also works under rootless podman); (b) pipe the script over stdin instead of writing it
    into the bind mount at all (no host-side copy, needs a Linux end-to-end test); (c) `--user
    65532:` + 0640 (narrower, but breaks rootless podman). **This decision is still open** — the
    comment ends "Once we pick (a), (b), or (c) I will update the branch against main and merge."
  - `mergeable_state` is `behind` (main has moved since the branch was last synced) — needs a
    maintainer `update-branch`; this triage routine has no write access to a fork branch.
  - **Reminder carried forward:** commit `277ef8e`'s first line ends with a line-opening `Fixes`
    trailer referencing issue 157. A merge commit or rebase carries it to `main` verbatim; a
    squash concatenates both commit messages by default, so it comes along there too unless the
    squash message is edited down to the PR body (which uses `Refs` and is clean). Strip the
    trailer from the merge message or reopen issue 157 right after — same mechanism that retired
    issue 172 twice.

  **Next action:** pick (a)/(b)/(c) for the CodeQL finding, `update-branch`, then merge.

- ⚠️ **#330** — `feat(delete): confirmation token on every delete tool; add
  delete_assignment_with_confirmation` (your own PR, `feature/318-delete-confirmation`, addresses
  #318, write-tool/FERPA-adjacent). Opened and fully CI-green same session (01:15–01:36Z). Three
  `claude[bot]` reviews plus one Codex review, all independently conclude nothing blocks merging,
  but all three `claude[bot]` passes converge on the same two points:
  1. **`CHANGELOG.md` isn't touched**, despite the PR body itself calling this a breaking change
     needing a minor bump (six signatures changed, `delete_announcement` removed,
     `delete_assignment_with_confirmation` added). The repo's own `[Unreleased]` section is
     actively used for exactly this (issue-309's entry is already sitting there) — this looks like
     a genuine gap against your own convention, not a nit.
  2. **No `guard.release()` on a post-confirm Canvas failure.** `messaging.py`'s send tools release
     the token back to the caller on a provable rejection so a legitimate retry doesn't need a
     fresh preview; none of the five single-target delete tools do this — a 403 after a successful
     token redemption burns the token even though nothing was deleted. One reviewer notes
     `content_migrations.py`'s existing `ConfirmationGuard` usage doesn't release either, so this
     is consistent with the majority pattern, not a new inconsistency — but still worth a
     deliberate yes/no rather than silence, per the reviewers' framing.
  `mergeable_state` is `blocked` despite all-green CI — reads as branch protection waiting on a
  human review approval (three bot comments aren't a review approval).

  **Next action:** add the `CHANGELOG.md` entry, decide the `release()` question one way or the
  other, get a human approval, then merge.

## New since 2026-08-29

None by an external party. (#328, #329, #330, #331 were all opened by you; see the heads-up above.)

## Going stale

- **#309** (zqian, content migration) — filed 2026-08-19, your v1 design + scoping questions went
  out 2026-08-21; still no response (9 days since your reply).
- **#301** (rpsimon-ai, assignment-group skill request) — filed 2026-08-18, you replied 2026-08-19;
  no response since (11 days).
- **#302** (rpsimon-ai, student-analytics skill request) — filed 2026-08-18, you replied 2026-08-21;
  no response since (9 days). **Worth a short follow-up now:** the Skill Request issue template you
  mentioned adding shipped this session in PR #328 (merged 2026-08-30T01:16Z), but neither #301 nor
  #302 has a comment pointing that out — the loop is open on both threads even though the work is
  done.

  ```
  Draft reply (#302, and equally applicable to #301):

  Following up — the Skill Request issue template mentioned above is live now
  (.github/ISSUE_TEMPLATE/skill_request.yml), so future requests like this one can
  skip the developer-facing fields that didn't fit here. Doesn't change anything
  about your existing request — still on my list, no new timeline yet — just
  closing the loop on the template.
  ```
- **#299** (turner-moore, cookie-auth feature) — filed 2026-08-17, you replied 2026-08-19; no
  response since (11 days).
- **#142** (watch item, `mcp<2` pin) — no activity since 2026-07-30 (31 days). Still blocked on an
  upstream fastmcp release; no action available.
- **#236, #157** — no external activity in weeks; both self-filed. #157 has real movement this
  cycle via PR #317 (see PRs section above) even though the issue thread itself is quiet.

## Checked, nothing needed

- **#325** — jonespm's accessibility-checker request. Fixed and live on `main` via PR #329; you
  already posted a full explanatory reply on the issue (2026-08-30T01:19Z) covering the new
  `ACCESSIBILITY_CHECKERS` setting and correcting one point in the original filing. Nothing further
  to add.
- **#172** — zqian/jonespm, New Quizzes. You already posted a status-update comment
  (2026-08-30T01:18Z) explaining that PR #191 was closed unmerged on 2026-08-26 and why, using
  "closed unmerged" phrasing rather than a closing keyword. Nothing further needed from this brief.
- **#315** — self-filed Python-3.10-drop tracking issue, closed by you (shipped in PR #331,
  `state_reason: completed`). No external party involved.
- **#318** — zqian's delete-confirmation-token request. Addressed by open PR #330 above; no new
  comment on the issue itself yet, and none needed until the PR merges.
- **#328, #329, #331** — same-repo maintainer PRs, all merged, no external involvement, nothing for
  this routine to do.

---

Nothing outside `internal/issue-triage/` was modified in this run. No public comment, label, close,
approval, or non-triage merge was performed.
