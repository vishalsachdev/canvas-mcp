# Triage brief — 2026-09-20

**Read path used:** GitHub MCP tools throughout (`gh` not checked in this sandbox — MCP tools
worked for every read needed).

**Cutoff:** 2026-09-15T01:45:55Z — the merge time of the previous brief's triage PR, **#379**
(following the convention the last several briefs have used: the actual comparison instant is
that PR's merge, not just the calendar date on the filename). Everything below newer than that
timestamp counts as "since cutoff."

Not a quiet day: a new PR arrived from a first-time external contributor, and it has an
unresolved-CI problem (see PRs below), so the early-exit condition does not apply even though no
issue needs a fresh reply from you right now.

## Needs your reply

None. #375 has a comment newer than cutoff (2026-09-19T17:18:26Z), but the most recent commenter
is you, not the reporter — see "Checked, nothing needed" below for what that comment covers.

## PRs

### #381 — docs: add Codex MCP setup instructions (@Mighty303, first-time contributor)
Docs-only change (1 file, README.md, +24/-1): adds Codex to the supported-client list and a new
`<details>` block documenting both `codex mcp add` and a `~/.codex/config.toml` entry, including a
`cwd` setting so the server finds the repo's `.env` when Codex is launched from elsewhere.

**State:** open, not draft, `mergeable_state: blocked`. Zero CI checks have reported
(`get_check_runs` returns 0; commit status is `pending` with no statuses). Per the fork-CI
convention: this is a first-time contributor (`author_association: NONE`) on a fork PR, so zero
checks means the four workflow runs are sitting at `action_required` awaiting your approval to
run — not a CI failure.

**Review history:** you left a `COMMENTED` review on 2026-09-19T17:18Z (not approve, not request
changes) asking the contributor to lead the Codex example with the `config.toml` form (since the
CLI command alone doesn't set a persistent `cwd`), to add `cwd` to the existing `canvas-api` table
rather than create a duplicate, and to verify startup from outside the repo with only the repo
`.env` supplying credentials. Your review also noted the four workflow runs were `action_required`.

**@Mighty303 replied 2026-09-19T21:07Z** (the most recent activity on this PR) saying they
addressed all three points and verified by running `codex mcp list` from `/private/tmp` (confirmed
registration) and completing an MCP handshake with no inherited `CANVAS_*` env vars, confirming the
server loaded the repo `.env` and exposed 97 tools. `git diff --check` passes. I read the current
diff and it matches this description — the `config.toml` block is now shown first, the CLI form is
kept as an alternative with an explicit "don't create a duplicate table" instruction, and the
verification steps they describe are exactly what your review asked for.

**Next action (yours):** this is a docs-only, low-risk change from someone who did the requested
follow-up work and verification legwork. Two things only you can do: (1) approve the pending
workflow runs (Actions tab) so CI actually executes on the current commit, and (2) re-review the
updated diff — it reads to me as ready to approve/merge if CI comes back green, but the merge
decision is yours.

No other open PRs.

## New since 2026-09-15T01:45:55Z

- **#381** (@Mighty303) — covered above under PRs; this is the only new issue or PR since cutoff
  from anyone other than you.

## Going stale

- **#157** (sandbox egress hardening) — no activity since cutoff; last non-owner touch was a
  `claude[bot]` label note back on 2026-07-04.
- **#172** (Canvas Quizzes, zqian) — no activity since cutoff; last non-owner comment was
  `jonespm` on 2026-08-04. Still blocked on a New-Quizzes-enabled sandbox per the standing note.
- **#236** (OAuth2 dev-key flow) — no activity since cutoff; last non-owner touch was a
  `claude[bot]` label note on 2026-08-07. Blocked on an admin-access pilot.
- **#301** (assignment-group skill request, rpsimon-ai) — no reporter reply since 2026-08-18; you
  asked for department templates/policies on 2026-08-19. Still waiting on them.
- **#302** (student-risk analysis skill, rpsimon-ai) — same reporter/pattern; you asked two scoping
  questions on 2026-08-21. Still waiting on them.
- **#374** (`use_for_grading` grading gap, chiptoe-svg) — no comments since cutoff, and the last
  non-you touch is the original report itself (2026-09-13), now more than 5 days old, so this
  mechanically clears the staleness threshold. In substance it's parked, not neglected: your two
  2026-09-14 comments already closed the loop on the PR #378 fix and explicitly asked the reporter
  to retest main and report back Canvas version/feature flags — nothing is currently on you here.

## Checked, nothing needed

⚠️ **#375** (high-sensitivity — rubric-update design question) — reviewed the full thread. Your
2026-09-19T17:18Z comment is the latest activity: it corrects a characterization from an earlier
brief (you note that brief wrongly treated the thread as "only waiting for a live retest," when
chiptoe-svg had already supplied the update-hazard research and explicitly said no live example
exists), sets acceptance criteria for a future `update_rubric` tool (preserve all criterion/rating
IDs, identify the association being changed explicitly, verify returned rubric identity/association
post-write, treat a different returned ID as an unconfirmed outcome rather than success, no
auto-retry, preflight/confirmation since read-back can detect but not undo a bad write), and is
explicit that PR #378 covered creation docs and grading safeguards only, not update semantics. This
reads as a complete, source-caveated update from you — nothing pending on your side, and the issue
stays open by design pending future `update_rubric` work. No new draft reply needed.

**#157, #172, #236, #301, #302, #374** — reviewed, unchanged in substance since the last brief, no
action beyond what's noted in Going Stale above.

---

Grepped this file and the PR body below for `fix|fixes|fixed|close|closes|closed|resolve|resolves|
resolved` immediately preceding an issue reference: no hits (all issue references above are bare
`#NNN` mentions in prose, not adjacent to a closing verb).
