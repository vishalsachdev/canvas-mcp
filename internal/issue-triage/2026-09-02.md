# Triage brief — 2026-09-02

**Read path used:** GitHub MCP tools (`mcp__github__*`). `gh` CLI not checked/not installed in
this sandbox (expected per routine instructions); MCP access worked on the first call.

**Cutoff:** 2026-09-01T02:31:24Z — the merge timestamp of PR #346 (the 2026-09-01 triage brief),
continuing the "previous triage PR's merge time" convention the last several briefs have used
(more precise than the raw `2026-09-01.md` file-write timestamp, which is generation time, not
merge time).

## Needs your reply

None. Only one open issue has any comment since the cutoff — **#142** (FastMCP 4 / MCP SDK 2
migration), where you posted the newest comment yourself at 2026-09-01T13:11:09Z (upstream
FastMCP 4.0.0 went stable; you retitled the issue back to startable work, relabeled it, and
closed Dependabot PR #344 rather than merging its isolated `mcp<3` widening). Since the most
recent commenter is `vishalsachdev`, this doesn't qualify as "waiting on you" — it's the opposite,
you already acted. Noting it here only because it resolves an open question from yesterday's
brief (see PRs section).

No new issues or PRs opened since the cutoff by anyone.

## PRs

**#340 — `fix(security): make sandbox uid:gid configurable via TS_SANDBOX_UID_GID (#338)`**
(k0shir0, fork PR, not draft). No new comments, reviews, or commits since the cutoff — this is
carried forward, not new:

- Still **`total_count: 0` check runs**. GitHub is still withholding the Actions run on this fork
  branch (`action_required`), same as the last two briefs. This is the same approval ask as
  yesterday and the day before — still outstanding, still needs "Actions tab → the run → Approve
  and run workflows."
- AmirF194's review thread (root-uid-0 finding, `config.py:449`) is still unresolved — no reply
  posted yet.
- **`mergeable_state` is now `behind`** (was "0 commits behind" in the 2026-09-01 brief's local
  check). This isn't new PR activity — it's `main` moving past the branch: #342, #343, and #345
  (the three clean Dependabot bumps flagged as "ready to merge" yesterday) merged, plus two
  unrelated docs commits (`587d512`, `43497f9`). The branch will need a rebase/merge from `main`
  before it's mergeable, on top of the still-open CI-approval and root-uid items.
- **Carrying forward, not re-verified today:** yesterday's brief measured that k0shir0's root-uid
  fix (`f8cba18`) rejects the literal `0:0` but not `00:00` / `00:65532` (string comparison vs. a
  format regex that allows leading zeros) — `config.py` around line 449. That finding stands
  unless a new commit has addressed it; there's been no new commit on this branch since
  2026-08-31T23:17:48Z, so it's presumably still live. Treat "approve CI, watch it go green, then
  merge" as **not** sufficient on its own until that's either fixed or explicitly accepted as
  out of scope.

**Next action:** same as yesterday — approve the withheld workflow run so CI can run at all. Once
you do, the branch will still need a `main` merge (now "behind") before it can land, and the
`00:00`-shaped gap is worth a one-line reply to k0shir0/AmirF194 before merging on a green run
alone:

```
Thanks both — AmirF194's repro was the clincher and k0shir0's fix (f8cba18) closes the literal
0:0 case with a test. One more gap before I merge: the fix compares strings, so 00:00 or 00:65532
(leading zeros) still pass the ^\d+:\d+$ format check and skip the uid==0 check. Could you switch
to a numeric comparison (int(uid) == 0 or int(gid) == 0) and add a test for the leading-zero case?
Approving the workflow run now so CI runs either way.
```

**#344 (Dependabot, `mcp<2` → `<3`)** — closed by you directly on 2026-09-01, not merged. This
resolves the "regenerate `uv.lock` before merging" flag from yesterday's brief; no action needed,
noted only for continuity.

**#342, #343, #345** — all merged since the last brief, as recommended. No outstanding PRs from
this batch.

## New since cutoff

Nothing — no issues, no PRs opened by anyone since 2026-09-01T02:31:24Z (other than the previous
triage PR #346 itself, which is out of scope).

## Going stale

Same standing list as the last several briefs, no change (no activity on any of these since the
cutoff): **#172** (last non-Vishal touch: jonespm, 2026-08-04 — blocked on a New-Quizzes sandbox),
**#309 / #301 / #302 / #299** (reporters' last touch 11–15+ days ago, no answer yet to your
scoping questions), **#318 / #325** (fixes already shipped in v1.12.0 — just waiting on reporter
confirmation).

## Checked, nothing needed

#142 (your own issue, actively worked by you today — see above), #338 (your own issue; tracked by
PR #340, covered above), #335 (automated weekly maintenance report, out of scope per instructions
— `weekly-maintenance.yml`), #157 / #236 (your own issues, no external activity since cutoff),
#340 (covered in PRs — no *new* activity since cutoff, carried forward because the fork-approval
block is still outstanding).

---

## Executor results (2026-09-02T02:19Z)

**Closing-keyword check:** `scripts/check_closing_keywords.py --strict` run over both the brief
file and PR #347's body — exit 0 on both. Nothing to reword.

### What I did

**Retriggers / branch updates: none possible.** The only open non-triage PR is **#340**, whose head
is on the **fork** `k0shir0/canvas-mcp`. Both permitted unblocks (update-branch, empty retrigger
commit) would push to a fork branch, which the executor boundary forbids outright. So the
`behind`-main state and the withheld `action_required` run both stay as the brief describes —
neither is something this routine can clear. Re-confirmed live: `get_status` on #340 returns
`total_count: 0`, `state: pending`.

**Local test run on #340 — the CI that GitHub is withholding.** I fetched the fork head read-only
into a throwaway worktree (no remote writes, no push, worktree removed afterwards) and ran the
suite that the blocked workflow would have run. Head SHA `f8cba185b8e5ce5ddfdc22e93c616c42870d8d20`
— matches what GitHub reports, so the brief's "no new commits since 2026-08-31T23:17:48Z" holds.

```
# at f8cba18 (PR #340 head, as-is)
1460 passed, 22 skipped, 2 warnings in 27.34s
uv run ruff check .   -> All checks passed!
uv run mypy src/canvas_mcp -> Success: no issues found in 50 source files
```

Because the branch is now `behind`, I also did a local trial merge of `origin/main` (`43497f9`)
into that head and re-ran everything, to answer "will the rebase this PR needs break anything":

```
# f8cba18 + origin/main merged locally (trial only, never pushed)
Automatic merge went well  — 0 conflicted files
1460 passed, 22 skipped, 2 warnings in 25.12s
uv run ruff check .   -> All checks passed!
```

So the `behind` state is mechanical only: the merge is conflict-free and green. The withheld
workflow is very likely to pass on approval — but that is a local result on a Linux runner without
Docker, not the real CI run, and it does **not** speak to the substantive finding below.

**PRs I did NOT test, and why:** none skipped — #340 is the only open non-triage PR, and it was
tested both as-is and trial-merged. #347 (this brief) is the triage PR itself.

### Corrections and verifications to the brief

Everything factual in the brief checked out. Four items were upgraded from asserted to measured,
and one draft-reply caveat is worth recording:

1. **The `00:00` leading-zero gap is now measured, not carried forward.** The brief flagged this as
   "presumably still live" from yesterday. I ran `validate_config()` directly against the PR head
   for five inputs:

   | `TS_SANDBOX_UID_GID` | effective after `validate_config()` |
   |---|---|
   | `0:0` | `65532:65532` (rejected — warning logged) |
   | `0:65532` | `65532:65532` (rejected — warning logged) |
   | `00:00` | **`00:00` (passes through unchanged)** |
   | `00:65532` | **`00:65532` (passes through unchanged)** |
   | `065532:65532` | `065532:65532` (passes through — benign, non-root) |

   Confirmed at `src/canvas_mcp/core/config.py:456-457` on the PR head: `uid, gid =
   config.ts_sandbox_uid_gid.split(":")` then `if uid == "0" or gid == "0"`. String comparison, so
   any leading-zero spelling of root skips the guard while still satisfying `^\d+:\d+$`. The
   brief's draft reply is accurate on this point and its suggested fix (`int(uid) == 0 or
   int(gid) == 0`) covers every row above.

2. **Caveat on that draft reply — one link in the chain is unmeasured here.** What I measured is
   the *config layer*: `00:00` survives validation and reaches the `--user` argv. What I could
   **not** measure is the container layer — whether `docker run --user 00:00` actually lands as
   uid 0. Docker is installed in this sandbox but the daemon is unavailable (`docker info` fails),
   so there is no way to run the container end-to-end from here. The root-as-consequence step is
   inference from how `--user` parses numeric ids, not a measurement. Phrase the reply accordingly
   if you send it, or have k0shir0/AmirF194 confirm on a real Docker host — AmirF194 already ran
   exactly this shape of container repro for the original `0:0` finding.

3. **A cosmetic nit in the same block, not worth blocking on:** the warning text hard-codes the
   string `TS_SANDBOX_UID_GID=0:0` even when the offending value was `0:65532`. The `(got '...')`
   suffix does show the real value, so nothing is misreported, but the two halves of the sentence
   disagree. Worth folding into the same commit if a fix round happens anyway.

4. **Cutoff, PR states, and the stale list all verified.** Cutoff `2026-09-01T02:31:24Z` is an
   exact match for PR #346's merge timestamp. #344 is closed with no merge commit (`merged_at`
   absent) — the brief's "closed by you directly, not merged" is right. #342 / #343 / #345 all
   carry merge timestamps of 2026-09-01T13:14–13:20Z. #142's newest comment is `vishalsachdev`
   at 2026-09-01T13:11:09Z, so "not waiting on you" is correct. Open-issue enumeration returns 12,
   and all 12 are accounted for across the brief's stale list (#172, #309, #301, #302, #299, #318,
   #325) and its checked-nothing-needed list (#142, #157, #236, #335, #338, #340) — no issue was
   dropped. No issue or PR was opened by anyone after the cutoff other than #347 itself.

5. **State change the brief records correctly but understates:** #142 stopped being a watch item on
   2026-09-01. Per your own comment, FastMCP 4.0.0 is stable on MCP SDK 2.x, `blocked-upstream` is
   off, and this is now startable migration work. It is the only open issue whose *status class*
   changed this cycle.

### Needs Vishal

Everything below is outside the executor boundary — public writes, fork branches, or merge
decisions on a PR that is not the triage brief.

1. **Approve the withheld workflow run on #340** (Actions tab → the run → "Approve and run
   workflows"). Third brief in a row carrying this ask. Nothing about this PR can move until CI is
   allowed to run, and no automation in this repo can clear it.
2. **Reply to k0shir0 / AmirF194 on #340 before merging on a green run.** The brief's draft reply
   is sound; use item 2 above to soften the container-layer claim from measured to expected. The
   review thread (`PRRT_kwDOOAwVQc6doTyC`, AmirF194 on `config.py:449`) is still unresolved.
3. **#340 needs a `main` merge before it can land** — conflict-free and green when I trialled it
   locally, but the push has to come from k0shir0 or from you, not from this routine.
4. **Optional, low cost:** the "good first issue" labelling recommendation for #301 and #302 from
   the weekly report (#335) is still unactioned. Labelling is a public write, so it stays here.

No public writes were made by this executor — no comments, labels, approvals, closes, or merges on
any issue or PR other than #347, and no pushes to any branch besides `triage/2026-09-02`. The
only remote read against the fork was a `git fetch`.
