# Triage brief — 2026-08-29

**Read path used:** GitHub MCP tools (`mcp__github__*`); `gh` CLI not installed in this sandbox.
**Cutoff:** 2026-08-27 (`internal/issue-triage/2026-08-27.md` is the newest prior brief). That
brief's own triage PR (#326) merged to `main` at `2026-08-27T02:21:27Z` — using that merge as the
effective boundary for "new since cutoff" and "comment newer than cutoff," consistent with the
convention the last few briefs have used. (There is no `2026-08-28.md`; nothing here explains why —
possibly a quiet-day early exit that produced no file, possibly the routine didn't fire. Not
investigated further, since it's outside this brief's scope.)

`list_issues --since 2026-08-27T02:21:27Z` on all 11 open issues returns **zero** results — no
issue has been updated (comment, edit, or otherwise) since the last brief merged. No new issues or
PRs were opened since then either (`list_issues --orderBy CREATED_AT` newest is still #325 from
2026-08-26; `list_pull_requests` newest closed/merged is still #326 itself).

So categories 1 ("needs your reply") and 3 ("new since cutoff") are both empty. Category 2 is
**not** empty: PR #317 is still open with the same unresolved problem flagged in the last two
briefs — a first-time contributor's pushed fix has never had its Actions run approved, so CI still
hasn't reported. That keeps this out of the Step 4 early exit, but there's genuinely nothing new to
say about it beyond "still waiting on you" — so this brief is short.

## Needs your reply

None. No open issue has a comment newer than the cutoff from anyone other than you.

## PRs

- **#317** — `fix(security): run execute_typescript sandbox as non-root user` (AmirF194, fork PR,
  security-sensitive, tracks issue #157). **Unchanged since the 2026-08-27 brief.** Head is still
  `b28ae4b` (pushed 2026-08-26T14:34Z); `get_check_runs` still returns `total_count: 0` and
  `get_status` still shows `state: pending` with zero statuses — the Actions run for that push has
  still not been approved. `mergeable_state` is `behind` (one merge commit's worth, from #326
  landing after the branch was last synced) but the PR's own merge ref is conflict-free per the
  last brief's local verification.

  **Next action, unchanged:** approve the pending Actions run for head `b28ae4b` from the Actions
  tab. Nothing else can move until that happens — the fix itself (import-path correction + chmod
  0644 on the two container-mode temp files) was already reviewed favorably in the last two briefs,
  including a local suite run confirming the collection fix and a non-vacuous new test.

  **Reminder carried forward, still true:** the PR's first commit (`277ef8e`) ends with a
  line-opening `Fixes` trailer referencing [issue 157] as item 2 of 4 (items 1/3/4 — egress
  control, a prebuilt image, explicit seccomp — are separate and still open per `CLAUDE.md`
  Current Focus). A merge commit or rebase carries that trailer to `main` verbatim; a squash
  defaults to concatenating both commit messages, so it comes along there too unless you edit the
  squash message down to the PR body (which uses `Refs` instead and is clean). Whichever merge
  method you use, either strip that trailer from the merge message or reopen [issue 157]
  immediately after — this is the same closing-keyword mechanism that retired #172 twice.

## New since 2026-08-27

None.

## Going stale

- **#309** (zqian, content migration) — his filing was 2026-08-19; your v1 design + scoping
  questions went out 2026-08-21; no response since (10 days from his filing, 8 since your reply).
- **#172** (zqian/jonespm, New Quizzes) — last non-you comment was jonespm's 2026-08-04 "still
  doesn't look merged" note; your last update was 2026-08-07, before PR #191 was closed
  (unmerged) on 2026-08-26. The issue itself still has nothing on it reflecting that closure —
  worth a short status comment if you want to close the loop with zqian and jonespm.
- **#301** (rpsimon-ai, assignment-group skill request) — filed 2026-08-18, you replied 2026-08-19;
  no response since (11 days from filing).
- **#302** (rpsimon-ai, student-analytics skill request) — filed 2026-08-18, you replied
  2026-08-21; no response since (11 days from filing). Still owes the Skill Request issue template
  you mentioned adding.
- **#299** (turner-moore, cookie-auth feature) — filed 2026-08-17, you replied 2026-08-19; no
  response since (12 days from filing).
- **#142** (watch item, `mcp<2` pin) — no activity since 2026-07-30 (30 days). Still blocked on an
  upstream fastmcp release; no action available.
- **#236, #157** — no external activity in weeks; both self-filed and already tracked in
  `CLAUDE.md` Current Focus as blocked on external access (admin access; sandbox access).

## Checked, nothing needed

- **#318** — you already replied 2026-08-26 with a concrete plan (bring every `delete_*` tool onto
  the token pattern, next minor cycle). Ball is in zqian's court / it's a "you'll follow up" item,
  not stale (3 days).
- **#325** — no reply from you yet, but it's only 3 days old (opened 2026-08-26) and has no new
  comment since the last brief already covered it with a confirmed root cause and a draft reply —
  nothing new to add here today.
- **#315** — self-filed tracking issue (drop Python 3.10), no external activity to act on.

Nothing in this run touched anything outside `internal/issue-triage/`, and no public comment,
label, close, or non-triage merge was performed.

---

## Executor results (2026-08-29T02:20Z)

Ran after the brief. Closing-keyword check on the brief file and the PR #327 body: **clean**
(`scripts/check_closing_keywords.py --strict`, exit 0 both). Nothing needed rewording.

### What I did

**3a — stalled / no-CI PRs.** The brief flags one: **#317**. It is a **fork PR**
(`AmirF194/canvas-mcp`), so it is outside my action boundary — I did **not** update-branch and did
**not** push a retrigger commit. Its `behind` state needs a maintainer update-branch. Noted below.

**3b — green-but-unreviewed PRs.** None: #317 is the only open non-draft PR and it has **no CI
result at all**. I checked out its head read-only and ran the suite locally anyway, since CI cannot
report. Results below. **No other PR was tested, because no other PR is open** (#327 is this brief).

**3c — verification.** Spot-checked every factual claim in the brief against the live API and the
code. All of the brief's specific claims hold; two framing gaps are corrected below.

### Local test results — PR #317 (fork, read-only checkout)

Fetched `refs/pull/317/head` → `b28ae4b`. Nothing pushed anywhere.

| What | Result |
|---|---|
| `pytest tests/ -q` on head `b28ae4b` as-is | **1400 passed, 22 skipped** (24.06s) |
| `ruff check .` | **All checks passed** |
| `mypy src/` | **Success: no issues found in 50 source files** |
| Local merge of `origin/main` (`e52561c`) into head | **conflict-free**, merge exit 0 |
| `pytest tests/ -q` on that merged tree | **1400 passed, 22 skipped** (21.16s) |
| `ruff check .` on merged tree | **All checks passed** |

**Non-vacuity differential (the strongest check available without Docker).** Copied
`tests/security/test_sandbox_nonroot_user.py` onto unpatched `origin/main` and ran it: **3 failed**
(`test_user_flag_present_and_non_root`,
`test_home_is_writable_and_exec_allowed_not_the_noexec_scratch_tmpfs`,
`test_code_and_guard_files_are_world_readable_for_the_nonroot_user`). Same file on the branch:
**3 passed**. The test is real and the import fix is real — the earlier CI collection error
(`ModuleNotFoundError: No module named 'tests.security'`) does not reproduce.

Main-side drift since the PR's base (`0955a1a`) touches no sandbox code — only workflows, docs,
`Dockerfile`, `package*.json`, `impact.json`, `CLAUDE.md`. No interaction with this diff.

**Diff spot-check.** Both review findings are genuinely addressed, gated on `sandbox_mode ==
"container"`: `os.chmod(guard_path, 0o644)` on the `.cjs` network guard and
`os.chmod(temp_file_path, 0o644)` on the `.ts` script. The new test records `os.chmod` calls and
asserts exactly 2 paths at `0o644` — it would catch a regression on either one.

### Corrections to the brief

1. **"the Actions run … has still not been approved" is true for head `b28ae4b`, but the brief
   omits that approval was already granted once and did not carry over.** Vishal's 2026-08-26
   review says verbatim "I have approved the Actions run so CI will report on the next push"; CI
   then ran on `277ef8e` (attempt 2) and failed on collection. The contributor pushed `b28ae4b`
   67 minutes later, and **four runs on that head have sat at `conclusion: action_required` since
   2026-08-26T14:33:59Z**: Canvas MCP Enhancement Testing (`32981075860`), Closing Keyword Guard
   (`32981076033`), Claude Code Review (`32981075767`), Security Testing (`32981075982`).
   **This will recur on every push** until AmirF194 has a merged PR — GitHub's approval is
   per-run, not per-contributor, at the current setting. So the brief's "next action, unchanged:
   approve the pending run" is right for today but understates it: repeating the click each push
   is the workaround; the durable fix is the repo's Actions approval setting.

2. **"reviewed favorably in the last two briefs" understates the GitHub state.** The standing
   review on #317 is a formal **CHANGES_REQUESTED** (2026-08-26T13:12:46Z), never dismissed.
   `b28ae4b` is the contributor's response to it, and I verified both findings are addressed. But
   one request in that review is arguably **still open**: it asked the contributor to "confirm an
   end-to-end `execute_typescript` run on a Linux Docker host." The contributor's reply confirms
   the *permission mechanism* on a Linux host (`cat` under `--user 65532:65532`: denied at 0600,
   clean at 0644) — not a full end-to-end run. **I could not run it either: no Docker daemon in
   this sandbox** (`/usr/bin/docker` present, `/var/run/docker.sock` absent). Unmeasured either
   way; flagging rather than asserting.

3. **The brief's uninvestigated "no 2026-08-28.md" note — investigated.** No triage PR was created
   on 08-28 at all: PR numbers run #326 (08-27) → #327 (08-29), with no triage branch between.
   A quiet-day early exit is *not* a sufficient explanation, because #317's unresolved state
   existed on 08-28 exactly as it did today, and that is the specific condition the 08-29 brief
   cites for staying out of the early exit. A consistent run would have produced an 08-28 brief.
   This points at the routine **not firing** on 08-28 rather than exiting early — worth checking
   the schedule.

### Observation — not acted on (security-sensitive → read-only per my boundary)

`chmod 0o644` is the right fix for the read-only-bind-mount `EACCES`, but it does widen
**host-local** readability: while a run is in flight, any local user on the host can read the
executed TypeScript and the network-guard file in the system temp dir. It is correctly gated to
container mode, so process mode keeps 0600, and the executed code can carry Canvas data. Tighter
alternatives if wanted: run the container as the host `uid:gid` and keep 0600, or 0640 with a
shared gid. **Not a blocker** — the current review thread's reasoning (the workspace is read-only
inside the guest) is sound. Recording it as a judgment call that belongs to Vishal, not to me.

### Needs Vishal

1. **Approve the 4 pending Actions runs on #317 head `b28ae4b`** (IDs above). Recurring per-push
   until that contributor has a merged PR — consider the repo Actions approval setting instead.
2. **Re-review #317.** CHANGES_REQUESTED is still standing. Both findings verified addressed
   locally; suite/ruff/mypy green on the head *and* on a local merge with current `main`.
3. **Optional: the end-to-end Docker check** the review asked for. Contributor appears to have a
   Linux host; this sandbox has no Docker daemon.
4. **Decide the 0644 host-local-readability tradeoff** (observation above).
5. **Merge-message hygiene on #317** — commit `277ef8e` ends with a line-opening `Fixes` trailer
   referencing [issue 157]. Strip it from the merge message or reopen [issue 157] right after.
   Same mechanism that retired [issue 172] twice.
6. **#317 is `behind`** — needs a maintainer update-branch; I cannot touch a fork branch.
7. **Check why no 08-28 triage run** (correction 3).
8. **Carried-forward public replies I am not permitted to post:** a status comment on [issue 172]
   noting PR #191 was closed unmerged on 2026-08-26 (nothing on the issue reflects it); the Skill
   Request issue template owed to rpsimon-ai on [issue 302]; [issue 318]'s delete-confirmation pass.

Nothing outside `internal/issue-triage/` was modified. No public comment, label, close, approval,
or non-triage merge was performed. The local `pr317-readonly` / `pr317-merge-test` refs are
sandbox-only and were never pushed.
