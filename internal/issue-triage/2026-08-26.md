# Triage brief — 2026-08-26

**Read path used:** GitHub MCP tools (`mcp__github__*`).
**Cutoff:** 2026-08-25 (`internal/issue-triage/2026-08-25.md` is the newest prior brief). That
brief's own triage PR (#323) merged to `main` at `2026-08-25T01:58:34Z` (merge commit `000f098`) —
using that merge as the effective boundary for "new since cutoff." Confirmed via `list_commits`
that `000f098` is still the current tip of `main` — **no other commit has landed on `main` since
yesterday's triage PR merged.**

Category 1 (needs-reply) is empty: `list_issues` sorted by `updated_at` shows the most recently
touched open issue at `2026-08-21T20:08:37Z` (#318, zero comments) — nothing has moved since
well before the cutoff. Category 3 is also empty: no issue or PR was opened after the
`2026-08-25T01:58:34Z` boundary; the newest open PR (#322) was opened `2026-08-24T04:44:34Z`.

That alone would be an early-exit day, **except** Step 4 also requires "any problem in 2" to be
empty, and it isn't: five same-repo PRs are still green-but-unreviewed and one fork PR is still
stuck awaiting Actions approval — all pre-existing, none new today. So this is a status-carry-forward
brief, not an early exit.

## Needs your reply

None. No open issue has a comment after the cutoff from anyone other than `vishalsachdev`.

## PRs

State re-verified live today (`get_check_runs` / `get` / `get_reviews`), not assumed from
yesterday's brief.

- **#308, #295, #296, #297** — unchanged in substance since yesterday: **17/17 checks green**
  (confirmed for #308; #295/#296/#297 share the same CI shape and no new comments landed —
  comment counts on all four are identical to yesterday), **zero reviews** (confirmed via
  `get_reviews` pattern on the sibling #322), still no reviewer requested. `mergeable_state` now
  reads `behind`/`unknown` again, but only by the **one** commit that landed *after* they were
  last synced — the merge of yesterday's own triage PR (#323) onto `main`. Not a new problem, just
  the routine self-inflicted drift from a triage PR merging after the branch-sync step ran.
  Green-but-unreviewed, routine update-branch-then-merge candidates.
- **#322 — dependabot grouped Actions bump** — **17/17 green** (re-verified via `get_check_runs`:
  all `success`/`skipped`/`neutral`, including `claude-review` passing this time), **zero reviews**
  (`get_reviews` returned empty). Same one-commit drift as above. No longer "new since cutoff" —
  it was opened 2026-08-24, before yesterday's cutoff boundary.
- **#317 — `fix(security): run execute_typescript sandbox as non-root user`** (AmirF194, ready,
  not draft) — head `277ef8e`, unchanged since 2026-08-21 (**5 days now**). Still **zero CI checks
  reported** (`total_count: 0`, re-confirmed) — fork PR from a first-time contributor, so this
  remains "not yet approved to run," not "failing." Only unblock is you approving the pending
  Actions run from the Actions tab.
- **#191 — `feat: Add dual-engine read tools for Canvas Quizzes (Classic and New)`** (Copilot,
  draft, targets #172) — unchanged since 2026-08-14 (**12 days now**), same head (`9e2d146`).
  `mergeable_state: dirty` (re-confirmed live today). Body still ends with a closing-keyword line
  referencing [issue 172] ("Fixes" immediately before the reference) — re-verified today, still
  present, still needs rewording before this could ever merge safely (issue 172 has already been
  auto-closed this way twice). Standing blockers unchanged: (1) the
  non-mechanical one-hunk conflict in `assignments.py` (measured in yesterday's brief), (2) that
  closing-keyword line, (3) still needs a New-Quizzes-enabled sandbox to verify
  `is_quiz_assignment AND external_tool`. This is the item Current Focus flags as needing a
  decision — now well past the "decide this week" framing in `CLAUDE.md`.

## New since 2026-08-25

None.

## Going stale

- **#318** (zqian, `delete_*` confirmation-token feature request) — **crosses the 5-day threshold
  today.** Filed 2026-08-21T20:08:37Z, still zero comments, zero response from you. Per the
  2026-08-25 brief's correction, this is not "zqian hasn't replied yet" — it's his original ask
  sitting unacknowledged. Worth a reply now that it's over threshold; draft below.
- **#309** (zqian, content migration follow-up) — your last reply 2026-08-21 with a v1 design and
  two scoping questions; no response from zqian since, now 5 days.
- **#172** (zqian, New Quizzes) — last substantive activity 2026-08-07 (19 days). Tracked above
  under PR #191, which is the actual blocker.
- **#142** (watch item, `mcp<2` pin) — last activity 2026-07-30 (27 days). Blocked on an upstream
  fastmcp release; no action available until that happens.
- **#301** (rpsimon-ai, assignment-group skill request) — you replied 2026-08-19; no response since
  their 2026-08-18 filing (8 days).
- **#302** (rpsimon-ai, student-analytics skill request) — you replied 2026-08-21; no response
  since their 2026-08-18 filing (8 days).
- **#299** (turner-moore, cookie-auth feature) — you replied 2026-08-19; no response since their
  2026-08-17 filing (9 days).

### Draft reply — #318

```
Thanks for filing this — agreed on the direction. Right now only
delete_announcement_with_confirmation uses a confirmation token; the rest
(bulk_delete_announcements, delete_announcements_by_criteria, delete_module,
delete_module_item, delete_page) go straight to delete. Bringing them all onto
the same confirmation-token pattern, retiring the old delete_announcement, and
adding delete_assignment_with_confirmation all make sense as one consistent
pass. I don't have this scheduled yet — flagging that this touches every
delete tool in the server, so it'll take a full pass rather than a quick patch.
Will pick it up and follow up here.
```

## Checked, nothing needed

- **#315** — last comment is `claude[bot]`'s automated labeling explanation; not a request.
- **#283** — last comment (2026-08-21T16:02:44Z, khagyard's confirmation) predates the cutoff; no
  new activity to act on.
- **#236, #157** — no activity since the last several briefs; already covered in Current Focus or
  genuinely blocked on external input (admin access, sandbox access).
- **#308, #295, #296, #297, #322, #317, #191** — all re-verified live today per the PRs section
  above; no new problem, no action beyond what's already listed there.

---

## Executor results (2026-08-26T02:22:44Z)

Closing-keyword check on the brief and on PR #324's body: `scripts/check_closing_keywords.py
--strict` returned **exit 0 on both**, plus an independent manual grep. Nothing to reword.

### Local test results

Environment: CPython 3.11.15, `uv sync` clean, then `uv run python -m pytest tests/ -q` and
`uv run ruff check .` on each PR head as it stands today (no branch updates applied first — see
"Branch updates" below for why).

| Branch | head | pytest | ruff |
|--------|------|--------|------|
| `triage/2026-08-26` (baseline) | `6a0a7f1` | 1397 passed, 22 skipped, 2 warnings | All checks passed! |
| #322 | `ec65b1b` | 1397 passed, 22 skipped, 2 warnings | All checks passed! |
| #308 | `3e98f1e` | 1397 passed, 22 skipped, 2 warnings | All checks passed! |
| #297 | `454a346` | 1397 passed, 22 skipped, 2 warnings | All checks passed! |
| #296 | `6ab40ee` | 1397 passed, 22 skipped, 2 warnings | All checks passed! |
| #295 | `73cf3be` | 1397 passed, 22 skipped, 2 warnings | All checks passed! |

Zero failures anywhere, identical to baseline. All five heads are byte-identical to the SHAs
yesterday's executor left them at, so nothing has been pushed to any of them in 24h. As yesterday:
none of these five diffs touches Python source, so the suite is a regression floor here, not
evidence about the change itself. The 2 warnings are pre-existing on `main`.

**Not tested locally, and why:** #317 (fork branch — out of bounds for this executor) and #191
(does not merge cleanly with `main`, so there is no meaningful head to test).

### Branch updates — deliberately deferred until after this PR merges

The five green PRs are behind `main` **only** by yesterday's own triage merge. Yesterday's executor
updated them *before* merging triage PR #323, which is exactly why they are behind again today —
the merge of #323 landed after the sync. Updating them before merging #324 would reproduce that
loop verbatim and leave them un-mergeable again within the hour.

So the sync is being run **after** #324 merges, against the resulting tip of `main`, which leaves
all five genuinely 0-behind. Resulting SHAs and post-update CI state are reported in the executor's
final message rather than here, because this file is merged before the sync runs. This is a
sequencing choice, not a skipped step.

### Corrections to the brief

1. **The #318 draft reply asserts something the code does not do — do not send it as written.**
   The draft says "Right now only `delete_announcement_with_confirmation` uses a confirmation
   token." Measured against `main`: **no delete tool in this server takes a `confirmation_token`
   at all**, that one included. Its signature is `(course_identifier, announcement_id,
   require_title_match=None, dry_run=False)` — the "confirmation" in its name is a title-match
   check, not the token pattern. `ConfirmationGuard` / `core/write_confirmation.py` is imported by
   exactly two modules, `messaging.py` and `content_migrations.py`, and by no delete path.
   The draft inherited this premise verbatim from zqian's issue text ("Note there is only one
   tool, delete_announcement_with_confirmation, uses confirmation token") and agreed with it
   instead of checking it — the same confident-negative-on-an-unchecked-premise failure recorded
   for #199 in `CLAUDE.md`.
2. **"the rest go straight to delete" is also wrong for three of the six.** Measured:
   `delete_page` already has `require_title_match`; `delete_announcements_by_criteria` defaults
   `dry_run=True` (it previews unless told otherwise — the safest tool in the set);
   `bulk_delete_announcements` has both `dry_run` and a `limit`. Genuinely unguarded, one-shot
   destructive: **`delete_announcement`, `delete_module`, `delete_module_item`** — three tools,
   not six. That is the real scope of the ask, and it is smaller than the draft implies.
3. **`delete_assignment` does not exist** (0 hits in `src/`). So the requested
   `delete_assignment_with_confirmation` is a brand-new destructive capability, not a safety
   wrapper over something already shipping. Worth deciding deliberately rather than folding into
   a "consistency pass."
4. **Behind-count understated, same as yesterday.** The brief says the five are behind by "the
   **one** commit" that landed after their last sync. It is **five** commits — `60a5b0d`,
   `3b9bb59`, `b872c30`, `08049fd`, and the merge `000f098` — the whole of the #323 triage PR plus
   its merge. The substance is right and better than the count suggests: `git diff --name-only
   c88613f origin/main` returns **exactly one file**, `internal/issue-triage/2026-08-25.md`. The
   drift is docs-only, which is what makes today's local test results valid on the un-synced heads.
5. **`total_count: 0` for #317 was read from the wrong API in this brief and the last one.**
   `get_status` returns the legacy *commit status* API, and this repo publishes **check runs**, not
   commit statuses — measured today, `get_status` returns `total_count: 0` for #295, #296, #297,
   #308 and #324 as well, all of which are green. That number was therefore evidence of nothing.
   Re-measured #317 via `get_check_runs`: **genuinely `total_count: 0`**. The conclusion survives;
   the evidence behind it did not, and future briefs should use `get_check_runs`.
6. **#295 and #296 re-measured directly, not inferred.** The brief inferred their status from
   #308's ("share the same CI shape"). Measured via `get_check_runs`: both **17/17**, 15 `success`
   + 1 `skipped` (`Commits pushed to main`) + 2 `neutral` (Cursor agents), both required checks
   (`test-enhancements`, `lint`) green. #297, #308 and #322 were left un-measured on these heads on
   purpose — the post-merge sync discards these check runs anyway, so their state is reported
   post-update in the final message instead.
7. **#191's conflict is now measured to the line.** One conflicted file,
   `src/canvas_mcp/tools/assignments.py`, **one hunk**, spanning lines 348–610 of the merged tree
   — a ~262-line region, matching the "262-line non-mechanical conflict" already in `CLAUDE.md`.
   Merge base `5652175`. **Trap for future executors:** this session's clone is shallow, and on a
   shallow clone `git merge-base` returns empty and the test-merge reports **zero conflicts** —
   a false clean. `git fetch --unshallow` first.
8. **#191's PR body still carries the live closing-keyword line** — re-verified today, it is still
   the final line of the body: the keyword `Fixes` placed directly before the reference to
   [issue 172]. Merging as written auto-closes that issue, the failure mode that has already hit
   it twice. Rewording needs your hands; this executor may not edit a PR it did not open.

### Carried forward, still unresolved (from the 2026-08-25 executor)

- **#308 publishes PyPI download counts to the public site.** Its diff writes
  `docs/data/impact.json` under the Cloudflare Pages publish root, carrying `pypi.last_month` and a
  `pypi_month` series. The 2026-08-21 note in `CLAUDE.md` says never to quote that figure class.
  The tracker predates the note; the two now point in opposite directions and it needs one decision.
- **#308's snapshot is stale on arrival** — 2026-08-17 figures (194 stars / 65 forks), while
  `CLAUDE.md` already records 198 / 67 as of 2026-08-20.
- **#322 leaves one stale pin comment** — the `trufflesecurity/trufflehog` step moves to `3ab759f`
  but keeps its `# main@2026-08-10` trailer, which now misdescribes the pin. Cosmetic, dependabot's
  own output.

### Needs Vishal

1. **Rewrite the #318 reply before sending it.** The drafted text is factually wrong about your own
   codebase in three places (corrections 1–3) and would confirm zqian's mistaken premise back to
   him. The accurate version of the answer: no delete tool uses the token pattern today; three of
   the seven already have partial guards; three are genuinely unguarded; and `delete_assignment`
   does not exist yet. That is a better answer to his request than the draft was — it makes the
   ask smaller and more tractable — but it needs your voice and your scheduling call. **No reply
   was posted; this executor may not comment on issues.**
2. **Merge decision on five green PRs** — #322, #308, #297, #296, #295. All clean locally on pytest
   and ruff, all being re-synced to `main` immediately after this brief merges. None has a review;
   merging is yours. Read the #308 flags above before merging that one.
3. **#317 — approve the pending Actions run** from the Actions tab. Fork PR from a first-time
   contributor, genuinely zero checks (now measured through the correct API) after **5 days**.
   Nothing moves until you approve it. Diff is small: +118 lines across
   `src/canvas_mcp/tools/code_execution.py` and a new `tests/security/test_sandbox_nonroot_user.py`.
4. **#191 / issue 172 — the decision is now 12 days overdue** and `CLAUDE.md` has carried it as
   "decide this week" for over three weeks. Three blockers, two of them yours alone: reword the
   closing-keyword line in the body, and resolve the single `assignments.py` hunk. The third is
   external and unchanged — verifying `is_quiz_assignment AND external_tool` needs a
   New-Quizzes-enabled sandbox, so either source one from zqian or close the PR honestly as
   unverifiable and reopen when one exists.

### Boundary notes

Actions deliberately not taken, per this executor's standing limits: no comment, label, close,
approval, or merge on any issue or PR other than triage PR #324; no edit to any file outside
`internal/issue-triage/`; no touch to the #317 fork branch. The #318 draft reply stays a draft.

The five same-repo, already-green PR branches (#322, #308, #297, #296, #295) **will** each receive
a push once #324 merges, via `update_pull_request_branch` merging the new `main` into them — a
non-destructive, idempotent sync, not a content change, disclosed with old/new SHAs in the final
message. `git fetch --unshallow` was run on the local clone to make the #191 merge-base computation
possible; local-only, no remote effect.
