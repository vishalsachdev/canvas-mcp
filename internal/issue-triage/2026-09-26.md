# Triage brief — 2026-09-26

**Read path used:** GitHub MCP tools throughout (`gh` not installed in this sandbox).

**Cutoff:** 2026-09-25 — the newest prior brief is `internal/issue-triage/2026-09-25.md`, per the
routine's file-naming convention.

**Mechanically quiet, but not an early exit.** No open issue has a comment newer than cutoff from
anyone other than `vishalsachdev` (category 1: empty). No issue or PR was opened since cutoff by
anyone other than `vishalsachdev` (category 3: empty). But two PRs still carry a red check
(claude-review, for the known Dependabot-secret reason) and one fork PR still reports zero checks —
those are persisting "problems" under category 2, so per Step 4 this is not a clean early exit even
though nothing changed. The main development worth noting: **#411 and #412 both merged today**
(2026-09-25, ~16:12Z and ~16:16Z), each after a fencing follow-up addressing the review findings the
2026-09-25 brief flagged. `main` is now at `db639b0`.

## Needs your reply

None. No open issue has a comment newer than cutoff from anyone other than you.

## PRs

### #411, #412 — MERGED 2026-09-25, no action needed

Both PRs from the 2026-09-23 prompt audit merged today. Per #411's PR body, commits `cb5609b` and
`b77b748` ("Final resource-fencing follow-up") address the Codex P1 the 2026-09-25 brief flagged
(unfenced `syllabus_body`/assignment-description resource paths) with live MCP resource-read tests
covering ordinary content, forged fence markers, empty responses, and the syllabus request contract
— full suite reported at 1,650 passed / 21 skipped, ruff/mypy/manifest-JSON clean. I have not
independently re-verified the fencing fix against source myself today; noting it as reported in the
PR body rather than re-confirming. #412's inbox-notice wording merged separately and unchanged from
what the 2026-09-25 brief already reviewed. Neither needs anything further from you.

### #395 — chore(deps): bump the actions group across 1 directory with 6 updates (Dependabot) — unchanged

Still open, unmerged, now `mergeable_state: behind` (main has advanced past its base with today's
two merges). 20 checks reported, only `claude-review` red — same withheld-OAuth-secret cause
documented in the 2026-09-24/25 briefs (Dependabot PRs don't receive the Claude GitHub App secret;
this is a fail-fast guard, not a review objection). No new activity since last brief.

Next action: unchanged — routine bump, low risk, merge at your discretion (a rebase/update-branch
will be needed given `behind`, though Dependabot usually handles this itself once idle).

### #390 — fix: separate external-tool assignments from missing submissions (@EastArctica, fork) — unchanged

Still 0 reported check runs (fork PR, first-time contributor; Actions withheld pending your
approval from the Actions tab). `mergeable_state: behind` — main has advanced by three commits
(`c34695168`→`db639b0`) since this PR's base. Diff unchanged from prior briefs: one function change
in `src/canvas_mcp/tools/student_tools.py` separating external-tool (e.g. Gradescope) assignments
from the missing/overdue bucket.

Next action: unchanged — needs an Actions-tab approval for CI signal, and a rebase/update-branch
before merge. The 2026-09-25 executor's source-level nit still applies and hasn't been raised with
the contributor: `"external_tool" in assignment.get("submission_types", [])` would raise
`TypeError` if Canvas ever returns that key as `null` rather than omitting it — unverified against a
live instance, worth a look by whoever reviews it.

### #387 — chore(deps-dev): bump @types/node from 26.5.1 to 26.6.1 (Dependabot) — unchanged

Same shape as #395: 17 checks reported, only `claude-review` red for the same OAuth-secret reason.
`mergeable_state: behind`. No new activity.

Next action: unchanged — merge at your discretion.

## New since 2026-09-25

None. No issue or PR was opened by anyone other than you since cutoff.

## Going stale

Unchanged from the last several briefs, no non-`vishalsachdev` activity since 2026-09-08: **#157**,
**#172**, **#236**, **#301**, **#302**. Also stale by the mechanical rule but waiting on the
reporter, not you: **#374** (chiptoe-svg invited 2026-09-14 to retest the merged fix; no reply in 12
days) and **#375** (see below — a corrected draft reply has been sitting unsent since at least
2026-09-25).

## Outstanding from prior briefs (not re-verified today, restated for continuity)

These were routed to you in the 2026-09-24/25 briefs and, as far as I can tell from today's read,
are still open:

1. **#399 looks like a closure candidate.** The 2026-09-25 executor independently verified PR #403
   (merged `5714a58`) satisfies every acceptance criterion in #399's body, line-by-line against
   `src/canvas_mcp/code_api/client.ts`. Closing an issue is outside this routine's boundary.
2. ⚠️ **#375 has a corrected draft reply waiting to send — high-sensitivity (rubric/grade-write
   correctness).** The draft (verified against `src/canvas_mcp/tools/rubrics.py` in the 2026-09-25
   brief) tells chiptoe-svg that `update_rubric` shipped in PR #386 (`e5ff3b1`). Full text is in
   `internal/issue-triage/2026-09-25.md` under "Carried forward from yesterday," item 2. Still
   unsent as of this read.
3. **#391 (Windows install failure, @dmalpiCS, first-time reporter) is still unanswered — now 4
   days old, zero comments.** Draft reply is in `internal/issue-triage/2026-09-25.md`, item 3. No
   Windows sandbox exists here to verify the `spawn uv ENOENT` diagnosis independently.
4. **Merge decisions on #395 and #387** are yours — both verified clean by the 2026-09-25 executor
   (trial-merge onto then-current main, full suite passing).
5. **#390 needs an Actions-tab approval** to get real CI signal (fork, first-time contributor).

## Checked, nothing needed

#411, #412 (merged today, reviewed above), #384 (weekly maintenance report from `claude[bot]`, no
comments — excluded automation per standing instruction), #399 / #391 / #395 / #387 / #390
(reviewed — see **PRs** and **Outstanding from prior briefs**), #157 / #172 / #236 / #301 / #302 /
#374 / #375 (reviewed — see **Going stale** / **Outstanding from prior briefs**, no new comments
from anyone since their last-noted activity).

---

*Read path: GitHub MCP tools (`mcp__github__*`). No repository files outside
`internal/issue-triage/` were modified. No comments, labels, merges, or closes were performed by
this routine.*
