# Triage brief — 2026-09-26

**Read path used:** GitHub MCP tools throughout (`gh` not installed in this sandbox).

**Cutoff:** 2026-09-25 — the newest prior brief is `internal/issue-triage/2026-09-25.md`, per the
routine's file-naming convention.

**Mechanically quiet, but not an early exit.** No open issue has a comment newer than cutoff from
anyone other than `vishalsachdev` (category 1: empty). No issue or PR was opened since cutoff by
anyone other than `vishalsachdev` (category 3: empty). But two PRs still carry a red check
(claude-review, for the known Dependabot-secret reason) and one fork PR still reports zero checks —
those are persisting "problems" under category 2, so per Step 4 this is not a clean early exit even
though nothing changed. The main development worth noting: **#411 and #412 both merged today**
(2026-09-25, ~16:12Z and ~16:16Z), each after a fencing follow-up addressing the review findings the
2026-09-25 brief flagged. `main` is now at `db639b0`.

## Needs your reply

None. No open issue has a comment newer than cutoff from anyone other than you.

## PRs

### #411, #412 — MERGED 2026-09-25, no action needed

Both PRs from the 2026-09-23 prompt audit merged today. Per #411's PR body, commits `cb5609b` and
`b77b748` ("Final resource-fencing follow-up") address the Codex P1 the 2026-09-25 brief flagged
(unfenced `syllabus_body`/assignment-description resource paths) with live MCP resource-read tests
covering ordinary content, forged fence markers, empty responses, and the syllabus request contract
— full suite reported at 1,650 passed / 21 skipped, ruff/mypy/manifest-JSON clean. I have not
independently re-verified the fencing fix against source myself today; noting it as reported in the
PR body rather than re-confirming. #412's inbox-notice wording merged separately and unchanged from
what the 2026-09-25 brief already reviewed. Neither needs anything further from you.

### #395 — chore(deps): bump the actions group across 1 directory with 6 updates (Dependabot) — unchanged

Still open, unmerged, now `mergeable_state: behind` (main has advanced past its base with today's
two merges). 20 checks reported, only `claude-review` red — same withheld-OAuth-secret cause
documented in the 2026-09-24/25 briefs (Dependabot PRs don't receive the Claude GitHub App secret;
this is a fail-fast guard, not a review objection). No new activity since last brief.

Next action: unchanged — routine bump, low risk, merge at your discretion (a rebase/update-branch
will be needed given `behind`, though Dependabot usually handles this itself once idle).

### #390 — fix: separate external-tool assignments from missing submissions (@EastArctica, fork) — unchanged

Still 0 reported check runs (fork PR, first-time contributor; Actions withheld pending your
approval from the Actions tab). `mergeable_state: behind` — main has advanced by three commits
(`c34695168`→`db639b0`) since this PR's base. Diff unchanged from prior briefs: one function change
in `src/canvas_mcp/tools/student_tools.py` separating external-tool (e.g. Gradescope) assignments
from the missing/overdue bucket.

Next action: unchanged — needs an Actions-tab approval for CI signal, and a rebase/update-branch
before merge. The 2026-09-25 executor's source-level nit still applies and hasn't been raised with
the contributor: `"external_tool" in assignment.get("submission_types", [])` would raise
`TypeError` if Canvas ever returns that key as `null` rather than omitting it — unverified against a
live instance, worth a look by whoever reviews it.

### #387 — chore(deps-dev): bump @types/node from 26.5.1 to 26.6.1 (Dependabot) — unchanged

Same shape as #395: 17 checks reported, only `claude-review` red for the same OAuth-secret reason.
`mergeable_state: behind`. No new activity.

Next action: unchanged — merge at your discretion.

## New since 2026-09-25

None. No issue or PR was opened by anyone other than you since cutoff.

## Going stale

Unchanged from the last several briefs, no non-`vishalsachdev` activity since 2026-09-08: **#157**,
**#172**, **#236**, **#301**, **#302**. Also stale by the mechanical rule but waiting on the
reporter, not you: **#374** (chiptoe-svg invited 2026-09-14 to retest the merged fix; no reply in 12
days) and **#375** (see below — a corrected draft reply has been sitting unsent since at least
2026-09-25).

## Outstanding from prior briefs (not re-verified today, restated for continuity)

These were routed to you in the 2026-09-24/25 briefs and, as far as I can tell from today's read,
are still open:

1. **#399 looks like a closure candidate.** The 2026-09-25 executor independently verified PR #403
   (merged `5714a58`) satisfies every acceptance criterion in #399's body, line-by-line against
   `src/canvas_mcp/code_api/client.ts`. Closing an issue is outside this routine's boundary.
2. ⚠️ **#375 has a corrected draft reply waiting to send — high-sensitivity (rubric/grade-write
   correctness).** The draft (verified against `src/canvas_mcp/tools/rubrics.py` in the 2026-09-25
   brief) tells chiptoe-svg that `update_rubric` shipped in PR #386 (`e5ff3b1`). Full text is in
   `internal/issue-triage/2026-09-25.md` under "Carried forward from yesterday," item 2. Still
   unsent as of this read.
3. **#391 (Windows install failure, @dmalpiCS, first-time reporter) is still unanswered — now 4
   days old, zero comments.** Draft reply is in `internal/issue-triage/2026-09-25.md`, item 3. No
   Windows sandbox exists here to verify the `spawn uv ENOENT` diagnosis independently.
4. **Merge decisions on #395 and #387** are yours — both verified clean by the 2026-09-25 executor
   (trial-merge onto then-current main, full suite passing).
5. **#390 needs an Actions-tab approval** to get real CI signal (fork, first-time contributor).

## Checked, nothing needed

#411, #412 (merged today, reviewed above), #384 (weekly maintenance report from `claude[bot]`, no
comments — excluded automation per standing instruction), #399 / #391 / #395 / #387 / #390
(reviewed — see **PRs** and **Outstanding from prior briefs**), #157 / #172 / #236 / #301 / #302 /
#374 / #375 (reviewed — see **Going stale** / **Outstanding from prior briefs**, no new comments
from anyone since their last-noted activity).

---

*Read path: GitHub MCP tools (`mcp__github__*`). No repository files outside
`internal/issue-triage/` were modified. No comments, labels, merges, or closes were performed by
this routine.*

---

## Executor results (2026-09-26T02:22Z)

Ran after the brief above. Environment: sandboxed clone at `db639b0`, `gh` not installed, GitHub
MCP tools for all remote reads. All three non-brief PR heads were fetched by `refs/pull/N/head`
and the SHAs match what the API reports (`#395` `826cece`, `#387` `8f63289`, `#390` `d574fe2`), so
the numbers below are against the exact heads under review.

### Local verification runs

Baseline on `main` (`db639b0`), for comparison: **1649 passed, 22 skipped** (33.1s), exit 0.

| PR | head | trial-merge onto current `main` | `pytest tests/ -q` | `ruff check .` |
|----|------|-------------------------------|--------------------|----------------|
| #395 | `826cece` | clean (`ort`, 8 workflow files) | **1649 passed, 22 skipped** (32.4s), exit 0 | All checks passed |
| #387 | `8f63289` | clean (`ort`, 2 files) | **1649 passed, 22 skipped** (31.9s), exit 0 | All checks passed |
| #390 | `d574fe2` | clean (`ort`, 2 files) | **1650 passed, 22 skipped** (32.4s), exit 0 | All checks passed |

Each was merged into a throwaway worktree branched from `origin/main`; none required conflict
resolution. #390's +1 test is the regression the contributor added alongside the fix
(`tests/tools/test_student_tools.py`, +29 lines).

**TypeScript suite also run** (relevant to the #399 verification below): `npm test` reports
**96 tests, 96 pass, 0 fail** (8.0s), exit 0. First attempt failed 7/7 with `ERR_MODULE_NOT_FOUND`
purely because this clone had no `node_modules`; after `npm ci` (exit 0, 7 packages, 0
vulnerabilities) everything passes. That is a sandbox artifact, not a repo defect — noting it only
so the first failure in the logs isn't mistaken for a regression.

**Unchanged from the brief:** #395 still reports 20 check runs with `claude-review` the only
`failure`; every required check (`test-enhancements`, `lint`, `test (3.11/3.12/3.13)`,
`verify-confirmation`, `test-code-api`, and the full security set) is `success`. #390 still
reports `total_count: 0`. Both confirmed live this run.

### Nothing to unstall

No same-repo PR is stalled or missing CI, so there was no update-branch or empty-commit retrigger
to perform.

- **#390 was not touched.** It is a fork branch (`EastArctica/canvas-mcp`); pushing to it is
  outside this routine's boundary. Its zero-check state needs a maintainer approval from the
  Actions tab, which only you can give.
- **#395 and #387 were deliberately not update-branched**, although both are `behind`. They are
  not stalled and not missing CI — they have full, recent check runs. Pushing a merge commit to a
  Dependabot branch makes Dependabot stop maintaining that PR (it treats a non-Dependabot push as
  a signal to back off), which would trade a trivial `behind` state for a branch that no longer
  auto-rebases or auto-closes on a newer bump. Both trial-merge cleanly onto current `main`, so
  nothing is actually blocked. If branch protection demands an up-to-date branch at merge time,
  the right lever is GitHub's own "Update branch" button or `@dependabot rebase`, not a push
  from here.

### Verification of the brief's claims

**#411's fencing follow-up — VERIFIED on `main`, upgrading the brief's "reported, not
re-confirmed".** The brief relayed the fix from #411's PR body without checking it. I checked it.
`src/canvas_mcp/resources/resources.py` on `main` now imports `fence_untrusted` and applies it on
**both** resource paths: `get_course_syllabus` ends in
`return fence_untrusted(syllabus_body, "course syllabus")` and `get_assignment_description` ends in
`return fence_untrusted(description, "assignment description")`. That closes both the Codex P1 the
2026-09-25 brief flagged *and* the sibling `get_assignment_description` gap that the 2026-09-25
executor found independently (it had noted zero fencing hits anywhere under
`src/canvas_mcp/resources/`). Both are genuinely fixed on `main` today.

**Provenance note on `cb5609b` / `b77b748`.** These are real commits (`fix: fence Canvas resource
content and cover resource registry` and `docs: correct bulk grading delay and explicit test
fixture`), but they are **not reachable from `main`** — #411 was squash-merged as `e9456c6`, so
they survive only on `origin/audit/prompt-surface-2026-09-23`, which still exists post-merge.
Same for `origin/audit/untrusted-notice-wording` behind #412. Citing them is fine; just don't
expect `git show` to resolve them in a fresh clone that only fetched `main`. Both branches are
merge-cleanup candidates.

**#399 — closure candidate INDEPENDENTLY RE-CONFIRMED.** I did not take the prior verification on
trust; I re-checked all eight acceptance criteria against `src/canvas_mcp/code_api/client.ts` on
`main` (`fetchAllPaginated`, lines 257–278):

| Criterion | Evidence on `main` |
|-----------|--------------------|
| Follow next links regardless of page length | `nextPageUrl(response.link, url)`; no `per_page`/short-page short-circuit remains |
| No credential forwarding to changed origin/endpoint | `validatePageUrl(url, initial)` called before every dispatch (l.267, defined l.244) |
| Cycle detection | `seen: Set<string>`, throws `Pagination cycle detected; no partial result returned` |
| Finite budget, explicit failure not partial success | `MAX_PAGINATION_PAGES = 10000` (l.180); throws on exhaustion rather than returning `results` |
| Reject malformed responses with a diagnostic | `if (!Array.isArray(response.data)) throw new Error('Invalid paginated response: expected an array')` |
| No shared mutable cursor / no caller-param mutation | `{...params, page: 1, ...}` spread; `seen`/`results`/`url` are all call-local |
| Preserve #397 write no-retry/no-redirect | `sendCanvasRequest(..., {redirect: 'error'})` |
| Update verification scope/mapping | `verify/TYPESCRIPT.md:95` now points at `TYPESCRIPT_PAGINATION.md` |

The named-test criterion is satisfied too: `tests/code_api/pagination-control-plane.test.ts`
covers short/empty pages, full final page without next, self-cycle, budget exhaustion, unsafe next
links, malformed Link headers, non-array responses, private cursors across concurrent callers, and
credential-snapshot stability — plus `pagination-redirect.test.ts` for redirect bypass. All green
in the 96/96 run above. **#399's fix has fully shipped; it is a clean close.** Closing an issue is
outside this routine's boundary.

**#375 draft reply — commit citation re-verified, and the correction holds.** `e5ff3b1` resolves
to `feat: add guarded rubric updates (#386)` on `main`, and `update_rubric` exists at
`src/canvas_mcp/tools/rubrics.py:1648`. `ad685aee` is still **not** a valid object in this repo
even after fetching every remote branch and tag — the 2026-09-25 correction was right, and the
draft as written uses the correct SHA.

⚠️ **Draft-reply accuracy flags** (per the standing rule that neither the triage agent nor I can
measure live Canvas or Windows behavior):

- The **#375 draft self-flags correctly** — it already says "source-and-mocked-test verified only,
  not tested against a live Canvas write" and asks chiptoe-svg to exercise the read-back identity
  check. No change needed.
- The **#391 draft mixes one verified claim with one unmeasured one.** Verified: the extension
  really does launch with a bare `uv`, relying on PATH — `manifest.json` has
  `"command": "uv"`, `"args": ["run", "--directory", "${__dirname}", "canvas-mcp-server"]`, no
  absolute path. **Unmeasured:** the Windows PATH-refresh *causation* ("GUI apps only pick up PATH
  from your last sign-in", sign-out/reboot as a fix). That is a plausible and common diagnosis, but
  there is no Windows sandbox here and it has not been reproduced. If you send it, the manifest
  detail is safe to state flatly; the sign-out step reads better as "worth trying" than as the
  known cause.

**#390's carried-forward source nit — correct on the semantics, but it should not be framed as a
#390 defect.** `"external_tool" in assignment.get("submission_types", [])` would indeed raise
`TypeError: argument of type 'NoneType' is not iterable` if Canvas returned `submission_types:
null`, since `dict.get`'s default applies only to an *absent* key, not a null value. But the
identical pattern is already on `main` in code #390 never touches:
`src/canvas_mcp/tools/assignments.py:754` (`response.get("submission_types", [])`) and
`assignments.py:108` (`', '.join(response.get('submission_types', ['N/A']))`, which would raise the
same way). So this is a pre-existing repo-wide assumption about Canvas's assignment payload, not
something this contributor introduced. Raising it as a blocking review note on a first-time
contributor's PR would be misplaced; if it's worth fixing it's worth a separate hardening issue
covering all three sites. Still unverified against a live instance either way.

**#391 live state confirmed:** still **zero** comments, 4 days old, unanswered.

**Minor count correction.** The brief relays #411's PR body reporting "1,650 passed / 21 skipped".
Today's `main` measures **1649 passed / 22 skipped**. The one-test delta is a skip-condition
difference in this sandbox, not a lost test; flagging only so the numbers in this file are
internally consistent.

**Not re-verified by me:** the brief's characterisation of `claude-review`'s red state on
#395/#387 as a withheld-secret fail-fast (I did not re-open the job log this run; the prior brief
verified it there, and #414's own `claude-review` passing is consistent with it), and the
2026-09-08 "last non-maintainer activity" dates on the going-stale set.

### Needs Vishal

Everything below is outside this routine's action boundary.

1. **Close [issue 399](https://github.com/vishalsachdev/canvas-mcp/issues/399)** (re: #399 —
   keyword deliberately detached so merging this brief cannot auto-close it). Independently
   re-confirmed above, criterion by criterion, against shipped code
   and green tests. It was missed when its four sibling issues (#398, #400, #401, #402) were
   closed on 2026-09-23.
2. ⚠️ **Send the #375 reply** (high-sensitivity: rubric/grade-write correctness). Full text in
   `internal/issue-triage/2026-09-25.md`, "Carried forward from yesterday", item 2. Commit SHA
   `e5ff3b1` re-verified correct this run. Unsent since at least 2026-09-25.
3. **Send the #391 reply** — @dmalpiCS is a first-time reporter, 4 days with zero response. Draft
   in `internal/issue-triage/2026-09-25.md`, item 3, with the accuracy flag above: state the
   manifest fact flatly, soften the Windows sign-out causation.
4. **Merge decision on #395 and #387.** Both trial-merge clean onto current `main` with the full
   suite and ruff green (table above). Only `claude-review` is red, for the known withheld-secret
   reason. Do not hand-push to either branch; use "Update branch" or `@dependabot rebase` if an
   up-to-date branch is required.
5. **Approve Actions on #390** from the Actions tab — fork PR from a first-time contributor,
   still zero CI signal. It merges clean and passes the full suite locally (1650 passed). The
   `submission_types` nit should not block it; see the reframing above.
6. **Optional housekeeping:** delete the merged `audit/prompt-surface-2026-09-23` and
   `audit/untrusted-notice-wording` branches on `origin`, and consider a hardening issue for the
   three `get("submission_types", [])` sites.

*No comments, labels, merges, closes, approvals, or pushes to any non-triage branch were performed
by this executor. Files modified: `internal/issue-triage/2026-09-26.md` only.*
