# Daily Triage Brief — 2026-09-09

**GitHub read path:** GitHub MCP tools (`get_me`, `list_issues`, `list_pull_requests`, `issue_read`, `pull_request_read`).
**Cutoff:** 2026-09-08 — newest existing brief file was `internal/issue-triage/2026-09-08.md`. That file (plus its own executor addenda) already covers activity through 2026-09-08T02:33Z; this brief covers what happened on the remainder of 2026-09-08 through now.

## Needs your reply

None. All five currently-open issues (#157, #172, #236, #301, #302) had their most recent comment posted by `vishalsachdev` himself — a batch of "Triage decision" comments posted 2026-09-08 between 15:33 and 15:35 UTC, working through the backlog with root-cause pointers, draft tool surfaces, effort estimates, and next-step recommendations. No external commenter has touched any open issue since the 2026-09-05 cutoff used by the prior brief.

## PRs

### #360 — Protect shared-server exports and redact logged URL credentials
*Author: vishalsachdev, ready for review, not draft.*

- CI: all 16 checks green on head `8d87d4f` (test 3.11/3.12/3.13, lint, claude-review, CodeQL x2, SAST, secret scan, dependency scan, security test suite, PR-body check, Cursor agents neutral).
- `mergeable_state: behind` — main has advanced (the two dependabot batches plus PR #362) since this PR's base (`337b8ea`); no conflict reported, but the branch should be brought up to date before merging.
- No formal GitHub review has been submitted (`get_reviews` is empty) — only automated PR comments from Codex and Claude, both "no blocking issues" but both independently flagging the same thing carried over from the prior brief: this PR's own commit history contains a security-review report documenting several **still-unpatched** findings with file:line and exploit detail (specifics deliberately withheld from this tracked, public file — ask directly) — in tension with `SECURITY.md`'s instruction not to publicly disclose unpatched vulnerabilities. No comment from you on this PR addresses that question yet, so it's still open.
- ⚠️ High-sensitivity (FERPA-adjacent: peer-review reports, anonymization maps) — the code change itself is a restriction, not a new exposure, and both reviewers verified the guards fire before any Canvas fetch or file write.
- **Next action:** decide the disclosure question (patch the five findings, or formally accept and record the disclosure — "delete the file" is not a real fix once it's in PR/comment history); sync the branch with main; then get an approving review and merge.

### #359 — chore: remove unused code and dependencies
*Author: vishalsachdev, draft (from `chore/repository-simplification-audit`).*

- CI green on head `c401210`.
- `mergeable_state: dirty` — this PR now has a real merge conflict against current main (new since the prior brief; main has moved through two dependabot batches and PR #362 since this branch was opened).
- You already posted an owner comment (2026-09-08 15:31) confirming the `tsconfig.json` fix is verified independently (1,471 passed, `npm ci` 0 vulnerabilities, `npm run build` passed) and said explicitly the PR "should remain a draft until the author chooses to advance it" — so the prior brief's "consider taking it out of draft" question is settled; no action needed on that point.
- **Next action:** none until you choose to advance it; when you do, it will need a rebase/merge to resolve the conflict against main first (the tsconfig question itself is already closed out).

## New since 2026-09-08

- PR #362 "chore: triage fixes 2026-09-08" (vishalsachdev) — merged 2026-09-08 16:00. Fixed the intermittent `test_pii_access_logged` flake that the prior brief's addenda identified (scoped the assertion to the parsed `endpoint` field and pinned a timestamp containing the same digit sequences as the test IDs, instead of asserting against the whole raw log line).
- PRs #358, #357, #356, #355 (dependabot) — all merged 2026-09-08, closing out the routine bumps the prior brief flagged as ready.
- No new issues or PRs opened by anyone other than `vishalsachdev` since cutoff.

## Going stale

- **#172** (zqian, Canvas Quizzes) — last non-owner comment was jonespm on 2026-08-04; work is tracked-and-blocked on a New-Quizzes-enabled sandbox to verify detection logic, not neglected.
- **#301, #302** (rpsimon-ai, skill requests) — you asked follow-up questions on 2026-08-19 / 2026-08-21; no reply from the reporter since the issues were opened on 2026-08-18.
- **#236, #157** (your own backlog/roadmap items) — only your own routine "Triage decision" comment since filing; not waiting on anyone external, just unscheduled.
- (#299, turner-moore, cookie-auth — flagged stale in the prior brief — no longer appears in the open-issue list, so it looks to have been resolved/closed since then.)

## Checked, nothing needed

#157, #172, #236, #301, #302 — read full comment history on each; the latest comment on every one is from `vishalsachdev`, so none qualify for "needs your reply."

---

## Executor results (2026-09-09T02:18Z)

Ran against brief head `4523680`. Closing-keyword check clean on both the brief file and the
PR #363 body (`scripts/check_closing_keywords.py --strict`, exit 0 on each) — nothing to reword.

### Local verification runs

Both runs are on throwaway worktrees; nothing was pushed to either PR branch.

**PR #360 branch as-is (`8d87d4f`):**

```
1477 passed, 22 skipped, 1 warning in 30.43s     (uv run python -m pytest tests/ -q -rf, exit 0)
ruff check .  -> All checks passed!              (exit 0)
mypy src/canvas_mcp -> Success: no issues found in 50 source files
```

**PR #360 merged locally onto current `main` (`87f2e60`) — the state that would actually land:**

```
1477 passed, 22 skipped, 1 warning in 29.91s     (exit 0)
ruff check .  -> All checks passed!              (exit 0)
```

The single warning is pre-existing and unrelated to this PR (a `RuntimeWarning: coroutine
'AsyncMockMixin._execute_mock_call' was never awaited` raised from
`core/validation.py:381` by `tests/security/test_untrusted_content.py::TestRound11Surfaces::
test_bulk_grade_rejects_fenced_criterion_comment`); it appears identically on both trees.

**Not tested, and why:** PR #359 — it is a draft with a real conflict against `main`, so there is
no mergeable tree to test, and the owner has already said on 2026-09-08 to leave it as a draft.
PR #363 (this brief) is docs-only. No other PRs are open.

**Flake note:** `test_pii_access_logged` (the intermittent failure #362 addressed) passed on both
trees, including on the #360 branch, which predates #362's determinism fix. One clean pass is not
evidence the flake is gone — it was intermittent to begin with — it just did not reproduce here.

### Corrections and sharpenings to the brief

1. **#360's head commit is titled `[no-test] docs-only` — but the tests did run.** Worth stating
   because the message reads like a CI skip marker. Measured on the exact head sha `8d87d4f`: the
   "Run the test suite" step executed and succeeded in all three matrix jobs (`test (3.11)`,
   `test (3.12)`, `test (3.13)`), plus `lint` (ruff + mypy) and the `test-enhancements` aggregator.
   The marker is inert in `canvas-mcp-testing.yml`. CI green stands.
2. **"all 16 checks green" — partially confirmed, stated more precisely.** Only four *workflows*
   ran on this branch: Canvas MCP Enhancement Testing, Security Testing, Closing Keyword Guard,
   Claude Code Review — all `success` on `8d87d4f`. Both required checks (`test-enhancements`,
   `lint`) are green. The brief's 16 is presumably a check-*run* count including non-Actions app
   checks, which I did not enumerate; the CodeQL / SAST / secret-scan / dependency-scan entries it
   lists do not appear among this branch's workflow runs. Nothing here contradicts "green," but
   treat the 16-item list as unverified in its detail.
3. **#360 "behind, no conflict" — confirmed by measurement, not just by GitHub's label.**
   `git merge-tree --write-tree origin/main` against the PR's branch head exits 0 with no
   conflicts. `main` is 24 commits ahead of the PR's recorded base `337b8ea`.
4. **#359 "dirty" — the conflict is now localized.** `git merge-tree` against `origin/main` reports
   content conflicts in exactly two files: `package.json` and `package-lock.json`. That is #359's
   dependency removals meeting the dependabot npm bumps that landed on 2026-09-08 (tsx 4.23.13 via
   PR #355, `@types/node` 26.4.1 via PR #356). No Python source conflicts. Mechanical to resolve
   when the author advances it.
5. **#299 — the brief inferred it "looks to have been resolved/closed"; here is the measured fact.**
   Closed 2026-09-08T15:34:16Z by `vishalsachdev` with `state_reason: not_planned` and a `wontfix`
   label. It was declined, not resolved — the distinction matters if the reporter follows up.
6. **The #360 disclosure concern is real and I verified it directly.** The removed report is
   absent from the head tree but still resolves via a specific commit on the pushed branch (identifier
   deliberately not restated here), so it remains publicly fetchable and visible in the PR's
   commit view. Its status lines confirm the brief's count exactly: two findings fixed, and the
   remaining several **all carrying "recommendation; not implemented" or "not implemented"** —
   several unpatched findings published with file:line detail on a public repo. The brief's
   framing that deleting the file is not a fix is correct as measured.
7. **Open-issue set confirmed:** exactly #157, #172, #236, #301, #302 open; every one last updated
   2026-09-08 between 15:33 and 15:35 UTC by `vishalsachdev`. "No open issue needs a reply" holds.
8. **Merges confirmed:** #362, #358, #357, #356, #355 are all present in `main`'s history.
9. There is no brief file for 2026-09-06 or 2026-09-07; whether the routine ran on those days is
   unverified from here (a quiet-day exit produces no file, so absence is not evidence of a miss).

### Actions deliberately NOT taken

- **Did not push an "Update branch" merge to #360**, though it is behind and the brief asks for a
  sync. The executor's FERPA rule is read-only on anonymization/student-data/token territory, and
  this PR is squarely there (peer-review report exports, anonymization maps) — the brief flags it
  high-sensitivity itself. Nothing is lost by holding: I measured the merge is clean and the merged
  tree is green, which is everything the push would have told us. It is a one-click **Update
  branch** on the PR page when you want it.
- **No empty CI-retrigger commits.** Neither open PR is stalled or missing checks; every workflow
  on both has completed. The retrigger tool did not apply today.
- **Nothing touched on #359.** Resolving its conflict needs a non-empty commit to a branch I did
  not create, which is out of bounds, and you have already said to keep it a draft.

### Needs Vishal

1. **#360 — decide the security-disclosure question. This is the only real blocker and it is now
   two days old.** Several unpatched findings (specifics withheld from this tracked, public file —
   ask directly for detail) are publicly readable at a specific commit on the pushed branch, with
   file:line and exploit detail, against `SECURITY.md`'s own instruction not to publicly disclose
   unpatched vulnerabilities. Three routes: patch the remaining findings, formally accept and
   record the disclosure, or rewrite the branch history — and history rewriting is yours alone to
   do, both because it is a force-push and because the content is FERPA-adjacent. Some of the
   remaining findings are themselves in the read-only territory, so I cannot patch them either.
2. **#360 — needs an approving review and a merge decision.** No formal review has ever been
   submitted (the reviews list is empty; the Codex and Claude comments are automated). Merge is
   yours; I may only merge the triage brief PR. Tests and lint are green on the merged tree.
3. **#360 — sync the branch** whenever you merge (see above; clean, one click).
4. **#359 — no action needed** until you advance it; the conflict is confined to `package.json` /
   `package-lock.json` when you do.
5. **Optional housekeeping:** the `[no-test]` prefix on #360's head commit does not skip anything
   in this repo's workflows. If it is meant to, it is not wired up; if it is not meant to, the
   convention is misleading in exactly the place where a reader most wants to trust CI.
