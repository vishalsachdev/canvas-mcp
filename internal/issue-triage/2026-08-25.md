# Triage brief — 2026-08-25

**Read path used:** GitHub MCP tools (`mcp__github__*`).
**Cutoff:** 2026-08-24 (`internal/issue-triage/2026-08-24.md` is the newest prior brief). That
brief's own triage PR (#321) merged to `main` at `2026-08-24T01:53:26Z` (`8cc62e0`) — using that
merge as the effective boundary for "new since cutoff."

Category 1 (needs-reply) is empty: `list_issues` sorted by `updated_at` shows the most recently
touched open issue at `2026-08-21T20:08:37Z` (#318) — nothing has moved since well before the
cutoff. Category 3 is **not** empty: dependabot opened PR #322 at `2026-08-24T04:44:34Z`, after
the #321 merge boundary. That alone rules out the Step 4 early exit, so this is a status-plus-one
brief.

Also worth noting: `main` advanced twice today by direct push from `vishalsachdev` (commits
`2e8146b` and `c88613f`, both docs-only — the adoption-numbers caveat and the Illinois API-token
instructions correction) rather than through a PR. That's why every open same-repo PR below now
shows one or two commits behind `main`; it is routine drift, not a new problem.

## Needs your reply

None. No open issue has a comment after the cutoff from anyone other than `vishalsachdev`.

## PRs

- **#322 — `chore(deps): bump the actions group across 1 directory with 6 updates`** (dependabot,
  new since cutoff) — **17 checks run, 16/17 green**, including both required checks
  (`test-enhancements`, `lint`). The one failure is `claude-review`, which is not a required check
  on this repo (dropped in #188 because it can't run with secrets on some PR types) — its failure
  here doesn't block anything. No reviews posted. `mergeable_state: behind` (base `8cc62e0`, current
  `main` `c88613f`) — two commits behind from today's direct docs pushes, not from a real conflict.
  Same shape as the #308/#295/#296/#297 pattern below: green, unreviewed, needs a routine
  update-branch-then-merge pass.
- **#308, #295, #296, #297** — unchanged from yesterday's brief: all four still **17/17 green**
  (`claude-review` included, unlike #322), still no reviews posted. Now **two** commits behind
  `main` (same direct-push drift as #322). Green-but-unreviewed, routine re-sync-and-merge, not a
  new problem. (#298, the sibling dependabot Actions-bump PR from before the group update, was
  auto-closed by dependabot — superseded by #322 — not merged; no action needed there.)
- **#317 — `fix(security): run execute_typescript sandbox as non-root user`** (AmirF194, ready,
  not draft) — head `277ef8e`, unchanged since 2026-08-21 (4 days). Still **zero CI checks
  reported** — fork PR from a first-time contributor, so this remains "not yet approved to run,"
  not "failing." Next action is still yours: approve the pending Actions run from the Actions tab.
- **#191 — `feat: Add dual-engine read tools for Canvas Quizzes (Classic and New)`** (Copilot,
  draft, targets #172) — unchanged since 2026-08-14 (11 days), same head (`9e2d146`).
  `mergeable_state: dirty` per the last live read (not re-verified today; nothing suggests it
  changed — same head). PR body still carries a live closing-keyword line before the reference to
  issue 172 that would need rewording before any merge. Standing blockers unchanged: (1) the
  non-mechanical conflict, (2) the closing-keyword line, (3) still needs a New-Quizzes-enabled
  sandbox to verify `is_quiz_assignment AND external_tool`. This is the item Current Focus flags as
  needing a decision.

## New since 2026-08-24

- **#322** — dependabot grouped GitHub Actions bump (6 updates), opened `2026-08-24T04:44:34Z`.
  See PRs section above.

## Going stale

- **#172** (zqian, New Quizzes) — last substantive activity 2026-08-07 (18 days). Tracked above
  under PR #191, which is the actual blocker.
- **#142** (watch item, `mcp<2` pin) — last activity 2026-07-30 (26 days). Blocked on an upstream
  fastmcp release; no action available until that happens.
- **#301** (rpsimon-ai, assignment-group skill request) — you replied 2026-08-19; no response from
  rpsimon-ai since their 2026-08-18 filing (7 days).
- **#302** (rpsimon-ai, student-analytics skill request) — you replied 2026-08-21; no response
  since their 2026-08-18 filing (7 days).
- **#299** (turner-moore, cookie-auth feature) — you replied 2026-08-19; no response since their
  2026-08-17 filing (8 days).

## Checked, nothing needed

- **#318** — last activity is `vishalsachdev`'s own correction commits (2026-08-22); zqian hasn't
  responded yet (4 days, not yet stale).
- **#309** — last comment is your own reply (2026-08-21) with a v1 design and two scoping
  questions; waiting on zqian (still under the stale threshold).
- **#315** — last comment is `claude[bot]`'s automated labeling explanation; not a request.
- **#283** — last comment (2026-08-21T16:02:44Z, khagyard's confirmation) predates the cutoff; no
  new activity to act on.
- **#236, #157** — no activity since the last several briefs; already covered in Current Focus or
  genuinely blocked on external input (admin access, sandbox access).

## Executor results (2026-08-25T01:48:40Z)

Closing-keyword check on the brief and PR #323 body: `scripts/check_closing_keywords.py --strict`
exit 0, plus an independent manual grep — clean, nothing to reword.

### Branch updates performed

Updated all five green-but-unreviewed same-repo PRs onto current `main` (`c88613f`) via
`update_pull_request_branch`. All five succeeded and are now **0 commits behind**:

| PR | old head | new head | files vs `main` |
|----|----------|----------|-----------------|
| #322 | `81a5933` | `ec65b1b` | 6 workflow files |
| #308 | `d063c28` | `3e98f1e` | `docs/data/impact.json` |
| #297 | `1ac01e7` | `454a346` | `package.json`, `package-lock.json` |
| #296 | `77ab1cc` | `6ab40ee` | `package.json`, `package-lock.json` |
| #295 | `3f84664` | `73cf3be` | `Dockerfile` |

No empty retrigger commits were needed — every branch picked up CI on the merge commit by itself.

### Local test results

Environment: CPython 3.11.15, `uv run python -m pytest tests/ -q` and `uv run ruff check .`,
each run on the branch's post-update head.

| Branch | pytest | ruff |
|--------|--------|------|
| `triage/2026-08-25` (baseline, `60a5b0d`) | 1397 passed, 22 skipped, 2 warnings | All checks passed! |
| #322 `ec65b1b` | 1397 passed, 22 skipped, 2 warnings | All checks passed! |
| #308 `3e98f1e` | 1397 passed, 22 skipped, 2 warnings | All checks passed! |
| #297 `454a346` | 1397 passed, 22 skipped, 2 warnings | All checks passed! |
| #296 `6ab40ee` | 1397 passed, 22 skipped, 2 warnings | All checks passed! |
| #295 `73cf3be` | 1397 passed, 22 skipped, 2 warnings | All checks passed! |

Zero failures anywhere; identical counts to baseline, which is expected — none of the five diffs
touches Python source, so the suite is a regression floor here, not evidence about the change
itself. The two warnings are pre-existing on `main` (an unawaited-coroutine `RuntimeWarning` in
`tests/security/test_untrusted_content.py` and a Starlette `httpx` deprecation in
`tests/test_fastmcp_compat.py`), not introduced by any of these PRs.

**Not tested locally, and why:** #317 (fork branch — out of bounds for this executor) and #191
(draft, and it does not merge cleanly with `main`, so there is no meaningful head to test).

### GitHub CI on the updated heads

Re-read after the updates landed. All five are now **17/17 with no failing check** (15 success,
1 skipped `Commits pushed to main`, 2 neutral Cursor agents — the neutral pair is the steady-state
result on this repo, not a problem). Both required checks (`test-enhancements`, `lint`) are green
on all five.

### Corrections to the brief

1. **Behind-count was understated for four PRs.** The brief says #308/#295/#296/#297 were "two
   commits behind `main`." They were based at `87cf671`, which is **five** commits behind
   `c88613f` (`56d02eb`, `d94db24`, `8cc62e0`, `2e8146b`, `c88613f`) — the two direct docs pushes
   plus the whole of the #321 triage merge. Only #322 (base `8cc62e0`) was genuinely two behind.
   Immaterial to the outcome, but the drift was older than "today's pushes."
2. **#322's `claude-review` failure is superseded — it now passes.** The brief recorded 16/17 with
   `claude-review` failing, and correctly noted it is not a required check. On the updated head it
   returns **success**, so #322 is 17/17 like the other four. The distinction the brief drew
   between #322 and #308/#295/#296/#297 ("`claude-review` included, unlike #322") no longer holds.
3. **#191's conflict is now measured, not inferred.** The brief flagged `mergeable_state: dirty`
   as "not re-verified today"; the live API currently reports `unknown`. Test-merged `origin/main`
   into the #191 head locally (read-only, aborted): the merge fails with exactly **one conflicted
   file, `src/canvas_mcp/tools/assignments.py`, one conflict hunk**. Merge base is `5652175`.
   Consistent with the standing "non-mechanical conflict in `assignments.py`" note.
4. **#191's PR body still carries a live closing-keyword line** before the reference to issue 172
   (verified in the body text today, unchanged). Merging it as written would auto-close that issue
   — the failure mode that has already hit issue 172 twice. Rewording it needs your hands: this
   executor may not edit a PR it did not open.
5. **#317 confirmed at zero checks.** `get_check_runs` returns `total_count: 0`. Fork branch, so
   this is "awaiting your approval in the Actions tab," not a failure — and out of bounds for
   automated retrigger, as the brief said.

### Flagged for judgment, not acted on

- **#308 publishes PyPI download counts to the public site.** Its diff writes
  `docs/data/impact.json`, which lives under the Cloudflare Pages publish root, and the file
  carries `pypi.last_month: 2208` plus a `pypi_month` series in `history`. That is the exact figure
  class the 2026-08-21 note in `CLAUDE.md` says never to quote, on the grounds that it swings by an
  order of magnitude month to month. The impact tracker predates that note and this is its normal
  behavior, so this is a policy question, not a regression — but the caveat and the pipeline now
  point in opposite directions, and it is worth deciding once which wins.
- **#308's data is stale on arrival.** It is a `2026-08-17` snapshot (194 stars / 65 forks /
  19 contributors). `CLAUDE.md` already records 198 / 67 as of 2026-08-20, so merging it publishes
  numbers that are known-superseded on the day they ship. Re-running the collector before merge
  would land current figures instead.
- **#322 leaves one stale pin comment.** The `trufflesecurity/trufflehog` step moves from
  `a7082b6` to `3ab759f` but keeps the trailing `# main@2026-08-10` comment, so the comment now
  misdescribes the pinned commit. Cosmetic, and it is dependabot's output rather than anything a
  reviewer wrote; every other bump in the PR keeps its SHA pin with an accurate version comment.

### Needs Vishal

1. **Merge decision on five green PRs** — #322, #308, #297, #296, #295. All re-synced to `main`,
   all 17/17 in CI, all clean locally on pytest and ruff. None has a review; merging is a human
   action this executor may not take. Read the #308 flags above before merging that one.
2. **#317 — approve the pending Actions run** from the Actions tab. Fork PR from a first-time
   contributor, still zero checks after 4 days. Nothing can move until you approve the run.
3. **#191 / issue 172 — the decision is now 11 days overdue** and `CLAUDE.md` has flagged it as
   "decide this week" for over three weeks. Three blockers stand, and two of them are yours alone:
   reword the closing-keyword line in the PR body, and resolve the one-hunk `assignments.py`
   conflict. The third is unchanged and external — verifying `is_quiz_assignment AND external_tool`
   needs a New-Quizzes-enabled sandbox, which means sourcing one from zqian or closing the PR
   honestly as unverifiable.
4. **Nothing to reply to.** Category 1 was empty and this executor confirmed no draft replies were
   pending in the brief, so no drafts are being held for your review today.

### Boundary notes

Actions deliberately not taken, per the executor's standing limits: no comment, label, close,
approval, or merge on any issue or PR other than triage PR #323; no push to any branch other than
`triage/2026-08-25`; no edit to any file outside `internal/issue-triage/`; no touch to the #317
fork branch. `git fetch --unshallow` was run on the local clone to make the #191 merge-base
computation possible — a local-only operation with no remote effect.
