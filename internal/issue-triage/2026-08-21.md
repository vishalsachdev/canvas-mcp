# Triage brief — 2026-08-21

**Read path used:** GitHub MCP tools (`mcp__github__*`).
**Cutoff:** 2026-08-20 (`internal/issue-triage/2026-08-20.md` is the newest prior brief, committed
2026-08-20T01:55:26Z — using that commit timestamp as the effective boundary since the brief's own
"Executor results" section already covers activity up through 2026-08-20T01:52Z).

Category 3 is non-empty (one new issue) and category 1 is non-empty (two issues have a
non-Vishal comment after the cutoff), so proceeding past early exit.

## Needs your reply

**#275 — "[Bug]: peer review tools still not working"** (khagyard, 10 comments) — **good news,
closeable.** khagyard replied 2026-08-20T12:49:39Z: *"The fix worked thank you!"* — confirming
PR #288's Planner-feed discovery path (merged, shipped in v1.10.0) actually surfaces a student's
pending peer review. This closes out the multi-week thread (root cause: the assignment-scoped
`peer_reviews` endpoint is instructor-focused; the student UI itself reads `/api/v1/planner/items`,
which is what #288 added as a second discovery source).

Draft reply:
```
That's great to hear, thanks for sticking with the back-and-forth on this one — your test
payload back in August was what let us confirm the Planner-feed field shapes before merging,
so this was very much a team effort. Closing this out since the fix is confirmed working
end-to-end against production Canvas. If you hit another peer-review edge case, feel free to
open a new issue.
```

---

**#313 — "[Feature]: add configuration to limit # of tools based on Canvas user role"**
(zqian, University of Michigan — high-priority external collaborator, owns #170/#172/#181;
1 comment from jonespm) — opened 2026-08-20T16:02:13Z, jonespm replied 2026-08-20T17:34:58Z.

zqian's problem: Azure's platform caps combined MCP tools at 128, and they need to run Canvas
MCP alongside other connectors, so they're asking for a `CANVAS_USER_ROLE` env var that filters
the tool list to student-facing or teacher-facing tools only. jonespm's reply raises a real
design wrinkle: role can be context-dependent (instructor in one course, student in another),
and a static role filter risks silently hiding tools a user actually needs; jonespm suggests
category-based grouping (pointing at `docs/llms.txt`) with a discovery mechanism that expands
tool groups by category instead.

**Checked the repo — this is already implemented and shipped.** `CANVAS_ROLE` exists today
(`src/canvas_mcp/core/config.py:277`, valid values `student`/`educator`/`all`) and gates tool
registration in `register_all_tools()` (`src/canvas_mcp/server.py:424-472`): student tools only
register when `role in ("student", "all")` (:447-451), educator tools only when
`role in ("educator", "all")` (:454-469). Documented in `AGENTS.md:31-36` with tool counts:
`CANVAS_ROLE=student` → ~37 tools, `CANVAS_ROLE=educator` → ~88 tools, `CANVAS_ROLE=all`
(default) → 94-99. Also settable per-invocation via `canvas-mcp-server --role student` (CLI
flag takes precedence over the env var). Both numbers comfortably clear the 128 combined-tool
cap zqian is hitting, even stacked with a few other connectors.

**Not fully solved:** jonespm's context-dependent-role point doesn't have an answer today — role
is fixed for the life of the server process (env var or CLI flag at startup), not adjustable
per-request or per-course. That matters more for the hosted/HTTP deployment (where a single
running instance serves many callers) than for the stdio/local case zqian's Azure combined-tools
problem sounds like it's describing.

Draft reply:
```
Good news: this already exists, and I think it solves your immediate 128-tool-cap problem as-is.
Set CANVAS_ROLE=student or CANVAS_ROLE=educator (env var, or --role on the CLI, which takes
precedence) and the server only registers that profile's tools at startup — about 37 for
student, 88 for educator, versus 94-99 unfiltered. Details in AGENTS.md. Can you try that and
let me know if it gets you under the combined 128-tool limit with your other connectors?

@jonespm's point is a real gap though: CANVAS_ROLE is fixed for the life of the process, so it
doesn't help if the same running instance needs to serve both an instructor-in-course-A and a
student-in-course-B. That's a different, harder problem than zqian's immediate ask (a static
per-deployment cap), and category-based dynamic discovery is one reasonable direction for it —
but I'd rather not conflate the two in one issue. zqian, if the static CANVAS_ROLE split doesn't
actually solve what you're running into, let me know what your deployment shape looks like
(one server instance per user? shared?) and I'll reopen the scoping question.
```

## PRs

- **#308 — `chore: refresh impact stats`** (vishalsachdev, ready, not draft) — all **17/17 checks
  green**, including `claude-review` (passes cleanly on own branch, not a fork). `mergeable_state`
  now reads **`behind`** (was `unknown` in the last brief) — main has moved since this branch was
  cut, but the diff is still a single `docs/data/impact.json` file, so a conflict is unlikely.
  Last brief's pypistats concern (`last_month` 7426→2208) was independently re-verified against
  live pypistats.org data and resolved as a false alarm (rolling-window aging, not a data bug) —
  nothing outstanding here. Ready to merge; optionally rebase first for a clean CI run against
  current `main`.
- **#295, #296, #297, #298 (dependabot)** — unchanged since the last brief (same heads, same
  2026-08-17T04:34-04:41 UTC check runs). All show **16/17 green**, only `claude-review` red
  (secrets withheld from dependabot-triggered runs — not a required check, per #188). All four
  read `mergeable_state: behind`. Per the prior brief's measurement, `main` has since picked up
  +2418/−338 across 51 files including real source changes (`core/tool_results.py`,
  `core/untrusted_content.py`, `server.py`, etc.), not just docs — so these green runs predate a
  meaningfully different `main` and have not been re-tested against it. Forcing a fresh run needs
  `update_pull_request_branch`, which is outside this routine's write boundary (branch-mutation on
  a PR this routine did not open). Note for whichever of #296/#297 merges second: both touch
  `package.json` and will transiently conflict until dependabot rebases.
- **#191 — `feat: Add dual-engine read tools for Canvas Quizzes (Classic and New)`** (Copilot,
  draft, targets #172) — **unchanged for a ninth cycle.** Same head (`9e2d146`),
  `mergeable_state: dirty`, zero CI checks (a `pull_request` workflow can't evaluate a merge ref
  an un-mergeable PR doesn't have), body still ends with a live `Fixes` line pointing at
  [issue 172] that would auto-close it on merge. Same three blockers as every prior brief: (1) the
  one-file conflict in `assignments.py` (non-mechanical), (2) reword the closing line to
  `Addresses` + the same reference, (3) still needs a New-Quizzes sandbox before the correctness
  bug (`is_quiz_assignment AND external_tool` measured live as a *Classic*-quiz signal, not New) can
  be fixed.

## New since 2026-08-20

- **#313** — see "Needs your reply" above (zqian + jonespm).

No other issues or PRs were opened since the cutoff.

## Going stale

- **#172** (zqian/jonespm, quizzes): last external touch was jonespm's 2026-08-04 comment — now
  17 days waiting on New-Quizzes sandbox access, which is also what's blocking #191.
- **#142** (Ash, `blocked-upstream` watch item): last external touch from `ashcastelinocs124` was
  2026-07-01. Deliberately a no-action watch item since 2026-07-30 — flagged for awareness only.

## Checked, nothing needed

#309 (zqian's content-migration request — you are not the most recent commenter, but no new
activity since the 2026-08-20 brief either; its draft reply is still unsent and still accurate,
carry it forward), #301, #299 (you replied 2026-08-19 with scoping questions for
`rpsimon-ai`/`turner-moore`; both await their response, not yours), #236 (self-filed; only
activity is automated labeling, no external ask), #157 (self-filed, no new activity), #293
(automated weekly-maintenance report, opened 2026-08-16 — not duplicated per standing
instructions).

**#302** ("[Skill Request] ... Assignment Groups and Weights", `rpsimon-ai`, first-time
contributor) — still has **zero comments**, opened 2026-08-18T16:10:15Z, so it doesn't strictly
meet today's "new comment since cutoff" or "5+ days stale" thresholds (it's ~3 days idle). But
this is now the third consecutive brief noting it's unanswered — the draft from the 2026-08-18
brief is still sitting unsent. Flagging again so it doesn't fall through the cracks; it will hit
the formal "going stale" threshold in ~2 days if still untouched.
