# Triage brief — 2026-09-11

Read path: GitHub MCP tools. Cutoff: **2026-09-10** (newest prior brief file in this directory).

**Summary: no new activity since the cutoff.** Every open issue, and both open PRs, are in exactly
the state the 2026-09-10 brief (and its executor addendum) already documented — same comment
counts, same head SHAs, same `mergeable_state`, no new reviews. This brief exists only because that
prior brief's PR problems (green-but-unreviewed, non-clean mergeable state) are still open and
Step 4 of this routine doesn't early-exit while a category-2 problem persists. See
`internal/issue-triage/2026-09-10.md` for the full analysis — it is not repeated here.

## Needs your reply

None. No open issue has a comment newer than 2026-09-10, and none from anyone other than you.

## PRs

Both PRs are unchanged from yesterday — same heads, same problems, still waiting on you:

### #359 — `chore: remove unused code and dependencies` (draft)
- Head `c401210`, still `mergeable_state: dirty` (conflict confined to `package.json` /
  `package-lock.json` against dependabot's npm bumps on `main` — confirmed in the 2026-09-10
  executor pass). CI green. No new comments since your 2026-09-08 owner review comment.
- **Next action:** unchanged — resolve the two-file conflict against `main` if/when you take it out
  of draft. No urgency; you've said to leave it draft.

### #360 — `Protect shared-server exports and redact logged URL credentials` (ready for review)
- Head `8d87d4f`, still `mergeable_state: behind` (confirmed 0-conflict merge against `main`,
  clean update available in one click). CI green, no formal review submitted (reviews list is
  still empty — only bot/Codex comments, both "no blocking issues").
- ⚠️ **High-sensitivity, still open, now 4 days old:** the branch's history still contains commit
  `bffebbf` (`REVIEW-2026-09-06.md`), which publicly documents five still-unpatched security
  findings (file:line + exploit detail) in tension with `SECURITY.md`. Nothing about this has
  changed since 2026-09-10 — the file is still fetchable at that SHA, the five findings are still
  unpatched on `main`, and a history rewrite still would not retract it (already explained in
  detail in the 2026-09-10 brief's executor section — worth a re-read if it's been a few days).
- **Next action:** unchanged — this is the one item on your desk with a clock on it. Decide the
  disclosure question (recommended: treat as disclosed, open a private GitHub Security Advisory,
  schedule the five fixes, add a root-level ignore rule for future `REVIEW-*.md` files), then
  branch-update + merge.

## New since 2026-09-10

None. No new issues, no new PRs, from anyone.

## Going stale

- **#172** (Canvas Quizzes, zqian) — still blocked on New-Quizzes-sandbox access; no external touch
  since jonespm on 2026-08-04 (zqian's own last input 2026-07-29). No change.
- **#301** (assignment-group templates skill, rpsimon-ai) — no reply since your 2026-08-19 questions.
- **#302** (student-risk analysis skill, rpsimon-ai) — no reply since your 2026-08-21 questions.

## Checked, nothing needed

#157 (sandbox egress hardening — your own backlog item, no external party waiting), #236 (OAuth2
developer-key flow — your own backlog item, blocked on an admin pilot, no pending external
response).

---

## Executor results (2026-09-11T02:18:55Z)

Closing-keyword check: `scripts/check_closing_keywords.py --strict` run over the brief file and the
PR #365 body — exit 0 on both, nothing to reword.

**The brief's headline claim is correct: nothing moved.** Independently confirmed below, so this
addendum is short by design. Where a fact was already established in the 2026-09-10 executor
section and nothing has changed, it is not re-derived here.

### Verified

- **Nothing landed on `main` since the last pass.** `a8acd4d..d749e80` is three commits, all from
  the 2026-09-10 triage PR, touching exactly one file (`internal/issue-triage/2026-09-10.md`). No
  source code changed, so every code-level finding recorded yesterday still holds by construction —
  including the state of the five unpatched items behind the #360 disclosure question.
- **Both PR heads unchanged.** #360 `8d87d4f`, #359 `c401210` — same SHAs as yesterday.
- **#360 is still behind-but-clean.** Now **31** commits behind `origin/main` (was 28);
  `git merge-tree --write-tree` against `origin/main` still exits **0 — no conflicts**. The branch
  update remains mechanical.
- **#359's conflict is still the same two files.** `git merge-tree` exits 1 with conflicts confined
  to `package.json` and `package-lock.json`. No source conflict.
- **No new review, no new comment on #360.** Reviews list is still empty; the three comments are all
  bots, newest `2026-09-08T00:08:53Z`.
- **"Needs your reply: none" confirmed.** All five open issues (#157, #172, #236, #301, #302) still
  show `updated_at` of 2026-09-08, every one from the triage pass itself.

### One thing worth checking that turned out fine

#360's head commit is titled **`[no-test] docs-only`**, which reads like a CI-skip marker — i.e. a
risk that "CI green" on that head meant "CI didn't run". It does not. I pulled the jobs for run
`34172120157`: `test (3.11)`, `test (3.12)`, `test (3.13)` each executed the **Run the test suite**
step (20–33s apiece), plus `lint` (ruff + mypy) and the `test-enhancements` aggregator — all
`success`. `canvas-mcp-testing.yml` contains no commit-message skip condition. The marker is inert
prose; the green is a real, full-matrix green.

### Local test + lint runs

Both PR heads checked out read-only in scratch worktrees and run in full:

| PR | head | `pytest tests/ -q -rf` | `ruff check .` |
|----|------|------------------------|----------------|
| #360 | `8d87d4f` | **1477 passed, 22 skipped**, 1 warning, 36.92s | All checks passed |
| #359 | `c401210` | **1470 passed, 22 skipped**, 1 warning, 35.77s | All checks passed |

Identical to yesterday's counts, to the test. The single warning on both is the known pre-existing
un-awaited-mock `RuntimeWarning` from `tests/security/test_untrusted_content.py` via
`core/validation.py:381` — not a regression from either branch. Only #360 is non-draft, so it is the
only PR the "green but unreviewed" rule required me to run; #359 was run anyway, since it costs 35
seconds and keeps the comparison honest. Those two are the only open non-triage PRs — nothing was
left untested. No fork branches touched. Nothing was stalled or missing checks, so no retrigger
commits were pushed.

### Addition to yesterday's disclosure finding

Yesterday's section treated the #360 exposure as the commit blob at `bffebbf`. That is still true —
the file is gone from the branch tip but reachable at that SHA — but it is not the whole surface:
the automated review bot's **public PR comment** on #360 (posted 2026-09-07, still visible on the
PR) independently restates the same five items, by weakness class and source file, as its own
top finding. So the content is public in two places, one of which no git operation touches at all.

This does not change the recommended response, it strengthens it: **treat as disclosed** and work
the fixes, rather than spending any effort on making the artifact disappear. One practical
consequence — if GitHub Support is ever asked to purge the cached commit view (step 3 of yesterday's
list), that request needs to cover the comment too, or it accomplishes nothing.

Consistent with yesterday, I have enumerated nothing here; the detail is in the report and the PR
comment, both of which you can reach.

### Not done, and why

- **#360 branch update — held again**, for the same reason as yesterday and no new one: it is
  conflict-free and inside my tier, but the disclosure decision sits upstream of it and the update
  buys nothing while the PR waits on your judgment. It stays a one-click operation; drift is growing
  slowly (28 → 31 commits) with zero conflicts, so there is no deadline pressure to update it early.
- **#359 conflict — still outside my boundary** (a non-empty commit to a branch I did not create),
  and it is a draft you asked to stay a draft.
- **Yesterday's recommendation #4 is still open**: `.gitignore` on `main` has no root-level
  `REVIEW-*.md` rule (checked; the only review-adjacent line is the
  `!internal/architecture-review.md` un-ignore). Adding it is a one-line edit outside
  `internal/issue-triage/`, so it is yours, not mine.

### Needs Vishal

1. **The #360 disclosure decision** — unchanged and now 5 days old; the only item here with a clock.
   Recommended: treat as disclosed, open a private advisory, schedule the fixes. Note the new
   detail above: the public PR comment carries the same content, so no git-side remedy retracts it.
2. **#360 merge decision** — green (verified full-matrix, not a skipped green), locally 1477/1477,
   0 conflicts, 31 behind. Branch update + merge are both outside my tier.
3. **#359** — the `package.json` / `package-lock.json` conflict (dependency drift only), whenever it
   leaves draft.
4. **One-line hardening** — add a root-level `REVIEW-*.md` ignore rule so the next review report
   cannot land outside `internal/` by accident.
5. **Stale, no action available to me:** #172 (New-Quizzes sandbox), #301, #302 (both awaiting
   rpsimon-ai since 08-19 / 08-21). Each needs a public nudge, which I may not post.
