# Triage brief — 2026-09-11

Read path: GitHub MCP tools. Cutoff: **2026-09-10** (newest prior brief file in this directory).

**Summary: no new activity since the cutoff.** Every open issue, and both open PRs, are in exactly
the state the 2026-09-10 brief (and its executor addendum) already documented — same comment
counts, same head SHAs, same `mergeable_state`, no new reviews. This brief exists only because that
prior brief's PR problems (green-but-unreviewed, non-clean mergeable state) are still open and
Step 4 of this routine doesn't early-exit while a category-2 problem persists. See
`internal/issue-triage/2026-09-10.md` for the full analysis — it is not repeated here.

## Needs your reply

None. No open issue has a comment newer than 2026-09-10, and none from anyone other than you.

## PRs

Both PRs are unchanged from yesterday — same heads, same problems, still waiting on you:

### #359 — `chore: remove unused code and dependencies` (draft)
- Head `c401210`, still `mergeable_state: dirty` (conflict confined to `package.json` /
  `package-lock.json` against dependabot's npm bumps on `main` — confirmed in the 2026-09-10
  executor pass). CI green. No new comments since your 2026-09-08 owner review comment.
- **Next action:** unchanged — resolve the two-file conflict against `main` if/when you take it out
  of draft. No urgency; you've said to leave it draft.

### #360 — `Protect shared-server exports and redact logged URL credentials` (ready for review)
- Head `8d87d4f`, still `mergeable_state: behind` (confirmed 0-conflict merge against `main`,
  clean update available in one click). CI green, no formal review submitted (reviews list is
  still empty — only bot/Codex comments, both "no blocking issues").
- ⚠️ **High-sensitivity, still open, now 4 days old:** the branch's history still contains commit
  `bffebbf` (`REVIEW-2026-09-06.md`), which publicly documents five still-unpatched security
  findings (file:line + exploit detail) in tension with `SECURITY.md`. Nothing about this has
  changed since 2026-09-10 — the file is still fetchable at that SHA, the five findings are still
  unpatched on `main`, and a history rewrite still would not retract it (already explained in
  detail in the 2026-09-10 brief's executor section — worth a re-read if it's been a few days).
- **Next action:** unchanged — this is the one item on your desk with a clock on it. Decide the
  disclosure question (recommended: treat as disclosed, open a private GitHub Security Advisory,
  schedule the five fixes, add a root-level ignore rule for future `REVIEW-*.md` files), then
  branch-update + merge.

## New since 2026-09-10

None. No new issues, no new PRs, from anyone.

## Going stale

- **#172** (Canvas Quizzes, zqian) — still blocked on New-Quizzes-sandbox access; no external touch
  since jonespm on 2026-08-04 (zqian's own last input 2026-07-29). No change.
- **#301** (assignment-group templates skill, rpsimon-ai) — no reply since your 2026-08-19 questions.
- **#302** (student-risk analysis skill, rpsimon-ai) — no reply since your 2026-08-21 questions.

## Checked, nothing needed

#157 (sandbox egress hardening — your own backlog item, no external party waiting), #236 (OAuth2
developer-key flow — your own backlog item, blocked on an admin pilot, no pending external
response).
