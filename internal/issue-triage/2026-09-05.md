# Triage brief — 2026-09-05

**Read path used:** GitHub MCP tools (`mcp__github__*`). `gh` CLI not installed in this sandbox
(expected per routine instructions); MCP access worked on the first call.

**Cutoff:** 2026-09-04T01:46:27Z — the merge timestamp of PR #350 (the 2026-09-04 triage brief),
continuing the "previous triage PR's merge time" convention used the last several briefs.
Confirmed via `search_issues(updated:>=2026-09-03)` (a superset of the actual cutoff): **0**
results — no issue activity anywhere since well before the cutoff.

## Needs your reply

None. No open issue has a comment newer than the cutoff from someone other than you.

## PRs

**#351 — `fix: decode code_api TypeScript files as UTF-8 in search_canvas_tools`** (bruchris,
fork PR, not draft) — **new since the last brief**, see below.

**#340 — `fix(security): make sandbox uid:gid configurable via TS_SANDBOX_UID_GID (#338)`**
(k0shir0, fork PR, not draft). Sixth brief in a row carrying this, still unchanged:

- Still **`total_count: 0` check runs** — Actions withheld on this fork branch pending your
  approval, unchanged since PR creation (2026-08-31).
- Head unchanged: `f8cba185b8e5ce5ddfdc22e93c616c42870d8d20`. `mergeable_state: behind` (nominal
  staleness against disjoint files, not a conflict — established in prior briefs).
- AmirF194's review thread (root-uid-0 finding) still unresolved; the leading-zero gap
  (`00:00`/`00:65532` slip past the string comparison) is still unaddressed.
- **Next action:** unchanged — approve the withheld workflow run; draft reply carried forward
  from the last brief (raises the leading-zero gap, credits the contributor for implementing
  AmirF194's suggestion exactly as given):

```
Thanks both — AmirF194's repro was the clincher, and k0shir0's f8cba18 implements the suggested
guard exactly as written, with a test. One gap that's in the suggestion rather than the
implementation: the comparison is on strings, so leading-zero spellings still slip through
(00:00 / 00:65532 pass the ^\d+:\d+$ check and skip the uid==0 check). Could you switch to a
numeric comparison (int(uid) == 0 or int(gid) == 0) and add a leading-zero test? Approving the
workflow run now so CI runs either way.
```

### #351 in detail (new)

Opened 2026-09-04T21:55:05Z by **bruchris**, who says in the PR body they maintain a different
Canvas LMS MCP server and found this while reading our code-API discovery path — appears to be a
first-time contributor here. Small, well-scoped, high-confidence fix:

- **Bug:** `search_canvas_tools` (`src/canvas_mcp/tools/discovery.py:130,145,162`, three call
  sites) reads bundled `code_api/**/*.ts` files with `Path.read_text()` and no explicit encoding,
  so decoding falls back to the host locale. Two files (`grading/bulkGrade.ts`,
  `discussions/bulkGradeDiscussion.ts`) contain `✓`/`✗` in console-output strings; on a
  non-cp1252/non-UTF-8 Windows code page (their repro table lists cp932 Japanese, cp936
  Simplified Chinese, cp1253 Greek) that raises `UnicodeDecodeError`. The content-search branch
  swallows it in a bare `except Exception: continue`, so the two files silently vanish from
  results with no error surfaced — `entries_with_error` stays `0`.
- **Fix:** pins `encoding="utf-8"` on all three reads (the files are UTF-8 on disk; this is a
  one-word-per-call-site change, +64/-3 across 2 files). Confirmed `execute_typescript` itself is
  unaffected — it runs the files through Node, not this Python read path.
- **Tests:** two new tests in `tests/tools/test_discovery.py` with a `monkeypatch` fixture that
  makes encoding-less reads resolve to cp932, so the tests actually exercise the bug on UTF-8 CI
  (verified both ways: `2 failed, 13 passed` before the fix, `15 passed` after — the PR body
  states this explicitly).
- I read the diff directly rather than only the description — the fix is exactly what the PR body
  claims: three `read_text()` calls gain `encoding="utf-8"`, nothing else touched. No FERPA/data
  angle (static bundled TypeScript source, not Canvas or student data).
- **CI status:** `total_count: 0` check runs, `mergeable_state: blocked`. Same fork-gate pattern as
  #340 — a first-time contributor's Actions runs are withheld pending "Actions tab → the run →
  Approve and run workflows"; `blocked` reflects required checks not having reported yet, not a
  failure.

**Next action:** approve the withheld workflow run (same step needed for #340); this looks
mergeable as-is once CI confirms green. Draft reply:

```
Thanks for this — nice catch, and I appreciate the regression tests with the cp932 shim; that's
the right way to make a locale bug visible on UTF-8 CI where it would otherwise stay invisible.
The fix looks correct and minimal. Approving the workflow run now so CI can actually execute;
I'll merge once it's green.
```

## New since 2026-09-04T01:46:27Z

**#351** (above) — the only new item. `search_issues(created:>2026-09-04)` returns nothing else
for this repo (other than this brief's own PR, opened after the search).

## Going stale

Same standing list as the last several briefs, no change (no activity on any of these since the
cutoff): **#172** (last non-Vishal touch: jonespm, 2026-08-04 — blocked on a New-Quizzes sandbox),
**#309 / #301 / #302 / #299** (reporters' last touch 12–19 days ago, no answer yet to open scoping
questions), **#318 / #325** (fixes already shipped in v1.12.0 — just waiting on reporter
confirmation).

## Checked, nothing needed

#142 (your own issue; no new activity), #338 (your own issue; tracked by PR #340, covered above),
#335 (automated weekly maintenance report, out of scope per instructions —
`weekly-maintenance.yml`), #157 / #236 (your own issues, no external activity since cutoff).

---

## Executor results (2026-09-05T02:20:23Z)

Executor agent run. Closing-keyword check on the brief and on the PR #352 body: both clean
(`scripts/check_closing_keywords.py --strict`, exit 0 on each).

### Local verification runs (substituting for the withheld fork CI)

Both open code PRs are **fork** PRs, so per the executor's action boundary I pushed nothing to
either branch (no rebase, no empty retrigger commit — those are same-repo-only). Instead I
checked both heads out read-only and ran the real checks locally.

| Ref | `uv run python -m pytest tests/ -q` | `uv run ruff check .` |
| --- | --- | --- |
| `main` (`e530092`) — baseline | **1456 passed, 22 skipped** | All checks passed |
| **#351** head `ec47950` | **1458 passed, 22 skipped** | All checks passed |
| **#340** head `f8cba18` | **1460 passed, 22 skipped** | All checks passed |
| **#340 trial-merged with `main`** (local, not pushed) | **1460 passed, 22 skipped** | All checks passed |

Zero failures in every run. Python 3.11 only (single interpreter in this sandbox) — CI's
3.11/3.12/3.13 matrix is **not** reproduced here, so this is a strong signal, not a substitute for
the required checks.

**#340 `behind` state confirmed benign, by measurement rather than inference.** I merged `main`
into a local throwaway branch off `f8cba18`: the merge applied cleanly (16 files, all triage
briefs + disjoint source) and the suite stayed at 1460. So the `mergeable_state: behind` in the
brief is staleness only, as prior briefs predicted.

**#351's tests are non-vacuous — verified in both directions.** I reverted only
`src/canvas_mcp/tools/discovery.py` to its `main` content and re-ran the discovery tests:
`2 failed, 13 passed` (`test_code_api_search_does_not_drop_files_on_non_utf8_locale`,
`test_code_api_signatures_readable_on_non_utf8_locale`). Restoring the fix: `15 passed`. That
matches bruchris's PR-body claim exactly. The cp932 monkeypatch shim really does make the bug
visible on UTF-8 CI.

### Corrections to the brief

1. **#340's "next action" is superseded, and acting on it would contradict a decision Vishal
   already made.** The brief carries forward "approve the withheld workflow run" plus a draft
   reply raising the leading-zero gap. Both are already done: Vishal posted a
   **CHANGES_REQUESTED review on 2026-09-01T13:10:35Z** (review `5078419562`, on head `f8cba18`)
   that raises the identical leading-zero finding with the same effective-value table, asks for
   `int(uid) == 0 or int(gid) == 0`, and states explicitly: *"I am leaving the fork workflow run
   unapproved until that correction lands; after the new commit I will approve and watch the
   checks."* So the withheld run on #340 is a **deliberate hold**, not an oversight — approving it
   now would undo that, and posting the draft reply would duplicate a four-day-old review. The
   ball is with k0shir0. The three prior briefs (09-02 / 09-03 / 09-04) appear to have carried
   this ask forward past the review that resolved it.
2. **The leading-zero gap is real and I measured it** (rather than reading the code and asserting).
   Running `validate_config()` with `TS_SANDBOX_UID_GID` set:

   | input | effective `ts_sandbox_uid_gid` |
   | --- | --- |
   | `0:0` | `65532:65532` (rejected, correct) |
   | `00:00` | **`00:00` (passes through)** |
   | `00:65532` | **`00:65532` (passes through)** |
   | `65532:0` | `65532:65532` (rejected, correct) |

   The value flows straight into the container invocation
   (`src/canvas_mcp/tools/code_execution.py:580`, `"--user", config.ts_sandbox_uid_gid`), and
   Docker parses `00` as uid 0 — so this is a genuine root-sandbox bypass of the guard the PR
   exists to add, not a cosmetic config-value issue. Confirms Vishal's own review finding.
3. **#351 is complete — no missed call sites.** The three reads it fixes are the *only*
   encoding-less text reads anywhere in `src/`; every other `open()` in `src/` is binary mode
   (`files.py:117,131`, `student_write.py:448,598`, `client.py:645`). And exactly two bundled TS
   files carry non-ASCII bytes (`canvas/grading/bulkGrade.ts`,
   `canvas/discussions/bulkGradeDiscussion.ts`) — matching bruchris's repro table precisely.
4. **Minor line-number drift.** The brief cites `discovery.py:130,145,162`; on `main` those reads
   are at lines **133, 148, 165**. The diff itself is exactly as the brief describes: three
   `read_text()` calls gain `encoding="utf-8"`, nothing else in the source touched.
5. **Both draft replies assert an action as already taken** ("Approving the workflow run now").
   Neither has been posted, and neither approval has happened — the executor cannot post or
   approve. For #340 that sentence should be **deleted** (see correction 1); for #351 it should be
   re-checked against whether Vishal actually wants to approve that run before the text says so.
6. **No unmeasured-Canvas-behavior claims** were found in either draft reply. Both concern local
   Python/CI behavior, all of which I verified above.

### Confirmed as accurate

- Cutoff and quiet-day claim: `search_issues(updated:>=2026-09-03)` returns **0** — re-run,
  reproduced.
- The "going stale" list (#172, #309, #301, #302, #299, #318, #325) — all seven still OPEN;
  none has activity newer than the cutoff.
- #351's read of the bug, the fix, and the FERPA assessment (bundled static TypeScript, no
  Canvas or student data) — verified against the diff.

### Needs Vishal

1. **#340 — no action, waiting on the contributor.** Do *not* approve the workflow run yet; your
   own 2026-09-01 review says you're holding it until the numeric-comparison fix lands. Nothing
   for you to do until k0shir0 pushes. (If you'd rather unblock CI while they work, that's a
   deliberate reversal of your stated hold, not a routine step.)
2. **#351 — ready for your approve-and-run, then a merge decision.** Fork Actions are withheld
   pending "Actions tab → the run → Approve and run workflows". Local signal is as good as it gets
   short of CI: full suite green (1458), ruff clean, tests proven non-vacuous, fix proven complete.
   Only the 3.12/3.13 matrix legs are unverified. Merge decision is yours — the executor may not
   merge any PR but the triage brief.
3. **Reply to bruchris on #351** — draft in this brief, minus the "approving the run now" sentence
   if you'd rather not commit to that in the same message. Worth noting they're a first-time
   contributor here who maintains a competing/parallel Canvas MCP server and found this by running
   our suite on Windows; the report quality is high (repro table across six code pages, honest
   note about the cp1252 mojibake case, offer to have us just apply the one-word change ourselves).
4. **Nothing else.** No issue needed a reply this cycle.

### Actions NOT taken, and why

- No comment, label, close, approval, or merge on any issue or PR other than this brief's PR #352.
- No push to #340 or #351 — both are fork branches; empty retrigger commits are same-repo-only.
- No workflow-run approvals (public/irreversible; also correction 1 for #340).
- The #340 trial merge was local-only, on a throwaway branch, never pushed.
