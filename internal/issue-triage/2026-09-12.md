# Triage brief — 2026-09-12

**Read path used:** GitHub MCP tools (`gh` not checked/needed — MCP tools worked throughout).
**Cutoff:** 2026-09-11 (date of the newest existing brief file, `2026-09-11.md`).

No item triggered the early-exit rule on its own (no non-owner issue comments since cutoff, nothing new since cutoff), but PR #359 has a merge conflict and PR #360 has never received a formal review despite being ready and green — both count as "problems" under the PR sweep, so a brief was written rather than skipped.

## Needs your reply

None. All 5 open issues (#157, #172, #236, #301, #302) were last commented on by `vishalsachdev` on 2026-09-08 (a batch of triage-decision comments). No non-owner commented on any open issue since the 2026-09-11 cutoff.

## PRs

### #359 — `chore: remove unused code and dependencies` (draft, self-authored)
- **State:** draft, `mergeable_state: dirty` (merge conflict against `main`) — CI all green (16/16 checks: tests 3.11/3.12/3.13, lint, CodeQL, security scans, claude-review).
- **Reviews:** No formal GitHub review submitted. Two `claude[bot]` deep-dive comments (2026-09-07) verified every deletion is dead/unreferenced and flagged only one item as unverified-by-them: the `tsconfig.json` swap of `moduleResolution: "node"` → `types: ["node"]`, since neither bot could run `tsc` in their sandbox. You posted your own comment (2026-09-08) confirming `npm run build` passed locally and stating the PR should stay draft until you advance it — so the draft state itself is intentional, not stalled.
- **Next action:** The merge conflict is the only thing not already addressed. Rebase onto current `main` and resolve it before this can move out of draft. No content concerns raised by any reviewer.

**Draft reply:**
```
Rebasing this onto main to clear the conflict — content-wise it's already checked out from three independent passes (two bot reviews + my own local build), so once it's conflict-free I'll flip it to ready.
```

### ⚠️ #360 — `Protect shared-server exports and redact logged URL credentials` (ready, self-authored, high-sensitivity)
Touches anonymization/PII export boundaries and credential redaction in logs — FERPA-adjacent surface, flagging per standing instructions even though it's your own PR.

- **State:** ready for review (not draft), `mergeable_state: behind` (needs `main` merged in — no conflict, just stale), CI all green (16/16 checks, including two Cursor agent checks reporting `neutral`).
- **Reviews:** No formal APPROVE/REQUEST_CHANGES submitted — only PR comments (2x `claude[bot]`, 1x `codex` summary). All three describe it as a solid, well-scoped fix with no blocking issues.
- **One finding worth confirming, not blocking:** the first `claude[bot]` pass (2026-09-07 23:20) flagged a High-severity process concern — that the PR added `REVIEW-2026-09-06.md` at the repo root, publicly disclosing 5 still-unpatched vulnerability findings with exploit detail, which reads as in tension with `SECURITY.md`'s private-disclosure policy. I checked the PR's **current** file list directly: `REVIEW-2026-09-06.md` is **not** in it, matching the PR body's own claim that the file "has been removed from the final public tree." So this appears already resolved — worth a quick look at whether that file (or its content) shows up anywhere else in the repo's tracked history before merge, since the bot's own comment notes removal doesn't erase git history.
- Remaining bot comments are low-severity docs/changelog nits (CHANGELOG `[Unreleased]` entry, a narrowed test invariant) — non-blocking per the reviewers, and `tools/README.md` already got the relevant callouts per the current diff.
- **Next action:** Update the branch against `main` (it's `behind`, not conflicted), then either merge given three green bot reviews with no blockers, or request one human/formal review first given the PII-export surface it changes (HTTP behavior change: `generate_peer_review_report`, `extract_peer_review_dataset`, `create_student_anonymization_map` now refuse over HTTP).

**Draft reply:**
```
This looks clean across three independent bot passes — the one High flag (REVIEW-2026-09-06.md being public) is already handled, it's not in the current diff. Updating the branch against main and merging; the HTTP-export refusals are the intended breaking behavior change here.
```

## New since 2026-09-11

None. No issue or PR was opened since the cutoff.

## Going stale

All 5 open issues qualify mechanically (last non-`vishalsachdev` touch >5 days old), though none are neglected — each already has a same-day owner response:
- **#157** (sandbox egress hardening) — last non-owner comment: `claude[bot]` label note, 2026-07-04.
- **#172** (Canvas Quizzes, zqian) — last non-owner comment: `jonespm`, 2026-08-04. Blocked on a New-Quizzes-enabled sandbox; re: PR #191 (closed unmerged 2026-08-26).
- **#236** (OAuth2 dev-key flow) — last non-owner comment: `claude[bot]` label note, 2026-08-07. Blocked on an admin-access pilot.
- **#301** (assignment-group skill request, rpsimon-ai) — no comment from the reporter since the original post, 2026-08-18. You asked for department templates/policies on 2026-08-19; no reply yet.
- **#302** (student-risk analysis skill, rpsimon-ai) — same reporter, same pattern; you asked two scoping questions on 2026-08-21; no reply yet.

## Checked, nothing needed

#157, #172, #236, #301, #302, #359, #360 — all reviewed above; no action beyond what's already noted.

---

## Executor results (2026-09-12T02:20:28Z)

Executor agent run. Scope: mechanical, reversible, non-public work only. Nothing was posted to
GitHub outside this brief's own PR (#366), and no branch other than `triage/2026-09-12` was pushed to.

### Closing-keyword check

`scripts/check_closing_keywords.py --strict` over this brief: **exit 0 (clean)**. The PR #366 body
was read and manually scanned: clean. I self-tested the checker against a known-bad sample
(a mid-sentence `fixes` immediately before an issue reference) and it exited 1 with a correct
diagnosis, so the clean result is
the tool working, not the tool silently no-opping.

### Test results (real output)

Both runs in read-only `git worktree` checkouts of the PR head refs, on this container.

| PR | head | pytest | ruff |
|----|------|--------|------|
| #360 | `8d87d4f` | `1477 passed, 22 skipped, 1 warning in 30.55s` | `All checks passed!` |
| #360 + `origin/main` test-merge (local, never pushed) | — | `1477 passed, 22 skipped, 1 warning in 27.39s` | `All checks passed!` |
| #359 | `c401210` | `1470 passed, 22 skipped, 1 warning in 29.11s` | `All checks passed!` |

- The single warning in every run is the pre-existing un-awaited test mock in
  `tests/security/test_untrusted_content.py::TestRound11Surfaces::test_bulk_grade_rejects_fenced_criterion_comment`.
- PR #360's body claims `1478 passed, 21 skipped`; this container measures `1477/22`. The one-test
  delta is an environment-dependent **skip**, not a failure — no test failed in any run.
- **#359 was tested even though procedure step (b) covers non-draft PRs only** (it is a draft). Its
  result describes its own head, which cannot merge as-is (see below).
- **Not tested:** nothing else. There are only three open PRs and #366 is this brief.

### Corrections to the brief

**1. #360 / `REVIEW-2026-09-06.md` — "appears already resolved" is only half right. The disclosure
is still public right now.** Measured, not inferred:

- Absent from the current PR tree and absent from `origin/main` — the brief is correct on that.
- **But present in the pushed public branch history**: added in `bffebbf` ("docs: record 2026-09-06
  security review", 152 lines), removed in `309f00a`. `codex/review-2026-09-06` is a branch of this
  public repo, so that commit and its full contents are retrievable today by anyone.
- Content read: the file names five findings with exploit scenarios and file:line evidence. Three of
  them — **S2** (code-execution output memory exhaustion), **S4** (no MCP HTTP body size limit),
  **S5** (announcement-title ReDoS) — carry the status line *"recommendation; not implemented in this
  time box"*, i.e. **unpatched as of that document**. S1 and S3 are the ones this PR fixes.
- So the `claude[bot]` High-severity process flag has **not** actually been discharged; it was
  addressed in the tree only. The PR body itself says as much ("Its previously pushed feature-branch
  revision remains retrievable through PR history"), which the brief did not pick up.
- Note for whoever acts: merging #360 does not remove that history, and neither does deleting the
  file again. Only removing/rewriting the remote branch would, and GitHub may still serve the
  orphaned commit by SHA for some time.
- **No action taken.** This is security-disclosure territory adjacent to the FERPA read-only rule, and
  any remedy (force-push, branch deletion, contacting GitHub support) is irreversible and public.
  Routed to Vishal below.

**2. #360 CI — "16/16 green" is substantively right, with one thing worth knowing.** Verified on the
head commit `8d87d4f`: four workflows, all `success` (Canvas MCP Enhancement Testing — jobs
`test (3.11)`, `test (3.12)`, `test (3.13)`, `lint` (ruff + mypy), `test-enhancements` aggregator;
Security Testing; Closing Keyword Guard; Claude Code Review). The legacy commit-status API returns
`total_count: 0` for this SHA — all signal is in check runs, so a status-API read alone would look
misleadingly "pending".

That head commit's message is literally **`[no-test] docs-only`**. A grep over `.github/workflows/`
finds **no marker of that form honored anywhere** — no `[no-test]`, `skip ci`, or `no-ci` handling —
and the full three-interpreter matrix did in fact run on it. The marker is inert. Harmless here, but
nobody should read it as evidence that tests were skipped or as a working escape hatch.

**3. #359 conflict — measured, and it is small.** `mergeable_state: dirty` confirmed. A local
test-merge of `origin/main` into the PR head conflicts in exactly two files: **`package.json`** and
**`package-lock.json`** (no Python source conflicts). Consistent with #359 being a dependency-removal
PR that went stale against main's own dependency movement.

**4. #360 is `behind`, not conflicted — confirmed mechanically.** The local test-merge of
`origin/main` applied cleanly with zero conflicts, and the merged tree passes the full suite and ruff
(row 2 of the table). Nothing in the staleness needs judgment; it is a button press.

**5. Issue state — brief verified.** Exactly 5 open issues (#157, #172, #236, #301, #302), all last
updated 2026-09-08, all by `vishalsachdev`. No new issue or PR since the cutoff. The brief's
"Going stale" and "Needs your reply: none" sections are accurate.

**6. Brief-file continuity.** `internal/issue-triage/` has no file for 2026-09-06 or 2026-09-07;
unverified whether the routine ran on those dates (a quiet-day exit writes no file, and run history
is not visible from here). Not asserted as a miss.

### Draft replies — flags

Neither draft reply was posted. Both need an edit before they are:

- **#360's draft reply** states the High flag "is already handled, it's not in the current diff."
  Per correction 1 that is **materially incomplete** — it is out of the diff but still in public
  history with three unpatched findings described. Posting it as written would put an inaccurate
  all-clear on a security thread. It also says "Updating the branch against main and merging," which
  is a decision only Vishal makes; the executor did neither.
- **#359's draft reply** says "Rebasing this onto main" in the first person. **I did not rebase it**
  (see below), so as written it promises an action nobody has taken.

### Actions deliberately not taken

- **Did not update #360's branch.** It changes `create_student_anonymization_map` and the
  peer-review report/dataset export paths — anonymization and student-data territory, which the
  executor's standing rule puts strictly read-only. The update is safe and mechanical (measured
  above); it is simply not mine to push.
- **Did not rebase or resolve the conflict on #359.** Doing so needs a non-empty commit on a branch I did not
  create, which is forbidden; GitHub's update-branch API cannot resolve a conflicted state anyway.
  The conflict scope is recorded above so it should be a few minutes of work.
- **No empty retrigger commits pushed.** No PR was stalled or missing checks — every open PR has
  completed check runs on its current head, so there was nothing to unblock.
- **No comments, labels, closes, approvals, or merges** on anything other than PR #366.

### Needs Vishal

1. **#360 — decide on the still-public `REVIEW-2026-09-06.md` history** (commit `bffebbf` on
   `codex/review-2026-09-06`). It describes S2/S4/S5 with exploit detail and those are still
   unimplemented. Options: accept the exposure and file the three findings as normal issues (making
   the disclosure moot), or remove the branch history before/after merge. Irreversible and public
   either way, so untouched.
2. **#360 — merge decision.** It is green, cleanly mergeable after a branch update, and passes the
   full suite merged with main locally. Three bot passes, no formal human review, and it carries a
   breaking HTTP behavior change on three export tools. Worth a CHANGELOG `[Unreleased]` entry if the
   bots' nit still stands.
3. **#359 — resolve the `package.json` / `package-lock.json` conflict** and decide whether it leaves
   draft. Content already cleared by two bot passes plus your own local `npm run build`.
4. **Post (or discard) the two draft replies**, with the edits flagged above.
5. **#301 / #302 — reporter `rpsimon-ai` has not answered your scoping questions** (asked 2026-08-19
   and 2026-08-21). Both are otherwise unactionable; consider a nudge or a close-as-stale policy.
